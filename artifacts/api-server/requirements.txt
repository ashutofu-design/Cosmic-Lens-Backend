flask
python-dotenv>=1.0.0
flask-cors
flask-compress>=1.17
flask-limiter>=3.8,<4
flask-sqlalchemy
psycopg2-binary
pyswisseph
python-dateutil>=2.9
gunicorn
werkzeug
firebase-admin
openai>=1.40.0
reportlab>=4.0
Pillow>=10.0
pypdfium2>=4.20
mediapipe==0.10.21
opencv-python-headless==4.10.0.84
numpy>=1.26,<2
pillow-heif==1.3.0
timezonefinder>=6.5,<7
rapidfuzz>=3.14,<4
redis>=5.0,<6
celery>=5.3,<6
flask-sock>=0.7,<1
simple-websocket>=1.0,<2
razorpay>=1.4,<2
pywebpush>=2.0
# Razorpay SDK imports pkg_resources (removed from setuptools 81+).
# This keeps us below the CVE-2026-59890 fix (83.0.0), which is a MANIFEST.in
# sdist-packaging bug on macOS — not reachable from a running server.
setuptools>=65.0,<81

# Security floor. Transitive via firebase-admin / pywebpush / pyjwt, but pinned
# here so the resolver cannot pick a version with the known JWT-path CVEs.
cryptography>=50.0.0
# App Check token verification (app_attestation.py) — was only transitive.
pyjwt[crypto]>=2.8

# NOTE: protobuf is held at 4.25.x by mediapipe==0.10.21 (protobuf>=4.25.3,<5),
# so CVE-2026-0994 (ParseDict recursion DoS, fixed in 5.29.6) has no available
# fix on this line. We never call json_format.ParseDict on untrusted input.
# Upgrading mediapipe (0.10.35+ relaxes the pin) is the route to clearing it.
