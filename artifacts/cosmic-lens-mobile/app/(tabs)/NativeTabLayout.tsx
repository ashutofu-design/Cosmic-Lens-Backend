/**
 * Web/Android stub — real native tabs live in NativeTabLayout.ios.tsx
 * so Metro does not pull native-tabs CSS into the web bundle.
 */
export default function NativeTabLayout() {
  return null;
}
