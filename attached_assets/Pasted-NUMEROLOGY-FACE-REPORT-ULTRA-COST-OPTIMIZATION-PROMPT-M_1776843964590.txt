NUMEROLOGY + FACE REPORT — ULTRA COST OPTIMIZATION PROMPT (MINI ONLY)

Goal:
Refactor report generation to minimize OpenAI cost using ONLY gpt-4.1-mini while maintaining high-quality output.

---------------------------------------

CURRENT ISSUES:

- 93 OpenAI calls per report ❌
- Repeated prompts causing token waste ❌
- No caching ❌
- Over-generation of unnecessary sections ❌
- High concurrency causing 429 errors ❌

---------------------------------------

TARGET:

- Max 15–20 OpenAI calls per report
- Use ONLY gpt-4.1-mini
- Reduce cost by 80–90%
- Maintain readable, premium Hinglish output

---------------------------------------

STEP 1 — BATCH SECTION GENERATION (CRITICAL)

Replace:
1 section = 1 API call

With:
1 API call = 4–6 sections

Implement section groups:

BATCH 1:
- TLDR Summary
- Power Summary
- Archetype

BATCH 2:
- Personality (OCEAN)
- Mask vs Real Self
- Decision Style

BATCH 3:
- Love & Relationship
- Compatibility

BATCH 4:
- Career
- Money

BATCH 5:
- Health
- Lifestyle

BATCH 6:
- Remedies
- Action Plan
- Final Truth

Each call returns structured JSON:
{
  "section_name": "text"
}

---------------------------------------

STEP 2 — REMOVE PROMPT REPETITION

- Define ONE global system prompt
- DO NOT resend long instructions every call
- Keep system prompt concise (<300 tokens)

---------------------------------------

STEP 3 — TOKEN CONTROL

Update logic:

max_tokens = word_target * 1.5

Hard cap:
MAX_TOKENS_PER_CALL = 500

---------------------------------------

STEP 4 — CACHE SYSTEM (MANDATORY)

Create cache layer:

cache_key = hash(name + dob + language + version)

Flow:

IF cache exists:
    return cached result

ELSE:
    call OpenAI
    store response

TTL: 24h or permanent

---------------------------------------

STEP 5 — SKIP LOW VALUE GENERATION

Before calling AI:

IF:
- data empty
- duplicate insight
- already covered

THEN:
→ skip API call

---------------------------------------

STEP 6 — REDUCE CONCURRENCY

Change:

ThreadPoolExecutor(6) → (3)

---------------------------------------

STEP 7 — REMOVE AI FROM STATIC PARTS

DO NOT use OpenAI for:

- Lucky numbers
- Basic lists
- Repeated toolkits
- Fixed explanations

Use templates instead.

---------------------------------------

STEP 8 — SAFETY LIMITS

Add:

DAILY_LIMIT_USD = 3
PER_REPORT_LIMIT_USD = 0.10

If exceeded:
→ fallback to template text

---------------------------------------

STEP 9 — FINAL PIPELINE

1. Engines → structured JSON
2. Merge + clean data
3. Batch AI calls (15 max)
4. Cache results
5. Assemble sections
6. Generate PDF

---------------------------------------

EXPECTED RESULT:

- API calls: 93 → 15
- Tokens: ↓ ~75%
- Cost: ₹40 → ₹3–₹5
- No more 429 errors

---------------------------------------

RULES:

- Use ONLY gpt-4.1-mini
- Do not increase number of calls
- Do not generate duplicate content
- Keep output human-like but concise

---------------------------------------

END PROMPT