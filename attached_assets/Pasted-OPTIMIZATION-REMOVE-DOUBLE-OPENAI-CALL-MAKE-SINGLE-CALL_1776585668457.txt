OPTIMIZATION — REMOVE DOUBLE OPENAI CALL (MAKE SINGLE CALL SYSTEM)

Current issue:
- Validator retry causing 2 API calls per request
- Cost and latency doubled

Goal:
Move to single-call deterministic narration system

------------------------------------------
STEP 1 — PRE-COMPUTED NARRATION TEMPLATE
------------------------------------------

In backend:

Create function:
format_final_answer(engine_output, language)

Example input:
{
  "verdict": "Marriage promised with delay",
  "timeline": "June 2026 to October 2026",
  "reason": "...",
  "remedy": "..."
}

Generate structured text BEFORE AI:

Example:
"Shaadi ka yog hai, lekin delay dikhta hai.

Reason:
7th lord strong hai lekin Saturn aur Venus weak hai.

Timing:
June 2026 se October 2026.

Remedy:
..."

------------------------------------------
STEP 2 — AI ROLE = TRANSLATOR ONLY
------------------------------------------

AI will:
- Convert tone (Hinglish / English / Hindi)
- Make it natural
- NOT change content

------------------------------------------
STEP 3 — REMOVE VALIDATOR RETRY
------------------------------------------

- Remove "retry if mismatch" logic
- No second API call

------------------------------------------
STEP 4 — STRICT PROMPT
------------------------------------------

"You must NOT modify any facts, numbers, or dates.
Only rephrase language."

------------------------------------------
FINAL RESULT:

- 1 API call only
- Faster response
- Same accuracy
- Lower cost

=== END ===

And one more thing 

ADD CONSTRAINT-AWARE FOLLOW-UP LOGIC

Problem:
User may reject given timeline (e.g. "Nov nahi chahiye")

Goal:
System must adapt and give NEXT VALID timeline

------------------------------------------
STEP 1 — DETECT USER CONSTRAINT
------------------------------------------

If user says:
- "yeh time nahi chahiye"
- "next year batao"
- "is date ke baad batao"

→ mark constraint_active = true

------------------------------------------
STEP 2 — ENGINE UPDATE
------------------------------------------

Modify marriage_engine:

- After computing primary window
- Also compute NEXT possible window

Logic:
- Find next dasha/antardasha of 2/7/11 significator AFTER current window
- Apply Jupiter transit again

------------------------------------------
STEP 3 — RESPONSE LOGIC
------------------------------------------

IF constraint_active:

Return:

"Given time avoid karne par next strong window: {next_window}"

------------------------------------------
STEP 4 — NO AI GUESS
------------------------------------------

- AI must NOT guess next year
- Use ONLY engine computed next window

------------------------------------------
FINAL RESULT:

- System behaves like real astrologer
- Handles follow-up intelligently
- No random answers

=== END ===