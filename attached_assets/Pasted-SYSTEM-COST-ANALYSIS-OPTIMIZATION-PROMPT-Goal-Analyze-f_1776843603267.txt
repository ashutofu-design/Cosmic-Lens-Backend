SYSTEM COST ANALYSIS & OPTIMIZATION PROMPT

Goal:
Analyze full OpenAI API usage for Face Reading + Numerology report generation system and return exact cost breakdown + optimization plan.

---------------------------------------

INPUT CONTEXT:
- System generates 40–90 page reports
- Uses multiple engines (Anthropometry, Samudrika, OCEAN, Health, etc.)
- OpenAI used ONLY in narrative_writer (Hinglish generation)
- Reports include 20–45 sections
- PDF generation after content creation

---------------------------------------

TASKS:

1. COST BREAKDOWN (MANDATORY)
Calculate and return:

- Total tokens used per report
- Tokens used per section
- Tokens used in narrative_writer only
- Number of OpenAI API calls per report

Return in this format:

{
  "total_tokens": X,
  "total_cost_usd": X,
  "cost_per_report_inr": X,
  "sections": [
    {
      "section_name": "Power Summary",
      "tokens": X,
      "cost": X
    }
  ],
  "narrative_writer_percentage": "X%"
}

---------------------------------------

2. API CALL TRACE

Log exact flow:

- Which function triggers OpenAI
- How many times called
- Any duplicate/retry calls
- Any loop causing repeated generation

---------------------------------------

3. BUG DETECTION (IMPORTANT)

Detect:

- Same section generated multiple times
- Empty retries / fallback calls
- Large token spikes (>1000 tokens per call)
- Any unnecessary OpenAI usage

---------------------------------------

4. COST HOTSPOT IDENTIFICATION

Identify top 3 cost-heavy areas:

Example output:
1. narrative_writer (75%)
2. repeated section generation (10%)
3. formatting rewrite (8%)

---------------------------------------

5. OPTIMIZATION PLAN (MANDATORY)

Provide exact fixes:

- Reduce tokens per section (target: 80–120 words)
- Limit OpenAI calls (max 8–10 calls per report)
- Cache repeated outputs
- Move static text to templates (no API)
- Use OpenAI ONLY for final polish

---------------------------------------

6. SAFE MODE IMPLEMENTATION

Add safeguards:

- max_tokens cap per call
- prevent duplicate generation
- block regeneration if cached exists

---------------------------------------

7. FINAL OUTPUT

Return clean summary:

- Current cost per report
- Optimized cost per report
- % cost reduction possible

---------------------------------------

RULES:

- DO NOT guess — use actual logs/data
- DO NOT skip any section
- KEEP output structured + precise
- NO generic suggestions

---------------------------------------

END PROMPT