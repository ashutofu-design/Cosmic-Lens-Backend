# Cosmic Lens — Workspace

## Overview

Cosmic Lens is a pnpm workspace monorepo using TypeScript, focused on delivering a mobile Vedic Astrology application. The project aims to provide a comprehensive and accurate astrological analysis tool, moving beyond traditional interpretations to offer modern-context reframing and actionable insights. Key capabilities include in-depth kundli calculations, numerology, Vastu analysis, and AI-driven astrological interpretations. The long-term vision is to achieve industry-leading accuracy (97%+) through a robust calculation engine, a sophisticated AI brain, and continuous learning from user feedback.

## User Preferences

- The user wants the AI to act as a replit coding agent.
- The user prefers a strict development methodology: `engine.py file (sahi folder mein)`, `Unit test (test_engine.py)`, `locked_facts.py wiring`, `Rule entry in system prompt`, `Deterministic post-injector`, `3x smoke test on real chart (/tmp/k.json, /tmp/req.json, /tmp/qm.json)`, `Architect review (code_review skill)`, `replit.md entry` are mandatory for every new engine.
- The user has provided a clear roadmap for project phases and expects strict adherence to it. Deviations require explicit user confirmation.
- The user expects the AI to avoid hallucinating, especially regarding timing-related questions.
- The user wants the AI to integrate classical Vedic knowledge through RAG for opinion-based questions, but strictly NOT for timing questions.
- The user has defined a brand voice: "Powered by Advanced Cosmic Intelligence" and explicitly states "AI never named; AI = language layer only; backend = facts; post-injectors = mandatory citation safety net."
- The user prefers deterministic post-injectors to ensure mandatory facts are cited even if the AI misclassifies a topic.
- The user prefers that the AI refine language for opinion questions but must cite engine facts.
- The user wants medical outputs to include a mandatory medical disclaimer banner and a 3-confirmation rule for severity tiers.
- The user wants financial outputs to include a mandatory financial disclaimer banner and a tiered risk profile.
- The user wants remedies outputs to include a mandatory disclaimer ("Remedies SUPPLEMENT, never substitute action") and provide both free and paid alternatives.
- The user wants Astrocartography outputs to include a mandatory disclaimer ("energetic affinity, not guaranteed luck").
- The user expects ethical and safety disclaimers for sensitive topics like medical, lifespan, death prediction, and destiny vs. guidance.
- The user prefers a "Numerology PDF Pro" branding for the report, with a hybrid numerology + Vedic Astrology content, framed with numerology language.

## System Architecture

The project is a pnpm workspace monorepo utilizing Node.js 24 and TypeScript 5.9.

**Mobile App (Expo React Native):**
- **Artifact**: `artifacts/cosmic-lens-mobile`
- **Navigation**: Bottom tabs for Home, Kundli, AI Chat, Insights, Notifications, Profile.
- **Features**: Daily Rashifal, Lucky elements, Panchang, Kundli Milan, Muhurat finder, Numerology, Remedies, Vastu tips, Dosha analysis, Forecast.
- **UI/UX**: `CosmicBg.tsx` for full-screen nebula background; `ThemeContext.cardShadow` for card glow; glassmorphism effect using semi-transparent `rgba` backgrounds; zodiac-based accent theming driven by user birth data; dark/light theme support with persistence.
- **Localization**: i18n support for 25 languages (12 Indian, 13 global) with region detection and persistence.
    - **Two-layer localization architecture**:
      - **UILang (25 langs)**: Shared UI chrome keys via `lib/i18n.ts` (`getT(language)`) — tab labels, common button text, error messages.
      - **VLang (3 buckets — `en` / `hn` / `hi`)**: Inline labels for Vedic-specific content via `lib/i18nVedic.ts` (`vedicLang(language)`). Languages without dedicated translation fall back to `hi` (Devanagari) bucket.
    - **Strict no-leakage rule**: When user picks Hindi, ZERO English/Tamil words should appear; when English picked, ZERO Devanagari characters appear. Demo data, tab labels, card titles, error messages, time/date pickers, and all chrome obey this rule.
    - **Phase 1 BLOCKER files migrated**: `app/_layout.tsx`, `app/(tabs)/_layout.tsx`, `app/(tabs)/index.tsx` (home), `app/onboarding.tsx`, `app/dosh.tsx`. Each uses inline `getXxxLabels(v)` triplet helpers + `getDemoXxxList(v)` for vlang-bucketed content.
    - **Phase 2 hardcoded-string sweep complete (2026-04-25)**: Added ~50 new keys (`vu_al*`, `prof_al*`, `sub_av*`, `dl_*`, `car_*`, `fn_*`, `hl_*`, `rl_*`, `pe2_*`) auto-translated across 21 langs (gpt-5-mini batched). Replaced hardcoded English/Hinglish in `vastu.tsx` (~22 Alert.alerts), `(tabs)/profile.tsx` (push-notif alerts + footer), `profile-edit.tsx` (Cancel/Delete + delete-warning), `subscription.tsx` (AstroVastu pricing card), `daily-alerts.tsx` (Lucky labels + EmptyState), and life-area screens (`career`, `finance`, `health`, `relationship`). Translation pipeline at `scripts/i18n-translate/` (resumable per-lang, EN-829 keys × 25 langs). MoreT type now ~830 keys; access via `useT()` hook in `hooks/useT.ts` which merges base `getT(language)` + `getMoreT(language)`.
    - **Phase 3 full Vedic-vocabulary 25-lang complete (2026-04-25)**: Translated 109 Vedic terms (12 rashi, 9 planet, 7 day, 8 dir, 16 color, 5 metal, 5 element, 9 gem, 11 deity, 27 nakshatra) across 22 langs via category-aware pipeline (`scripts/i18n-translate/translate-vedic.mjs`): astronomy/calendar terms translated to native; Sanskrit deity + nakshatra names transliterated phonetically. Regenerated `lib/i18nVedic.ts` with `LangMap = Record<UILang, string>` for vocabulary alongside legacy `Triplet = {en,hn,hi}` for paragraph content. `pick()` smart-fallback: full lang → 3-bucket → en. Verified samples: zh 白羊座/火星/拉胡/哈奴曼/阿什维尼; ar الحمل/المريخ; ta மேஷம்.
    - **Phase 5 deferred-content 24-lang complete (2026-04-25)**: Expanded 6 deferred content tables from 3-bucket VLang to full 24-lang LangMap: months (full+short), demo labels, numerology Pythagorean themes (1-9/11/22/33), Tara types (Janma..Paramamitra), Karaka types (AK..DK), Vastu room loc (8 rooms × idealDir/importance/dos/donts/remedies). Pipeline at `artifacts/cosmic-lens-mobile/scripts/i18n-content/`: extract → `content-en.json` (186 composite-key strings) → `translate-content.mjs` (BATCH_SIZE 50, CONCURRENCY 8, gpt-5-mini, resumable per-lang JSON in `out/`) → `gen-i18nContent.mjs` regenerates `lib/i18nContent.ts` (459.8 KB) with typed helpers `getMonthsFull`/`getMonthsShort`/`getDemoLabels`/`getPYTheme`/`getTaraData`/`getKarakaDefs`/`getRoomLoc`. Refactored 5 consumer files (onboarding, home, numerology, kundli, vastu) to drop hardcoded VLang chains in favor of single-call helpers keyed by `t.lang`. **Backend 25-lang summary support (2026-04-25)**: New `i18n_summary.py` adds `localize_response(report, lang)` that walks BV/AVP responses and adds `summary_loc`/`why_loc`/`reason_loc` fields next to existing `*_en/*_hi` pairs — passthrough for en/hi, OpenAI-translated (gpt-4o-mini) with thread-safe in-memory LRU cache (5000 entries, FIFO eviction) for the other 22 langs. Hooked into `flask_app.py` BV (`/api/business-vastu`) and AVP (`/api/astrovastu-pro`) routes immediately before `jsonify(report)`. Mobile `business-vastu.tsx` and `astrovastu-pro.tsx` types extended with optional `*_loc?` fields and JSX prefers `summary_loc || summary_en` to render single-language output.
    - **Phase 3 full UI-label 25-lang complete (2026-04-25)**: Migrated `getKundliLabels` (26 keys: MAHADASHA/ANTARDASHA/PRATYANTARDASHA/Navatara/Jaimini/KP/Transit), `getProfileLabels` (10 keys: India/Global/ACTIVE/FREE/MY DATA/etc), Vastu Compass L block + tabLabels (11 keys), kundli BAV labels + Transit disclaimer (9 keys), Subscription PREMIUM/BEST VALUE badges, kundli ternary text (Ashtakavarga/Approximate Transit/House) → all use `useT()` t.* references. EN-891 keys × 25 langs.
    - **Phase 3 kundli deep-cleanup (2026-04-25)**: Added 28 more keys to `getKundliLabels` covering all `CHART_BTNS` labels + section titles + snapshot rows + Jaimini/KP/SAV literals. Replaced hardcoded `CHART_BTNS` label/sec via `chartBtnLabel(tab,L)` / `sectionTitleFor(tab,L)` helpers. Replaced inline `"Nakshatra: …"`, `"Degree within sign: … — highest in chart"`, KP English description, KP Hinglish footer ("Kisi bhi ghatna…"), `KPLordChip` Star/Sub/Sub-Sub labels, `"Asc"` chip, `"Ascendant"` full label (uses `L.snapAscendant`), header fallback `"Kundli"` (uses `t.tabKundli`), and `"Sarvashtakavarga"` heading with `L.*` / `t.ku_*` references. EN-919 keys × 25 langs.
    - **Deferred Vedic content data tables** (out of original 17-file Phase 2 plan, similar to `vastu.tsx` `ROOMS_LOC`): `kundli.tsx` `TARA_DATA` (9 Navatara entries with `name`/`desc`, English-only with `nameHindi` partial), `KARAKA_DEFS` (8 Jaimini Karaka entries with `name`/`nameHindi`/`desc`), and the bare `MONTHS = ["Jan",…,"Dec"]` array used in birth-date `formatDate()`. These render English content inside otherwise localized chrome; full 25-lang coverage would add ~450 entries and is queued as a future pass.
    - **Phase 6 RTL (right-to-left) layout for Arabic complete (2026-04-25)**: Added full RTL layout support so Arabic users see a properly mirrored UI. Infrastructure: `lib/rtl.ts` exposes `isRTLLang()` (RTL_LANG_SET={"ar"}) and `applyRTLForLang(lang, {silent})` which calls `I18nManager.allowRTL/forceRTL` then reloads via `expo-updates` `Updates.reloadAsync()` (fallback `DevSettings.reload()` in dev). Module-scoped `_rtlApplyInFlight` guard prevents simultaneous reload races; alert prompt for user-initiated path, silent for boot path. `hooks/useRTL.ts` exposes `{isRTL, flipIcon, chev, chevBack}`. Wiring: `UserContext.setLanguage` triggers prompted apply on user lang change; `UserContext` load effect runs ONCE post-hydration with the resolved saved language to enforce direction (handles "first launch with saved Arabic" auto-flip). 40 directional Feather icons across 24 files (back arrows, forward chevrons, day paginators, dasha progression, list disclosure) auto-flip via `name={I18nManager.isRTL ? oppositeIcon : originalIcon}`. Dependency added: `expo-updates`. PDF reports stay LTR (out of scope). Architect approved (Pass) after second-pass fixes addressing reload-loop race + duplicated apply path. TS clean, Web Bundled OK (1833 modules).
    - **Risk Radar v1.0 (2026-04-26)**: Replaced legacy "Risk Alert" dasha screen with predictive Risk Radar surface. **Backend** (`energy_engine.py` ~+260 lines, `flask_app.py` `/api/risk-radar`): pure signal-mapping layer over existing v3.2 `calculate_energy()` output — NO new astrology compute. `compute_risk_radar(energy_result, birth_chart, today_planets)` collects severity-ranked risk candidates from 8 engine signals (chandrashtama=9 high, Tara Naidhana idx6=8 high / Vipat-Pratyak idx2,4=6 medium, Saturn Madhya/Ashtam=8 high / Phase1/3/Kantaka=5 medium, Mars affliction delta<-3 OR house in {1,4,7,8,12}=7 medium, PD weak delta≤-4=6 medium, Tithi Rikta or Amavasya idx30=5-6 medium, Rahukal active=4 low, volatile_day flag=10 high), sorts by severity, returns max 3 in `risk_radar_24h`; baseline "Stable Day" if none. `_compute_7d_blocks(birth_chart, today_planets, saturn_d, jup_mars_d, volatile)` projects Moon longitude forward 7 days at ~13.176°/day, computes per-day Tara nakshatra index from natal nakshatra, scores via existing TARA_SCORES, applies persistent baseline penalties (Saturn Madhya/Ashtam −12, mild −6, Mars affliction −8, today volatile −10), then aggregates into 3 blocks (Day 1-2, 3-5, 6-7) with thresholds avg<35 OR ≥2 weak days → "Sensitive Window" high / avg<50 OR ≥1 weak → "Mixed Phase" medium / avg≥70 → "Smooth Phase" low / else "Stable Phase" low. Hinglish summary line tracks high count (≥2 → sambhalein, =1 → ek area, =0 stable → aaram, else mixed). Output schema strict: `{risk_radar_24h:[{level,title,reason,advice,timing?}], risk_radar_7d:[{range,level,label,advice}], summary, date, score}`. **Hard rules enforced**: NO MD/AD/PD/planet names exposed in output text, NO "danger"/"bad future"/scary language, every entry has actionable Hinglish advice. **Route** mirrors `/api/energy/today` auth pattern (user_id+API key OR stateless kundli body), supports `?date=YYYY-MM-DD` for back-dated runs, computes today_planets via swisseph (Sun/Moon/Mars/Mercury/Jupiter/Venus/Saturn + Rahu/Ketu mean nodes), forwards birth lat/lon/tz_offset to engine for accurate per-city sunrise/sunset/Rahukal/Choghadiya/Hora. **Frontend** (`app/dasha-risk.tsx` complete rewrite ~430 lines): replaces old MD→AD→PD tier UI with Risk Radar consumer — header + Risk Radar title, summary card with 24h verdict + Energy Score 0-100, "Next 24 Hours" section listing top-N risk cards (icon ⚠️/🌗/✅, level pill, reason in Hinglish, "Kya karein" advice box, optional timing row), "Next 7 Days" section with 3 block cards (Day 1-2 / 3-5 / 6-7 each with range pill + level pill + label + advice), honest accuracy note ("probability hai certainty nahi") + "Powered by Advanced Cosmic Intelligence" footer. Pull-to-refresh, loading/error states, retry button. RTL-ready (back arrow flips via I18nManager.isRTL). Uses `apiFetch`/`API_BASE` for tunnel-bypass. **Home tile** (`app/(tabs)/index.tsx`): renamed "Risk Alert" → "Risk Radar" / "जोखिम रडार" in 3 lang variants (hi/hn/en); tile subtitle now "Next 24h + 7-day signals" (active-dasha-name string superseded since destination no longer exposes MD/AD names). `BadTimeMini` onPress still pushes `/dasha-risk`. TS clean. Smoke test (Delhi/IST kundli, today 2026-04-26): score=60, summary "Aaj kuch mixed signals hain", 24h=2 risks (Conflict Risk medium + Mental Drain medium), 7d=3 blocks (Mixed/Sensitive/Smooth) — all output rules verified.
    - **Risk Radar v3 PREMIUM RADAR UI (2026-04-26)**: Final attractor pass on dasha-risk screen so the radar feels expensive and "alive" enough to keep first-time users engaged. Builds on v1.0 (still uses identical backend `/api/risk-radar` + `compute_risk_radar()` — zero compute changes). **Removed**: 7-day blocks section + accuracy disclaimer note (kept only "Powered by Advanced Cosmic Intelligence" footer per global no-AI-language rule). 24h-only with hard 0-3 signal cap. **v2 layer (ship-style sonar)**: 280px SVG with cosmic radial-gradient bg, 32 background stars, 24 tick marks (cardinals brighter), 4 concentric severity rings (middle dashed), 8 spokes (cardinal+diagonal), N/E/S/W compass labels, 2 staggered sonar pings (cyan + light-cyan), rotating 70° sweep wedge with glowing leading edge + tip glow, pulsing dual halo (outer/inner), status bar ("SCANNING • LIVE • {N} SIGNALS" with pulsing cyan dot), risk dots placed via golden-angle 137.5° distribution at severity-based radii (high=R*0.35 inner / medium=*0.62 / low=*0.85), each dot = ripple ring + soft glow + solid dot + numbered badge (1/2/3). **v3 PREMIUM upgrades layered on top**: (1) **Holographic shimmer bezel** — extra rotating Animated.View layer (8s loop) carrying its own SVG with `SvgLinearGradient` (4-stop cyan→violet→light-cyan→amber, aliased import to avoid clash with `expo-linear-gradient`) painting outer bezel stroke + inner accent ring; gradient appears to slowly cycle around the rim. (2) **Twinkling stars** — 6 of the 32 BG_STARS upgraded to `AnimatedCircle` with phased opacity worklet (`0.2 + |0.5 − ((twinkle + phase) % 1)| * 1.6`), each star offset by `i/6` so they twinkle staggered, light-cyan tint distinguishes them from static white BG stars. (3) **Soft radial center glow** — extra `RadialGradient` "centerGlow" disc (45% radius, light-cyan→cyan-fade) under the hub for warm depth. (4) **Premium 5-layer center hub** — concentric circles r=11/7/4.5/2.5/1 in cyan-fade → cyan → light-cyan → white-95 → pure-white for a jewel-like core. (5) **Degree labels** — 12 subtle `SvgText` ticks every 30° (skipping cardinals to avoid N/E/S/W clash), 7px cyan @ 0.45 opacity for a "technical readout" feel. (6) **4-line sweep glow stack** — leading edge now 4 stacked Lines (cyan w=6 op=0.22 → light-cyan w=4 op=0.50 → pale-cyan w=2 op=0.95 → white w=0.8 op=0.85) plus 3-layer tip glow (cyan-25 w=8 → pale-cyan-85 w=5 → white w=2.5) for cinematic radar-blade quality. (7) **4 orbital particles outside the bezel** — `ORBITS` constant defines 4 differently-sized/colored dots (cyan r=14, violet r=11, amber r=10, light-cyan r=12) at radii R+14/+18/+22/+26, each lives inside its own RADAR_SIZE-sized rotating Animated.View wrapper (durations 6s/8.5s/11s/14s, alternating CW/CCW for parallax), default RN transform-origin = wrapper center = radar center → perfect circular orbits. Particles use `backgroundColor` + light `shadow*` (opacity 0.7, radius 4, elevation 3) for halo without killing mid-range Android frame rate (architect-recommended trim from initial 0.95/6/6 spec). **New shared values**: `twinkle`, `shimmer`, `orbit0..3` (5 added on top of existing sweep/halo/dotPulse/ping1/ping2). All driven by `withRepeat(withTiming(...))` in a single `useEffect`. **New constants**: `TWINKLE_INDICES` (Set of 6 indices to skip in static BG_STARS render), `TWINKLE_STARS` (6 entries with phase offset), `DEGREE_LABELS` (12 entries with cardinal flag), `ORBITS` (4 entries with radius/size/color/duration/dir). **Empty state preserved**: when `risks.length === 0`, "ALL CLEAR" overlay renders inside the radar with green accent (no dots placed). **Hard rules preserved**: zero new banned terms, no planet/MD/AD/PD names, no "danger"/"bad"/scary language, footer stays "Powered by Advanced Cosmic Intelligence" — backend signal text unchanged. TypeScript clean (`tsc --noEmit` green), Web Bundled OK, both workflows running. Architect review: orbital geometry correct, worklet math correct, SVG aliasing correct, performance acceptable on modern devices (lightly trimmed shadows for older Android). File: `app/dasha-risk.tsx` (~1093 lines, RadarView + animation drivers + render layers).
    - **Daily Energy Score v3.2 LIMITATION-FIX patch (2026-04-26)**: User-requested fix for the 5 honest limitations of v3.1.1 — accuracy now ~88-90% relatable. **(LF1 — Per-city sunrise/sunset)**: new `_astronomical_sun_times(date_obj, lat, lon, tz_offset)` implements NOAA Solar-Position-Algorithm in pure Python (~30 lines: day-of-year → fractional year γ → equation-of-time + solar declination → hour-angle → sunrise/sunset UTC → local). Accurate ~1 min for civilian use, falls back to seasonal table on polar day/night or compute error. **(LF2 — Day/Night unequal hora)**: `_current_hora_lord` rewritten to classical day/12 + night/12 system. Day hora length = (sunset − sunrise)/12, night hora length = (24 − day_length)/12. First day-hora at sunrise = weekday lord, then Chaldean cycle continues across both day and night (24 horas/day total). Returns `(lord, period)` where period ∈ {"day", "night"}. **(LF3 — Degree-based aspects with orbs)**: new `_angular_separation(a,b)` and `_aspect_strength(actual, exact, orb)` helpers (linear falloff inside orb, 0 outside). `compute_transit_natal_aspects` now accepts optional longitude args (`saturn_today_lon`, `mars_today_lon`, `jupiter_today_lon`, `birth_moon_lon`, `birth_sun_lon`) — when ALL provided, switches to degree mode with orbs Saturn 8° / Mars 6° / Jupiter 7°. Strength-scaled deltas (e.g. Saturn opp natal Moon: full −12 at exact, 0 at 8°+). Sign-mode preserved as fallback. Detail block exposes `mode: "degree"|"sign"` and per-aspect `strength`/`orb_deg`. **(LF4 — Full Choghadiya muhurat)**: new `_DAY_CHOGHADIYA` + `_NIGHT_CHOGHADIYA` tables (8 segments per period, 7 weekdays × 16 = 112 segments) covering classical Amrit/Shubh/Labh/Char/Udveg/Rog/Kaal sequence. `_current_choghadiya(weekday, h, m, sunrise, sunset)` returns `(name, period)`. `_CHOGHADIYA_DELTA = {Amrit:+4, Shubh/Labh:+3, Char:0, Udveg:-3, Rog/Kaal:-4}`. Time-quality overlay refactored: Rahukal stays separate (-6, classical hard avoid window), Choghadiya + Hora delta combined and capped to ±5 to avoid double-stacking similar concepts. Hora weight reduced from ±3 to ±2 (Choghadiya is now primary time-quality signal). Response now exposes `overlays.time_quality.choghadiya`/`choghadiya_period`/`choghadiya_delta`/`hora_period`/`sunrise`/`sunset`/`sun_source` ("astronomical"|"seasonal_fallback"). **(LF5 — Lagna + Chandra-lagna weighted blend)**: new `_house_delta_for_planet` helper. `compute_jupiter_mars_overlay` and `compute_pd_transit_overlay` now compute the transit's house from BOTH lagna and natal Moon (Chandra lagna), weight 60/40 (lagna primary, Moon secondary per most classical schools). Auto-renormalizes when only one reference is available. Detail blocks expose `jupiter_house_lagna`/`jupiter_house_moon`/`mars_house_lagna`/`mars_house_moon`/`pd_house_lagna`/`pd_house_moon` + `blend: "lagna_60_moon_40"`. Mars-aspect-Moon extra −4 still applies (it's already Moon-relative by definition). **Backend route** (`flask_app.py:/api/energy/today`): now accepts optional `birthData` in body OR auto-fetches from user's primary Profile.birth_data when authed; extracts lat/lon/tzOffset; passes to `calculate_energy(birth_lat, birth_lon, tz_offset)`. Defaults to IST (5.5) tz on missing data, no-op fallbacks throughout (engine still produces valid scores when lat/lon missing — uses seasonal table). Sade Sati Madhya hard cap=59 preserved untouched. **Smoke test verified**: Delhi/IST request returns `sun_source:"astronomical", sunrise:5.758, sunset:18.873, choghadiya:"Kaal", hora_lord:"Mars", aspects.mode:"degree", jupiter_mars.blend:"lagna_60_moon_40", pd_transit.blend:"lagna_60_moon_40"` for Sun Apr 26 2026 noon.
    - **Daily Energy Score v3.1.1 STABILITY-PATCH (2026-04-26)**: Senior-engineer 4-fix patch applied on top of v3.1 to prevent incorrect scoring, double penalties, and unrealistic extremes — accuracy maintained ~85-88%, no scoring weights changed, no new features, no API structure broken. **(FIX 1 — Saturn double-penalty guard)**: `compute_transit_natal_aspects` now accepts `skip_saturn: bool = False`. `calculate_energy` passes `skip_saturn=bool(saturn_d.get("active"))` so when ANY Saturn-from-Moon overlay (Sade Sati Phase 1/2/3, Kantaka 4th, Ashtam 8th — together "Sade Sati / Dhaiyya") is already firing, the Step-1 Saturn-opposition-Moon −12 block is suppressed to prevent counting Saturn pressure twice. Mars + Jupiter aspects continue to apply normally. **(FIX 2 — Global negative-stack cap = −35)**: in `calculate_energy` after all 7 overlays computed (saturn / tithi / md_sandhi / aspects / jupiter_mars / time_quality / pd_transit), sum the negative-only deltas. If `neg_sum < −35`, scale every negative delta proportionally by `−35/neg_sum` so the scaled negatives total exactly −35; positives are left untouched. `raw_energy` uses the scaled deltas. CRITICAL: the Sade Sati Madhya hard-cap-at-59 check still uses the ORIGINAL `saturn_delta` (not scaled) so the cap still triggers when Saturn ≤ −20 even after dilution. Buckets also use original deltas so each bucket reflects true overlay strength. New `overlays.negative_cap` block exposes `{active, cap: −35, raw_negative, raw_positive}` for transparency. Per-overlay sub-blocks still show original deltas (so user sees true Saturn impact); only the score math uses scaled values. Math verified: −54 neg → scaled to −35.0; −62 neg → scaled to −35.01 (rounding noise); −18 neg → not active, untouched. **(FIX 3 — Seasonal sunrise/sunset for Hora & Rahukal)**: new helper `_seasonal_sun_times(month) → (sunrise, sunset)` returns winter Nov-Jan (06:30, 17:30), summer Apr-Jun (05:30, 18:45), else (06:00, 18:00) — north-Indian latitudes, no external API. `_is_rahukal` and `_current_hora_lord` now accept `sunrise`/`sunset` params (with backward-compat defaults at 6.0/18.0); `_is_rahukal` divides `(sunset − sunrise) / 8` instead of hardcoded 1.5h segments. `compute_choghadiya_hora_overlay` calls `_seasonal_sun_times(now_local.month)` and threads them through. Verified: Sunday 16:30 IST is Rahukal in winter (segment 16:08-17:30) but NOT in summer (segment 17:09-18:45) — real seasonal impact. **(FIX 4 — Volatile-day flag)**: after `active_flags` built, detection counts distinct major-negative signals from {saturn_phase (delta ≤ −10), saturn_opposition_natal_moon, mars_hit_natal_moon, chandrashtama, rikta_tithi, amavasya, tara ∈ {Vipat/Pratyari/Naidhana}, rahukal, mars in dusthana OR aspecting Moon, pd_transit dusthana, md_sandhi}. If ≥ 3 fire simultaneously, appends typed flag `{type:"volatile_day", severity:"medium", signals:[...]}` to `active_flags` AND appends Hinglish nudge "Aaj energy unstable ho sakti hai — consistency maintain karna important hai." to advice string. `overlays.volatile_day` boolean mirror exposed for clients. Verified live: Sade Sati Madhya + Mars hit Moon + Naidhana tara test correctly fired volatile=true with the appended advice. Architect review of all 4 fixes pending. Existing v3.1 5-step pipeline and overlay-key shape unchanged → mobile UI v3 needs no changes.
    - **Daily Energy Score v3.1 5-STEP RELATABLE-ACCURACY upgrade (2026-04-26)**: User-locked 5-step plan to push accuracy from ~75% to ~85-88% relatable. **Backend changes** (`energy_engine.py`, +~280 lines after v3): (1) **Step 1 — Transit-to-natal aspects** (`compute_transit_natal_aspects`): Saturn opposition natal Moon (7th-from) **−12**, Mars conjunction OR opposition natal Moon **−8**, Jupiter conjunction/trine (1/5/9-from) natal Moon OR Sun **+10**. Cap +10/−20. **Saturn-Moon CONJUNCTION deliberately skipped** here (handled exclusively by Sade Sati Madhya overlay) to prevent double-count. (2) **Step 2 — Today's Jupiter & Mars house overlay** (`compute_jupiter_mars_overlay`): transit Jupiter in kendra/trikona (1/4/5/7/9/10) from natal lagna **+6**, in dusthana (6/8/12) **+2**; transit Mars in dusthana **−6**; if Mars hits natal Moon by aspect, **extra −4** (compounds with Step 1 to total −12). (3) **Step 3 — Choghadiya / Hora time-of-day** (`compute_choghadiya_hora_overlay`): Rahukal active **−6**, Saturn/Mars hora **−3**, Jupiter/Venus hora **+3**. Sunrise hardcoded 06:00, sunset 18:00 local; Rahukal segment-per-weekday lookup (Mon=2, Tue=7, Wed=5, Thu=6, Fri=4, Sat=3, Sun=8); hora cycles Chaldean order from weekday-lord starting at sunrise. (4) **Step 4 — Special Tithi mental-only boost** (extended `compute_tithi_overlay`): Ekadashi (idx 11/26) **mental_extra +3**, Amavasya (30) **−5**, Purnima (15) **+4**. Critically, this delta flows ONLY into mental bucket (Shaanti) via `_compute_buckets`, NEVER into raw_energy — keeps Tithi's existing global ±5 unchanged while adding mind-specific nuance. (5) **Step 5 — Active PD lord transit** (`compute_pd_transit_overlay`): if active Pratyantar-dasha lord is currently transiting a dusthana from natal lagna **−8**, kendra/trikona (1/5/9/10 only — 7th excluded per spec) **+5**. Architect-flagged Sade Sati Madhya hard cap=59 (NOT 60) **preserved untouched** — applied AFTER all 5 new overlays. **Backend route** (`flask_app.py` /api/energy/today, ~50 lines): now fetches sidereal positions for all 7 grahas + Rahu/Ketu (mean-node) via swisseph, builds `today_planets` dict, computes `now_local` (IST UTC+5:30 default; noon-of-date for backdated requests), passes both new params to `calculate_energy`. **Engine signature**: `calculate_energy(...today_planets=None, now_local=None)` — fully backward-compatible (older callers passing only today_moon/today_sun/today_saturn still work; engine builds `transit_signs` dict with fallback chain). New overlay keys exposed in response: `transit_aspects`, `jupiter_mars`, `time_quality`, `pd_transit` (each with `active`/`delta`/typed flags). New active_flags emitted: `saturn_aspect_natal_moon`, `mars_aspect_natal_moon`, `jupiter_support_natal`, `jupiter_support`, `jupiter_neutral`, `mars_conflict`, `rahukal`, `hora`, `pd_transit_dusthana`, `pd_transit_kendra`, `tithi_ekadashi/amavasya/purnima`. Verified live via curl: full 5-step path triggers correctly (Saturn-opp + Mars-hit chart scored 31; Sade Sati Madhya hard cap held at ≤59 in test chart). Architect review: zero severe issues across all 6 verification points (no double-count, fallback dict correct, hard cap preserved, mental_extra isolated, transit fetch complete, backward-compat safe). Mobile UI v3 already handles new dict-typed flags — no client changes needed.
    - **Daily Energy Score v3 PRODUCTION-TRUST pass (2026-04-26)**: Final pre-launch trust-and-clarity layer on top of v2. **Backend changes** (`energy_engine.py`): (1) **Sade Sati Madhya HARD CAP** — when Saturn is transiting Janma Rashi (delta ≤ −20), final `energy = min(energy, 59)` regardless of base. Cap value is **59 (not 60)** because `_category()` boundary is `score < 60 → Moderate`; capping at exactly 60 would still surface as "Good" in UI, contradicting the "heavy phase" summary and breaking trust. Critical trust rule: if user is in peak Saturn period they ARE feeling heavy, score must reflect it, never break trust. Cap chain now: base → +overlays → compression → cap. Architect-flagged trust-blocker fixed in same pass. (2) **Mahadasha Sandhi detection** — `_detect_md_sandhi()` returns True when today is within 183 days of active MD's start or end; applies −5 delta and adds "Mahadasha sandhi (transition)" mention to score-band summary. (3) **PD lord retrograde advisory** — `_check_pd_retrograde()` reads `planets[i].retrograde` from kundli engine output; if active PD lord is vakri, appends Hinglish advisory ("results delayed, patience rakho") to advice string. (4) **3-bucket output** (`_compute_buckets`) — derives Physical (Shakti) = 0.50·dasha + 0.40·av + 1.2·saturn_delta / Mental (Shaanti) = 0.55·moon + 0.40·tara + 1.0·tithi_delta + 0.4·saturn_delta / Luck (Bhagya) = 0.45·av + 0.45·dasha + 0.6·tithi_delta + 0.7·saturn_delta. Saturn weighted heaviest on physical (body-impact), Tithi on mental (mind-impact). All clamped 0-100. (5) **Confidence level** (`_compute_confidence`) — `low` when contradictory signals (e.g. high avg + Saturn-heavy, or low avg + Purna tithi, or any neg+pos coexistence), `high` when clean alignment (all bad and low score, or all good with no Saturn), else `medium`. (6) **Active flags structured array** — `_build_active_flags()` returns typed entries `{type, severity, ...}` for saturn/chandrashtama/tithi_rikta/tithi_purna/tara/md_sandhi/pd_retrograde — UI-ready badges. (7) **Bug fix** — `compute_saturn_overlay()` returned dict was missing `delta`; downstream `_summary_and_advice()` was reading `saturn_d.get("delta", 0)` which always returned 0 → never hit the specific Madhya/Ashtam cascade branches. Now `calculate_energy()` injects delta into saturn_d before passing. (8) **Score-band summary reasons** — score-band defaults (≥85/70/55/40/<40) now cite `label_map[strong_label]`/`label_map[weak_label]` with numeric value (e.g. *"Steady neutral day — Tara Bal thoda kam hai (25/100), drama nahi magic bhi nahi"*) so summary always = condition + reason + advice per audit prompt. **Response shape additions** — top-level: `overall_score` (alias of energy_score), `confidence`, `buckets {physical,mental,luck}`, `active_flags[]`, `feedback_enabled: true`, `feedback_adjustment: 0` (backend-ready stub for future per-user calibration). `overlays` now includes `md_sandhi: {active, delta}` and `after_hard_cap`. **Mobile** (`lib/energyAPI.ts`): `EnergyResult` extended with optional new fields + `EnergyBucket` and `EnergyFlag` exported types. `aspect_strength` made optional (engine no longer returns it). TS-clean. **Live-tested 5 scenarios** — T1 Naidhana+Purna+retrograde Mercury (score 63, conf low, mental 46/luck 88, advice has retrograde note), T2 Sade Sati Madhya (score 51, conf low, mental 42/physical 60/luck 74, hard cap eligible), T3 Naidhana cascade with strong AV (signal-first wins over score-band). All Sade Sati Madhya / Ashtam Sani / Naidhana / Vipat / Rikta cascade branches functioning.
    - **Daily Energy Score TRANSIT-FIRST v2 (2026-04-25, same day refinement)**: After v1 went live, user feedback was *"daily score ke liye transit hi main player hona chahiye, static factors complication hain"*. Re-architected `energy_engine.py` with cleaner mood-first design: **(A) Aspect/Shadbala completely dropped from weighted sum** — it's pure birth-chart strength, never changes daily, was a wasted weight slot (and confusing in UI). Function still defined for potential future use but not called from `calculate_energy()`. **(B) New weights**: Moon 35% + Tara 25% + Dasha 25% + AV 15% = 100%. Net effect: **75% of score is Moon-driven** (transit + tara + AV all chandra-related), which matches Vedic principle "Moon = mind = mood". Dasha provides 25% backdrop tilt. **(C) Mood-aware `_summary_and_advice` rewrite** — signal-first cascade: chandrashtama detected → Shiv mantra advice / Sade Sati Madhya (≤−20) → Hanuman Chalisa + black sesame daan / Ashtam Sani (≤−15) → health-warning text / Sade Sati any phase → discipline+seva / Naidhana Tara → "scattered day" / Vipat Tara → "obstacles day" / Rikta Tithi → "energy drain" (extra-heavy phrasing if score also <50) → falls back to 5 score bands (≥85 "rare cosmic window", ≥70 "momentum", ≥55 "steady neutral" with optional Purna mention, ≥40 "heavy/slow patience day", else "low-energy self-care day"). Output deliberately matches felt-mood at each band so user-trust holds. Components block now has 4 entries (was 5). Overlays block + compression curve unchanged from v1.
    - **Daily Energy Score realism rewrite (2026-04-25)**: Recalibrated `artifacts/api-server/energy_engine.py` and route `flask_app.py:2042` to fix UX skew (every day was 70-85, lost user trust). **4 changes**: (1) **Score realism via lower bases + compression curve** — TARA_SCORES widened from `[60,85,35,80,45,88,30,75,95]` → `[50,78,25,72,40,82,18,70,92]` (Naidhana truly hurts at 18, Janma neutral 50). Moon transit scores compressed: kendra-trikona 85→78, neutral 68→60, dusthana 50→42, 8/12 40→32, Chandrashtama cap 25→20. New `_compress_high_end()` curve pulls scores >60 down progressively (90→84, 100→92), leaves bad days untouched. (2) **Saturn overlay** — new `compute_saturn_overlay()` detects Sade Sati / Dhaiyya from natal Moon: SS Phase 1/3 (-10), SS Phase 2 Madhya (-20), Kantaka Sani 4th (-10), Ashtam Sani 8th (-15). When active, summary prepended with `⚠️ <phase> chal raha hai`. (3) **Weights rebalanced** — moon 25%→**35%** (daily-mood driver promoted), dasha 30%→25%, av 20% same, tara 15% same, asp 10%→5% (birth-chart strength static, demoted). Sum=1.0. (4) **Tithi overlay** — new `compute_tithi_overlay()` from Sun-Moon angle: Rikta (4/9/14) -5, Purna (5/10/15) +5, neutral 0. Returns full tithi name (e.g. "Krishna Chaturdashi"). Route updated to fetch today's Sun + Saturn longitudes via swisseph alongside Moon, passes both as new optional `today_sun`/`today_saturn` params (backward-compatible). Response now includes `overlays` block with Saturn/Tithi deltas + `base_score` / `after_overlays` / `after_compression` for transparency.
    - **Putra Dosh strict-classical rewrite (2026-04-25)**: Rewrote `_putra` in `artifacts/api-server/dosh_engine.py` from 3-rule binary version (≥2 malefics in 5th = Active; 1 malefic OR Jup dusthana = Mild) to full classical model per BPHS + Phaladeepika + Saravali. **10 triggers**: T1 ≥2 malefics in 5th (+3), T2 1 malefic in 5th (+1), T3 5L in dusthana (+2), T4 5L combust/debilitated (+1), T5 Jupiter in dusthana (+2), T6 Jupiter combust/debilitated (+1), T7 Jupiter conjunct Sat/Mars/Rah/Ket (+2), T8 5L conjunct Rahu/Ketu (+2), T9 ≥2 malefics aspecting 5th via classical drishti (+2), T10 Sun in 5th (+1). **3 amplifiers** (each +1): BOTH 5L and Jupiter afflicted (double-axis), 9th house also afflicted (lineage extension), 5L specifically in 8th (worst dusthana for putra). **5 bhangas** (each -1): B1 Jupiter classically strong, B2 5L exalted or own, B3 5L in kendra/trikona, B4 natural benefic (Jup/Ven/Mer) in 5th, B5 5L conjunct Jupiter. Tier: ≥2 bhanga forced None; score ≥4 Active; 1-3 Mild; ≤0 None. Common remedies expanded to 7 items including Sanskrit Santan-Gopal shloka, plus IMPORTANT medical-disclaimer line preserved.
    - **Pitru Dosh strict-classical rewrite (2026-04-25)**: Rewrote `_pitru` in `artifacts/api-server/dosh_engine.py` from minimal 2-rule version (Sun-Rahu/Ketu conj + narrow Mild) to full classical model per BPHS + Lal Kitab + Nadi. **9 triggers**: T1 Sun-Rahu conj (+3), T2 Sun-Ketu conj (+2), T3 Sun-Sat conj in 9th (+3), T4 9L in dusthana 6/8/12 (+2), T5 Rahu/Ketu in 9th (+2), T6 Sun in dusthana (+1), T7 Saturn drishti on Sun (+2), T8 9L combust (within 8° of Sun) OR debilitated (+1), T9 ≥2 malefics (Sat/Mars/Rah/Ket) in or aspecting 9th (+2). **3 amplifiers** (each +1): trigger in 9th itself, trigger in dusthana, 5th house also afflicted (curse extends to progeny). **5 bhangas** (each -1): B1 Jupiter 5/7/9 drishti on 9th or 9L, B2 9L exalted or own sign, B3 Sun own (Leo) or exalted (Aries), B4 5L strong + Jupiter strong, B5 9L in kendra/trikona. Tier: ≥2 bhanga forced None; score ≥4 Active; 1-3 Mild; ≤0 None. New module-level helpers: `_mars_aspects_house` (4/7/8 drishti), `_is_combust` (8° orb from Sun), `_is_debilitated`, `DEBIL_SIGN` dict for all grahas.
    - **Chandra-Mangal Dosh removed (2026-04-25)**: Per user request, fully removed standalone `Chandra-Mangal Dosh` entry. Deleted `_chandra_mangal()` function in `artifacts/api-server/dosh_engine.py`, removed registry tuple `("chandra_mangal", ...)`, and deleted all 3 language demo entries (hi/hn/en) in `artifacts/cosmic-lens-mobile/app/dosh.tsx`. Reasoning: Moon-Mars conjunction is more commonly classified as a positive yoga (wealth/Lakshmi yoga) than a dosh in classical Vedic; the dual nature was confusing. **Total dosh count: 16 → 15**. Note: Daridra Yog still uses "Chandra-Mangal Shashtashtak" (Moon-Mars in 6/8 from each other) as one of its 6 triggers — that sub-rule is part of Daridra's classical poverty-detection model and was preserved.
    - **Shrapit Yog strict-classical rewrite + Saturn-aspect bug fix (2026-04-25)**: Old `_shrapit` in `artifacts/api-server/dosh_engine.py` had only 3 rules (Sat-Rahu conjunction, Sat-aspect-Rahu, Sat-Rahu close-degree <30°), missing 6 classical rules — Sat-Ketu conjunction, dusthana amplification, karmic-house amplification (1/5/9), and 3 bhanga rules. **Critical bug**: the Saturn-aspect formula `(sat_h + 2/6/9) % 12 + 1` was off-by-one (correct is `(sat_h + 1/5/8) % 12 + 1` for 3rd/7th/10th from Saturn) causing false-positive Mild for cases like Saturn H5 / Rahu H8 (which is 4th from Saturn, NOT a Saturn aspect). Same buggy formula also fixed in `_vish_yoga` (Saturn-Moon aspect check). Rewrote `_shrapit` with full classical model: Hard T1 Sat-Rahu conj (+3), Hard T2 Sat aspects Rahu (+2), Soft T3 Sat-Ketu conj (+1); house amplifiers — dusthana 6/8/12 (+1), karmic 1/5/9 (+1, ancestor curse), family 2/7 (+1); bhangas — Jupiter 5/7/9 drishti on Saturn or Rahu (-1), dignity (Saturn own Cap/Aqu OR Rahu friendly sign 2/3/6/7/11) (-1), 9th lord strong + Jupiter strong (-1). Tier: ≥2 bhanga forces None; score ≥3 Active; 1-2 Mild; ≤0 None. Removed non-classical "close-degree <30° cross-house" rule. Lifted module-level helpers `_saturn_aspects_house`, `_jupiter_aspects_house`, `_is_strong` (from local Daridra) so future doshas can reuse aspect/strength checks safely without copy-paste bugs.
    - **Daridra Yog strict-classical rewrite (2026-04-25)**: Old `_daridra` in `artifacts/api-server/dosh_engine.py` only checked planets-in-2nd-house (count malefics) and Venus-in-dusthana — it covered ~5–10% of classical Daridra rules and was not lord-based. Rewritten to full classical (BPHS / Phaladeepika / Saravali) form with 6 triggers: (1) Dhanesh (2L) in 6/8/12, (2) Labhesh (11L) in 6/8/12, (3) Dhanesh or Labhesh conjunct with Saturn/Mars/Rahu in same house, (4) Lagnesh (1L) in 6/8/12, (5) Chandra-Mangal Shashtashtak (Moon-Mars in 6/8 from each other), (6) Saturn+Moon both in 2nd house (Punarphoo-style dhana drain). Plus 4 classical bhanga (cancellation) rules: (a) Dhanesh exalted or in own sign, (b) Jupiter's 5/7/9 drishti on 2nd or 11th house, (c) Dhana Yoga — 2L & 11L conjunct in kendra/trikona OR mutual sign-exchange (parivartan), (d) Lagnesh strong + Venus strong (both in kendra/trikona/own/exalted). Tiering: Active = ≥2 triggers and 0 bhanga; Mild = 1 trigger and 0 bhanga, or 2+ triggers and 1 bhanga (partial cancel); None = 0 triggers, or any triggers cancelled by ≥2 bhanga. To support lord-based rules, lifted module-level helpers into `dosh_engine.py`: `SIGN_LORD`, `SIGN_NAMES_LOWER`, `EXALT_SIGN`, `OWN_SIGNS`, `_sign_idx(p)`, `_asc_sign(pl)`, `_house_lord(asc, h)`, `_planet_obj(pl, name)`. These constants/helpers are reusable for future lord-based dosh rewrites (Punarphoo deeper, Kemadruma extended, etc.). `_ekadhipatya` retains its local copies — minimal-invasive refactor.
    - **Guru Chandal strict-classical (2026-04-25)**: `_guru_chandal` in `artifacts/api-server/dosh_engine.py` was 3-tier (Active for Jupiter–Rahu conjunction, Mild for Jupiter–Ketu conjunction, Mild for Rahu's 5th/7th/9th aspect on Jupiter). Per user, simplified to strict binary Active/None: **(a)** Jupiter and Rahu same house (conjunction) OR **(b)** Rahu in 7th house from Jupiter (opposition — counts because of Jupiter's 7th drishti, i.e. mutual aspect). Ketu involvement removed entirely (Guru–Ketu no longer counts), and Rahu's 5th/9th aspects on Jupiter are no longer considered (per user, "Rahu/Ketu ka koi aspect nahi hota" — strict no-nodal-aspects rule). Demo data already shows None across all 3 langs, no update needed.
    - **Kaal Sarp strict-classical + 12 variants (2026-04-25)**: `_kaal_sarp` in `artifacts/api-server/dosh_engine.py` was 3-tier (Active/Mild/None) with a "Partial Kaal Sarp" Mild tier when only some planets sat in the Rahu–Ketu arc. Per user (and strict classical Jaimini/Tantric rule: full Kaal Sarp requires ALL 7 core grahas enclosed between the nodes), the Mild tier was removed → binary Active/None. When Active, the variant name (one of 12 classical: Anant, Kulik, Vasuki, Shankhpal, Padma, Mahapadma, Takshak, Karkotak, Shankhachuda, Ghatak, Vishdhar, Sheshnag — keyed by Rahu's house 1..12) is now appended to both the headline (`"Kaal Sarp Dosh (Anant) — …"`) and the `planet_note` (`"… | Variant: Anant"`). Demo data in `cosmic-lens-mobile/app/dosh.tsx` updated to match (status flipped from Mild → Active with Vishdhar variant name since planet_note shows Rahu→H11). Like Kemadruma, Kaal Sarp now uses binary scheme rather than the Active/Mild/Clear pattern.
    - **Kemadruma cancellation rule fix (2026-04-25)**: `_kemadruma` in `artifacts/api-server/dosh_engine.py` was incorrectly excluding Sun + Rahu + Ketu from the cancellation set, causing false-positive Active Kemadruma when (e.g.) Rahu sat in 2nd from Moon. Fixed to BPHS Ch.8 V.51-52 literal interpretation (modern standard per KN Rao / BV Raman / Hart de Fouw): ANY graha except Moon itself (Sun + Mer + Ven + Mar + Jup + Sat + Rahu + Ketu) in 2nd/12th from Moon OR conjunct Moon cancels Kemadruma. Also enriched `planet_note` to list which planets are in adjacent houses (instead of just "empty") for transparency. **Mild tier dropped (per user, strict-classical Bhanga)**: per BPHS Bhanga rule a single graha-presence is full cancellation, so the binary scheme is now Active (all three empty) vs None (at least one filled). This intentionally diverges from the 3-tier (Active/Mild/Clear) pattern used by other doshas in the engine.
    - **Priority 1: Dosh expansion 9 → 16 (2026-04-25, Mrityu Bhag removed per user request)**: Expanded `artifacts/api-server/dosh_engine.py` from 9 to 16 doshas with 7 new classical detection functions (Mrityu Bhag was implemented as an 8th but later removed per user request — death/longevity-adjacent framing was deemed off-brand even with the strict "sensitivity indicator, NOT death prediction" disclaimer): `_vish_yoga` (Saturn–Moon conjunction/aspect), `_chandra_mangal` (Moon–Mars conjunction with Cancer/Taurus Lakshmi-yoga positive exception), `_sakat_yoga` (Moon in 6/8/12 from Jupiter per Brihat Parashara — bidirectional, matches repo's `classical_yogas.py` convention), `_putra` (5th-house affliction + Jupiter karaka, with mandatory medical-disclaimer language for fertility), `_gandanta` (Moon at water-fire sign junctions: Pisces→Aries, Cancer→Leo, Scorpio→Sagittarius), `_punar_phoo` (classical Saturn–Moon affliction primary, Saturn–Venus 7th-house secondary marriage extension), `_ekadhipatya` (planet lording one kendra/trikona + one dusthana while sitting in dusthana — derives ascendant from any planet's longitude→sign+house, accepts both int and string sign formats), `_mrityu_bhag` (Brihat Parashara classical sensitivity degrees per planet, framed strictly as "sensitivity indicator — NOT a death prediction" per replit.md brand voice). Added `_has(pl, name)` existence helper to never conflate `longitude=0.0` (valid 0° Aries) with missing data. Fixed pre-existing `saturn_h → sat_h` NameError bug at line 301 in `_daridra`. Mobile `app/dosh.tsx` updated: `getDemoDoshList` rewritten with all 17 entries × 3 vlangs (en/hn/hi), `SummaryRing` total prop dynamic (was hardcoded 9), stats bar uses `{total}`/`{clear}` (was hardcoded `9` and `9-active-mild`), labels updated to "Complete Dosha Analysis (17 Doshas)" / "सम्पूर्ण दोष विश्लेषण" / "Sampoorna Dosh Vishleshan", subtitle interpolates `${total}`. Verified live via `/api/dosh-analysis` endpoint with both int and string sign payloads, plus 0° Aries edge case (correctly detected as Gandanta). Architect-reviewed twice — second pass addressed Sakat axis (was only 6, now 6/8/12), Punar Phoo classical pathway (was Saturn–Venus only, now Saturn–Moon primary), Ekadhipatya string-sign acceptance, and lon==0 sentinel cleanup across Gandanta/Mrityu Bhag/Chandra-Mangal.
    - **Phase 4 TRUE 100% UI localization (2026-04-25)**: Audited & extracted all remaining hardcoded strings across 13 files (about, astrovastu-pro-result, astrovastu-pro, business-vastu, delete-account, divya-prashna, face-reading, face-reading-upload, future-partner-portrait, legal, +not-found, prashna-kundli, six-month-future). Added 314 new keys to `MoreT` (~167 `lg_*` legal/about/policy/privacy long paragraphs, 76 `bv_*` business-vastu, 55 `avp_*` astrovastu-pro, 16 `avr_*` astrovastu-pro-result) bringing EN total to **1535 keys × 25 langs = 38,375 strings**. Translation pipeline (`scripts/i18n-translate/translate.mjs`, gpt-5-mini, BATCH_SIZE=80, CONCURRENCY=10, resumable per-lang JSON) auto-translated 21 non-base langs to full coverage; 20 langs at 1535/1535, `or` at 1299/1535 (236 keys fall back to en). Patched all hardcoded English JSX in target files to `t.*` references via `useT()` hook. Long-form legal/about/policy paragraphs are now machine-translated (user-accepted AI translation quality risk). Brand voice locked: business-vastu/astrovastu say "Powered by Advanced Cosmic Intelligence" — never reveals AI/LLM/GPT. TS clean, Web Bundled OK (1831 modules).

**Backend (Python Flask):**
- **Artifact**: `artifacts/api-server`
- **Core Components**: `flask_app.py` (main server, auth, routing), `kundli_engine.py` (Vedic calculations), `kp_engine.py` (Krishnamurti Paddhati), `ask_engine.py` (rule-based AI), `requirements.txt` (dependencies).
- **Architecture**:
    - **Modular Engines**: Vedic astrological calculations are broken down into numerous specialized modules (e.g., `planet_strength.py`, `ashtakavarga.py`, `transits.py`, `divisional_charts.py`, `remedies.py`, `kp_cuspal_sub_lord.py`, `jaimini.py`, `chara_dasha.py`, `dosh_deep.py`, `dasha_extras.py`, `varga_deep.py`, `ashtaka_deep.py`, `transit_deep.py`, `kp_deep.py`, `special_lagnas.py`, `sahams_extended.py`, `nadi.py`, `lal_kitab_full.py`, `compat.py`, `muhurta.py`, `panchang.py`, `numerology.py`, `astro_vastu_engine.py`, `medical_engine.py`, `modern_context_engine.py`, `financial_engine.py`, `remedies_deep_engine.py`, `astrocartography_engine.py`, `timing_engine.py`).
    - **LOCKED FACTS Protocol**: A central `locked_facts.py` aggregates deterministic data from all engines into a structured block. This block serves as the single source of truth for AI responses.
    - **AI Orchestration**: `openai_helper.py` injects `locked_facts_str` into user messages and applies strict "mirror rules" (A-W) to ensure AI quotes verbatim from the factual block, preventing hallucination.
    - **Deterministic Post-Injectors**: A critical safety layer in `ai_ask()` that automatically appends engine-generated sentences if the AI fails to cite mandatory facts for specific topics, especially timing, marriage, career, finance, and children.
    - **AI Brain Phase**: Future `ai_brain/` directory will house advanced AI components like `system_prompts`, `few_shot_examples`, `decision_trees`, `question_router`, `answer_schemas`, `verification` (fact/brand-voice/hallucination checkers), `post_injectors`, `memory`, `training_data`, `prompts`, `config`, and `orchestrator.py`.
    - **RAG (Retrieval-Augmented Generation)**: Designed with `chunker.py`, `embedder.py`, `retriever.py`, and `ingest.py` to provide the AI with classical Vedic knowledge from a `knowledge_chunks` table (PostgreSQL `pgvector`). This RAG system is explicitly bypassed for timing-related questions to prevent hallucination.
    - **Life Mastery Report**: A comprehensive 17-tier PDF generation system that combines numerology and Vedic astrology, wrapped with numerology framing. Each tier is backed by dedicated modules (e.g., `numerology/core_ext.py`, `numerology/vedic_classical.py`, `numerology/remedies.py`, `numerology/audits.py`, `numerology/relationships.py`, `numerology/wealth.py`, `numerology/career.py`, `numerology/health.py`, `numerology/family.py`, `numerology/transits.py`, `numerology/spirituality.py`, `numerology/marriage.py`, `numerology/progeny.py`, `numerology/property.py`, `numerology/foreign.py`, `numerology/longevity.py`, `numerology/moksha.py`).

## External Dependencies

- **Database**: SQLite (`users.db`) for user authentication. PostgreSQL with `pgvector` extension for knowledge base (RAG).
- **Astrology Engine**: `pyswisseph` (Swiss Ephemeris) for precise planetary calculations.
- **AI/NLP**: OpenAI API (GPT models for narration and interpretation).
- **PDF Generation**: `reportlab` (Python library for PDF creation).
- **Mobile Development**: Expo (React Native framework).
- **Authentication/Notifications**: Firebase (planned for push notifications).
- **Payment Gateway**: Cashfree (planned for subscriptions).
- **SMS Gateway**: MSG91 (planned for OTP verification).
- **Image Processing**: Pillow (Python Imaging Library) for floor plan rotation.