# Cosmic Lens
A mobile Vedic Astrology application delivering accurate astrological analyses and AI-driven interpretations to users.

## Run & Operate
- **Run**: `pnpm dev`
- **Build**: `pnpm build`
- **Typecheck**: `pnpm typecheck`
- **Codegen**: `pnpm codegen`
- **DB Push**: `pnpm db:push`
- **Required Env Vars**: `OPENAI_API_KEY`, `DATABASE_URL`, `HEALTH_LLM_MODEL`, `HEALTH_ROUTER_MODEL`, `FINANCE_STATIC_BYPASS`, `HEALTH_STATIC_BYPASS`, `LEAN_PACK_MODE`, `LAYER2_FUZZY`, `LAYER3_LLM`

## Stack
- **Frameworks**: React Native (Expo), Flask (Python)
- **Runtime Versions**: Node.js 24, TypeScript 5.9, Python 3.10+
- **ORM**: SQLAlchemy
- **Validation**: Pydantic
- **Build Tool**: pnpm

## Where things live
- `apps/mobile`: Expo React Native application.
- `artifacts/api-server`: Flask backend.
- `packages/`: Shared utilities and components.
- **DB Schema**: `artifacts/api-server/database.py`
- **API Contracts**: Implicitly defined by Flask routes in `flask_app.py`
- **Theme Files**: `apps/mobile/src/theme/`
- **User Memory Layer (UCML)**: `artifacts/api-server/user_memory.py`

## Architecture decisions
- **Monorepo Structure**: Uses pnpm workspaces for dependency management and code sharing.
- **AI Orchestration**: `openai_helper.py` manages fact injection, verbatim citation, and deterministic post-injectors.
- **User Context Memory Layer (UCML) v1**: Silent, deterministic memory module (`user_memory.py`) building per-user profiles from question history with three tiers: `user_facts` (L1 atomic), `user_behavior` (L2 aggregates), `user_personality` (L3 vector). Engine accesses DB; LLM never touches it.
- **Health Timing Engine v1.0**: 9-step health-risk timing engine at `event_timing/health/health_engine_v1.py` with medical disclaimers and age guards. Emits a `▸ TIMING ENGINE` health row to `locked_facts.py`.
- **Specific-Partner Synastry Bridge — Phase 2.5.11.6 (May 7 2026)**: New ask-flow bridge that routes specific-person partner Qs ("Mere BF se shaadi hogi", "wife loyal rahegi", "mere pati ka swabhav") to either a real synastry calculation (when partner kundli is saved) or a CTA card prompting the user to add the partner's birth details. **Detector** in `openai_helper.py`: new `_SPECIFIC_PARTNER_RE` matches "mere/meri/my <bf|gf|boyfriend|girlfriend|husband|wife|pati|patni|fiance|mangetar|partner|saathi>", "uske/iske saath shaadi/rishta", "is rishte me", "hum dono ki/ka/ke" — distinct from generic future-spouse Qs ("future husband kaisa hoga" — DOES NOT trigger). New `_hint_relation_from_q()` maps the Q's pronoun/noun to the right Profile.relation slot (Boyfriend/Girlfriend/Husband/Wife/Fiance/Partner). **DB lookup** `_lookup_partner_profile(user_id)` queries `Profile.query.filter(user_id, relation IN [Husband, Wife, Boyfriend, Girlfriend, Fiance, Fiancee, Partner, Spouse], deleted_at IS NULL).order_by(updated_at DESC)`, returns `(partner_kundli_dict, partner_name, partner_relation)` or None. **Three-branch flow** wired in `raw_passthrough_ask` (signature gained `user_id` param; both `/api/ask` and `/api/ask/stream` updated to pass it from `rp_user.id`): (a) authenticated user + partner profile found → inject `=== PARTNER CHART ({Relation}: {Name}) — for compatibility comparison ===` block (built via `_partner_chart_block` reusing `_raw_compact_chart`) AFTER user's chart_text + activate new `partner_compat_rule` in system prompt that mandates a confirm-first first sentence ("{Name} ({Relation}) ke chart ke saath compatibility check kar raha hu —"), instructs LLM to scan 5 dimensions (Emotional Moon-Moon, Attraction Venus↔Mars cross-aspect, Stability 7H lord D1+D9, Communication Mercury-Mercury, Karmic Rahu-Ketu) and pick top 2-3, format strengths→friction→outlook, MAX 1 planet from EACH chart (so 2 max), 70-110w, NO Devanagari, NEVER reveal raw partner planet positions; (b) authenticated user + partner profile MISSING → early-return structured payload `{requires_partner_profile: True, partner_cta: {label, action: "open_profile_edit", relation: <hint>}, source: "requires_partner_profile", text: "Iss sawal ka accurate jawab tabhi mil sakta hai jab main aapke <relation> ka chart bhi dekh saku..."}` — no LLM call (saves cost + latency); (c) anonymous user → falls through to existing `marriage_psychology_rule`'s nuanced "depends on X,Y,Z" framing (no DB lookup possible). Prompt template gained `{partner_compat_rule}` at end of `{chart_text}{kp_reading_rule}{marriage_reading_rule}{sensitive_depth_rule}{long_story_rule}{marriage_psychology_rule}{partner_compat_rule}`. **Mobile** (`apps/cosmic-lens-mobile`): (1) `profile-edit.tsx` RELATIONS list extended with Boyfriend/Girlfriend/Fiance/Partner (i18n falls back to English key for these new ones); accepts new `?relation=Boyfriend` query param via `useLocalSearchParams` and auto-opens the Add-profile modal with that relation slot preselected (one-shot via `_autoOpenedRef`); (2) `ask.tsx` Message type gained `partnerCta?: {label, relation}` — JSON path detects `requires_partner_profile === true && partner_cta.relation: string` and renders an inline gradient-accent button (`partnerCtaBtn` style) below the message bubble with user-plus + arrow-right icons that calls `router.push({pathname: "/profile-edit", params: {relation}})`. **No new test files** — pure regex+helper+prompt-rule layer in openai_helper.py (8/8 detector smoke pass: BF/GF/wife/husband/partner all routed correctly, generic "future husband" correctly NOT triggered). **Trade-offs**: (a) i18n strings for Boyfriend/Girlfriend/Fiance/Partner fall back to English keys (acceptable for v1, full localization can be Phase 7 follow-up); (b) using most-recently-updated partner profile when user has multiple (rare edge — most users have 1); (c) the LLM compares charts via the prompt rule rather than running the dedicated `/api/love-compatibility` synastry-scoring engine — cleaner & faster, the LLM has both raw charts + the framework, and avoids the cost/latency of an internal HTTP-style call (synastry-engine integration can be Phase 2 if quality demands it).
- **Marriage/Love Domain Deep Evolution — Phase 2.5.11.5 (May 7 2026)**: Domain-focused TEST→AUDIT→FIX→RETEST loop on 25-Q marriage/love stress battery (5 sub-domains × 5 angles: delay, love-vs-arranged, breakup, current-relationship/LDR, partner-psychology, emotional-pattern). Phase-3 audit found 7 fails / 25 (72% baseline): (1) breakup-recent ("Pichhle mahine breakup hua") got 19w blunt 1-line "nahi" — TRUST-CRITICAL failure; (2) age-anxiety ("35 ki hu shaadi possible") got 27w no reassurance; (3) family-force-conflict ("family arranged me force kar rahi") got 22w no autonomy validation; (4) specific-person ("BF se hi shaadi hogi") flat 19w "haan chance hai" no nuance; (5) LDR 2-saal got 22w generic; (6) karmic-delay got 23w + Devanagari script leak (दिखती); (7) partner-trait wife-career carried subtle gender stereotype ("ghar sambhalegi"). **Root-cause architectural fixes** in `openai_helper.py`: (a) Expanded `_SENSITIVE_STATIC_RE` with 5 marriage-emotional trigger groups — `\bbreakup\b|brek-up|rishta toot|bf/gf chhod`, `toxic relation/controlling/dominate me/emotionally abus/gaslighting`, `(3X-9X) ki hu...shaadi/single/akela` (age-marriage anxiety, bidirectional), `family.{0,30}(pressure|force|maan nahi|zabardasti)` (with 30-char filler-word gap fix after L4 first-pass missed "family arranged me force"), `arranged.{0,30}(force|pressure|nahi karni)`, `one-sided|unrequited|jise pyar...wo nahi`. (b) New `_MARRIAGE_DOMAIN_RE` + `_is_marriage_domain_q()` — broad detector for love/marriage/relationship/breakup/divorce/dating/spouse/saas/sasural/arranged/akela/single/affair/cheat/dhokha. (c) New `marriage_psychology_rule` injected to system prompt for STATIC marriage Qs (mutually exclusive with sensitive/long-story; sensitive supersedes). The rule provides 6 framing protocols: DELAY-type classifier (Saturn-mature / family-blockage / self-focus / karmic-pattern → frame as "delay≠denial"+name+next-window), BREAKUP-phase classifier (temporary-distance / unstable-on-off / karmic-lesson / real-separation; min 40w with empathy+classification+healing-window), PARTNER-trait psychology vocabulary (attachment style / emotional availability / communication style / conflict approach / values — FORBIDS gendered framing like "wife ghar sambhalegi"), SPECIFIC-PERSON nuance ("depends on X,Y,Z" not flat haan/nahi), FAMILY-CONFLICT autonomy validation, AGE-ANXIETY reassurance, FUTURE-WINDOW search when current MD/AD/PD weak. Hard rules: 45-65w normal / 65-90w emotional, MAX 1 planet, Latin Hinglish only (NO Devanagari leak). Wired into prompt template at `{kp_reading_rule}{marriage_reading_rule}{sensitive_depth_rule}{long_story_rule}{marriage_psychology_rule}`. (d) Auto-promote `static_dasha_hint=True` for marriage-domain so partner/delay/timing framing references active phase naturally. **Verification**: 6/6 of the previously-failing Qs now PASS. Examples: B1 "Pichhle mahine breakup hua" → 76w with Venus+empathy ("dard fresh hai") + on-off classification + boundaries advice; D4 "35 ki hu shaadi" → 61w with Saturn + "delay nahi denial" + "mature phase me pakka"; L2 "BF se hi shaadi hogi" → 25w nuanced "depends on clarity, consistency, family alignment" (not flat haan); L4 "family force" → 88w with Saturn + autonomy validation ("pressure me haan bolna avoid karna better"); C2 "LDR 2-saal" → 67w with "real-world test demand" + planning-actionable; P3 "wife career-focused" → 41w with "self-directed, identity-conscious" (NO gender stereotype). NO new test files added — pure regex+prompt-rule layer in openai_helper.py.
- **Engine Evolution Mode — Phase 2.5.11.4 (May 7 2026)**: TEST → FAILURE-DETECT → FIX → RETEST loop on a 20-Q stress battery (15 domains × 8 question-types) surfaced 4 architectural failures in `openai_helper.py`: (1) sensitive regex missed parent-illness ("Papa beemar"), anxiety/panic attacks, suicidal/depression, addiction/abuse, job-loss; (2) planet-citation cap (max 1 in sensitive answers) was occasionally violated (Venus + Saturn cited together); (3) long-story Qs ("X saal se Y chal raha hai") collapsed to TIER-1 brevity (~25w) which felt dismissive after user invested narration; (4) anxiety/family-illness Qs lacked mandatory professional-help safety-net referral. **Root-cause fixes**: (a) Expanded `_SENSITIVE_STATIC_RE` with parent-illness pattern allowing up to 40-char filler-word gap between subject (papa/maa/mummy/husband/wife/bhai/behen/family) and illness keyword (beemar/cancer/hospital/ICU/admit/surgery/operation/tumor/treatment/...) in BOTH directions, plus added anxiety_attack/panic_attack/suicid/depress/addiction/sharab/nasha/job-loss patterns. (b) Added new `_LONG_STORY_RE` + `_is_long_story_q(question)` detector (matches "N saal/mahine se", "pichhle N", "har mahine/hafte/din", "baar baar"; requires question ≥12 words to avoid false-positives). (c) Wired `is_long_story` into raw_passthrough_ask: auto-promotes static_dasha_hint=True (current MD/AD/PD context) and injects new `long_story_rule` into system prompt — overrides TIER-1 brevity, mandates 35-50w with structure: acknowledge-pattern → WHY-it-repeats → what-changes/when/actionable + Final line. (d) Strengthened `sensitive_depth_rule` with absolute hard-cap "AT MOST ONE planet name in entire answer" + mandatory safety-net clause for anxiety/family-illness/addiction triggers ("agar 2+ weeks se feel ho raha hai mental-health professional se baat karo" / "doctor follow-up bilkul skip mat karo" / "nasha specialist ya counsellor ki help"). **Verification**: 14/14 retest pass — R07v1-v4 (all parent-illness phrasings) now sens=True; M01/M02/M03 (long-story Qs) trigger long=True with 58-78w empathetic answers; regression Qs (B01 Manglik / B02 Career / B03 Job-kab) unchanged at 17/60/46w. Examples: "Papa bohut beemar" → 82w with Sun citation + "doctor follow-up skip mat karo"; "Husband 1 mahine se ICU" → 77w with Saturn + safety-net; "5 saal se ladki ko like karta hoon" → 58w with WHY (hesitation/overthinking) + actionable (initiative). NO new test files added — all changes are prompt + regex layer in openai_helper.py.
- **Dasha Timeline Block for LLM (Phase 2.5.11, May 7 2026)**: Rewrote `_format_dasha_block(kundli)` in `reply_cosmo/engine_locked_to_llm/locked_facts.py` to emit the **current MD/AD/PD** plus every **dasha transition over the next 5 years** (MD changes + every AD inside the visible MD segment), so the LLM has full timing context whenever it sees D1+D9. New helpers `_parse_iso(v)` (tolerates `YYYY-MM-DD`, full ISO with/without `T`, trailing `Z`, fractional seconds, datetime passthrough), `_normalize_dasha_array(kundli)` (handles BOTH known shapes — Shape A `{planet, startDate, endDate, subDashas}` from the Vimshottari export and Shape B `{lord, start, end, antardashas: [{..., pratyantar: [...]}]}` from the engine-internal/test fixtures), `_fmt_d(dt)`. Active MD/AD/PD located by `start ≤ now ≤ end`. Output format: `▸ CURRENT DASHA: <MD> Mahadasha → <AD> Antardasha → <PD> Pratyantar` + `▸ DASHA WINDOW (current PD): YYYY-MM-DD → YYYY-MM-DD` (degrades to AD window if PD missing) + `▸ DASHA TIMELINE (next 5 years — MD + AD changes):` with bulleted MD lines marked `[now]` / `[starts]` and indented AD sub-lines marked `← current`. Hard cap at 60 lines (`… (truncated — showing 5-yr horizon only)`) keeps block size bounded for the LLM context budget. Three-tier graceful fallback: full timeline when `dashas[]` array present → compact `currentDasha` form (with explicit "(unavailable — full dasha array missing)" timeline note) when only the compact form is present → `(unavailable)` when both missing. Verified on Rajalaxmi (Nov 2026 → Moon MD → Mars AD → Mercury PD; horizon shows 5 Moon-X AD transitions through 2032 since Moon MD spans 2025-2035 and no MD transition falls in the 5-yr window). No new tests added — single-function rewrite, manually smoke-tested across both shapes + empty/compact-only/full-array edge cases.
- **Baby Engine Gestation-Aware Validation (Phase 2.5.10, May 7 2026)**: 3 new pipeline steps + outcome-based gate fallback. (1) **Step 5d** `_step5d_delivery_chart_check(conception_dt, lagna_si, planets_d1)` — projects 270 days forward from conception midpoint, scores Jupiter on natal 5H/9H/1H (+1 each) and Saturn on natal 5H (-1) / 9H (-0.3) at delivery time, returns `delivery_score` ∈ [-1.5, +2.5] + blessings/stresses lists. (2) **Step 5e** `_step5e_gestation_safety_scan(conception_dt, lagna_si)` — walks 9 monthly samples across gestation, counts Mars-on-natal-8H/12H + Saturn-on-natal-5H + Rahu-on-natal-5H pulses; `risk_score = 0.5 × total_pulses`. (3) **Step 5f** `_step5f_viable_rerank(windows, lagna_si, planets_d1, top_n=8)` — re-ranks top-N windows by `viable_score = conception×0.6 + delivery×0.2 - gestation_risk×0.2`. New result key `viable_top_3` carries conception_score / delivery_score / gestation_risk / viable_score / `expected_delivery_iso` / gestation_pulses dict + delivery_blessings/stresses. New conditional llm_directives `GESTATION_RISK_FLAGGED` + `PRENATAL_CARE_EMPHASIS` (when top_v.gestation_risk ≥ 1.5) and `DELIVERY_STRESS_INDICATED` (when delivery_score ≤ -0.5). **Outcome-based gate fallback** (architect-fix): replaced earlier blunt heuristic ("relax when ≤1 cross-confirmed AND no KP") with a strictly outcome-based rule — only retry Step 5 with relaxed gate when strict gating returns ZERO windows AND KP is unavailable to corroborate. Preserves D1∩D9∩D7 unanimity whenever it produces non-empty results; never relaxes when KP is available (lets real OBSTRUCTED verdicts stand). Trace pair `STEP5 strict_gate_returned_0_windows` + `GATE_FALLBACK strict_gate_relaxed (...)` exposes the retry transparently. Tests: 5 new (60/60 green) — Step 5d/5e shape + formula contract, Step 5f attaches keys + monotonic re-rank + tail preservation, fallback iff-strict-zero implication, fallback inhibited when KP available.
- **Baby Engine Phase 2.5.0 → 2.5.9 (consolidated, May 7 2026)**: Foundational 9-step Baby (Childbirth) Timing Engine at `event_timing/baby/baby_engine_v1.py`, mirroring health v1 / marriage v2.4 / finance v1 / travel v1 (FILTER → VERIFY → ACTIVATE → TRIGGER). **Pipeline** (FINAL form after all 10 phases): STEP1 D1 lean filter (3 categories: lordship 5L+18/9L+12/11L+10, occupants of 5/9/11 +14/+10/+8, aspects to 5H +9, karakas Jupiter+14/Moon+8/Venus+7 — no 7L/2L/6/8/12-occupant/Sun/Mars/funcmalefic noise) → STEP2 D9 dignity (real exalted/own/neutral/debilitated scoring after fixing `compute_d9` arg-type bug) → STEP3 D7 Saptamsha score → STEP3b D7 picture (D7 1L/5L dignity + house + 5H occupants + 5H aspects + flags + 1 hard-denial yoga `D7-Bandhya`) → STEP3c cross-chart filter (≥2/3 confirmations across D1+D9+D7 required for promoter eligibility; auto-disables when D9+D7 both unavailable) → STEP3.5 KP CSL of 5/11 with precedence OBSTRUCTED > ANCHORED > CHILD_YES > NEUTRAL → STEP4 weighted rank (D1·30 + D9·20 + D7·25 + KP·15 + karaka·10) → STEP4c KP 2-5-11 significator filter (NL+SBL must each signify ≥1 of {2,5,11}; tri-state pass/fail/unknown; pure-negation {1,4,10}-only is hard-block; strong-cross-chart Jupiter at confirmations==3 rescues from KP fail; both gates AND-composed into final_gate_map for Step 5) → STEP5 dasha activation (AD=5, PD=6, MD=1 weights; promoter vs obstruction-bearer signed scoring; explicit `active_window`/`active_priority` PEAK/STRONG/TRIGGER/BACKGROUND marking when gate-passed promoter rules at MD/AD/PD) → STEP6 K.N. Rao classical Double Transit (Jupiter+Saturn ONLY on 5H or 5L → DOUBLE_TRANSIT_5H_5L +1.5 / SINGLE_JUPITER +0.6 / SINGLE_SATURN +0.4; exact sidereal positions via `_planet_position_at` with sign/deg/nakshatra/pada/retrograde/house) → STEP7 SAV 5H+11H aggregate (santan_band STRONG/MEDIUM/WEAK) + Jupiter's own BAV bindus on 5H+11H (`jupiter_putra_strength` STRONG ≥5 avg / MODERATE 3-4 / WEAK ≤2) → STEP9 yogas trimmed to **3 D1 hard-denial only** (Bandhya / Progeny-Dosha / Miscarriage-Yoga) + 1 D7 hard-denial (D7-Bandhya); all redundant protective/informational yogas removed because earlier-step granular detection covers them (5L+9L combos in Step 1+4, Jupiter aspects in Step 1+6, Jupiter dignity in Step 2+7, D7 strength in Step 3b+3c). **Verdict** = CHILD_PROMISED | FAVORABLE | DELAYED | OBSTRUCTED | UNKNOWN; **tier** = watchful | supportive | celebratory | consult. **Composite `child_promised`** = ≥3 of 4 confirmations (jupiter_putra_strong + KP 5-cusp CHILD_YES + SAV santan-band ≥ MEDIUM + Jupiter in top-3 ranked). **Mandatory llm_directives include `NO_GENDER_PREDICTION`** (PCPNDT Act + global ethics — engine NEVER predicts boy/girl, no gender-hint vocabulary, regression-tested across all lagnas) plus BABY_DISCLAIMER / NOT_MEDICAL_ADVICE / NO_GUARANTEED_CONCEPTION / NO_GUARANTEED_DATE / SEVERITY_TIER. Conditional CHILD_PROMISED_INDICATED, CONSULT_FERTILITY_SPECIALIST (OBSTRUCTED), MEDICAL_PRECAUTION_RECOMMENDED (Bandhya/Miscarriage), USER_TOO_YOUNG_FRAMING (age<20), ADVANCED_AGE_FERTILITY_CONTEXT (age≥45). Result keys: `verdict`, `band`, `child_promised`, `current_window`, `next_3_windows` (conception-ranked), `child_active_windows` + `next_child_window` (chronological gate-passed promoter list, capped 8), `protection_windows`, `top_child_planets`, `weighted_breakdown`, `kp_layer`, `d7_picture`, `cross_chart_filter`, `kp_significator_filter`, `transits` (positions Jupiter+Saturn only + double_transit verdict + as_of_utc), `ashtakavarga` (sav_5/sav_11/santan_band/jup_bav_5/jup_bav_11/jupiter_putra_strength), `yogas`, `risk_flags`, `factors`, `llm_directives`, `remedies`. Engine delegates remedies via `remedy.get_remedies(topic="baby")` with graceful fallback to `topic="health"` + progeny-area tags. Wired in `locked_facts.py` ▸ Baby row + baby_remedies_block + sub-blocks for SAV+Jupiter-BAV / KP 2-5-11 / next promoter window / Jupiter+Saturn transits with DOUBLE TRANSIT verdict. Thread-local cache `get_last_baby_result()`. Tests at `test_baby_engine.py` (55 pre-2.5.10 + 5 gestation = 60 cases incl. NO_GENDER_PREDICTION ethical-guard regressions across all lagnas).
- **Travel Timing Engine v1.0 (Phase 2.4, May 7 2026)**: 9-step travel/foreign-travel timing engine at `event_timing/travel/travel_engine_v1.py`, mirroring health v1 / marriage v2.4 / finance v1 architecture (FILTER → VERIFY → ACTIVATE → TRIGGER). Pipeline: STEP1 D1 travel-significator filter (12L FOREIGN + 9L long-distance + 3L short-trips + 7L away-from-home + 4L home-anchor INVERTED + occupants of 3/9/12 + planets aspecting 9H + karakas Rahu/Moon/Jupiter/Mercury/Saturn/Venus) → STEP2 D9 dignity → STEP3 **D4 Chaturthamsha** (Parashara's residence chart, with D1-dignity fallback when D4 helper missing) → STEP3.5 KP CSL of 3rd, 9th & 12th cusps → STEP4 weighted ranking (D1·30 + D9·20 + D4·25 + KP·15 + karaka·10) → STEP5 dasha activation (AD=5, PD=6, MD=1; signed travel-promoter vs home-anchor with separate risk_score for Mars/Rahu uprooting) → STEP6 transits (Jupiter on 9/12 = visa-luck/foreign-fortune, Saturn on 12H = long-stay/delayed-visa, Saturn on 4H = uprooted, Rahu on 12H = sudden foreign-shift / on 9H = sudden long journey, Mars on 3/9 = accident risk, Sade Sati on Moon = relocation pressure) → STEP7 SAV bindus on 9H + 12H (foreign band) → STEP9 yogas (Foreign-Settlement / Tirtha / Vidya-Yatra / Travel-Yoga positive; Sthanabhrama / Visa-Block / Risk-Travel negative). Verdict = `TRAVEL_PROMISED | FAVORABLE | LOW_PROBABILITY | HIGH_RISK_TRAVEL | UNKNOWN`; tier = `watchful | supportive | celebratory | consult`. Composite `foreign_promised` flag requires 3+ confirmations (foreign yoga + KP 12-cusp travel + SAV foreign-band ≥ MEDIUM + Rahu in top-3 ranked). Mandatory llm_directives: `TRAVEL_DISCLAIMER`, `NO_GUARANTEED_VISA_OUTCOME`, `NO_GUARANTEED_TRAVEL_DATE`, `NO_DESTINATION_NAMING`, `SEVERITY_TIER:<tier>`; conditional `FOREIGN_TRAVEL_INDICATED`+`CONSULT_PROFESSIONAL_FOR_LEGAL` (when foreign_promised), `ADVISE_TRAVEL_INSURANCE_AND_CAUTION` (when HIGH_RISK_TRAVEL). Engine delegates remedies via `remedy.get_remedies(topic="travel")` with graceful fallback to `topic="career"` + travel-area tags when travel-topic catalog absent. Wired in `locked_facts.py` (▸ Travel row + travel_remedies_block). Thread-local cache `get_last_travel_result()`. Tests at `test_travel_engine.py` (28 cases incl. architect-pattern regressions).
- **Finance Timing Engine v1.0 (Phase 2.3, May 7 2026)**: 9-step wealth/finance timing engine at `event_timing/finance/finance_engine_v1.py`, mirroring health v1 / marriage v2.4 architecture (FILTER → VERIFY → ACTIVATE → TRIGGER). Pipeline: STEP1 D1 wealth-significator filter (2L/5L/9L/11L wealth + 6L/8L/12L leak + occupants of 2/11/12 + planets aspecting 2H + karakas Jupiter/Venus/Mercury/Sun/Moon) → STEP2 D9 dignity → STEP3 **D2 Hora** (Parashara's wealth chart, replaces D30) → STEP3.5 KP CSL of 2nd & 11th cusps → STEP4 weighted ranking (D1·30 + D9·20 + D2·25 + KP·15 + karaka·10) → STEP5 dasha activation (AD=5, PD=6, MD=1; signed wealth-promoter vs leak-bearer) → STEP6 transits (Jupiter on 2/5/11 = gain, Saturn on 2H = squeeze / 12H = drain, Rahu on 11H = sudden gain / 6H = debt risk, Sade Sati on Moon) → STEP7 SAV bindus on 2H + 11H → STEP9 yogas (Dhana / Lakshmi / Kubera / Gaja-Kesari / Vipreet-Rajyoga positive; Daridra / Kemadruma / Papakartari-on-2H negative). Verdict = `WEALTH_PROMISED | STABLE | STRESSED | HIGH_LEAK_WINDOW | UNKNOWN`; tier = `watchful | supportive | celebratory | consult` (matches **money topic** in remedy engine). Mandatory llm_directives: `FINANCIAL_DISCLAIMER`, `NOT_INVESTMENT_ADVICE`, `NO_GUARANTEED_WEALTH`, `NO_GUARANTEED_LOSS`, `SEVERITY_TIER:<tier>`, `NO_STOCK_TICKER_NAMING`. 3-confirmation rule for `consult` tier. Engine delegates remedies via `remedy.get_remedies(topic="money")`. Wired in `locked_facts.py` (▸ Finance row + finance_remedies_block) alongside legacy `wealth_timing()`. Thread-local cache `get_last_finance_result()` for downstream consumers. Tests at `test_finance_engine.py` (24 cases).
- **Remedy Engine v1.1 (Phase 2.2, May 6 2026)**: Standalone hybrid 3-tier engine at `remedy/` (catalog + remedy_engine_v1 + conflict_check + substitutions + stack_builder). Public API `get_remedies(topic, planets, areas, severity, user_facts)` + `render_for_locked_facts(result)`. Covers **health / marriage / career / money / business** × 9 grahas × 3 tiers (PRACTICAL first → AYURVEDIC → VEDIC last). Money topic = dhan-yog/savings/debt/investing (separate from career = role/income-source). Business topic = startup/founders/cashflow/scaling (separate from career = employee track). New SYSTEM_PRACTICES added for `savings, debt, investing, income_growth, expense_control, emergency_fund, credit_score, taxes, gold_assets, founders_fit, product_market_fit, cashflow, hiring, sales_pipeline, partnerships, scaling, brand, legal_compliance`. Money/business severity tiers: `watchful / supportive / celebratory / consult`. Anti-superstition: vedic never alone, every entry carries KPI + cost + caveats. Health engine delegates via `_compute_health_remedies`. Marriage uses `top_marriage_planets`, career uses `house_lords`. All topics emit parallel `▸ <TOPIC> REMEDIES` blocks via `locked_facts.py`.
- **Marriage Engine v2.4**: Integrates multi-format DOB parsing and practical-age floor for marriage window predictions, influencing LLM directives for young users.
- **User-Facing Translation Layer (GOLDEN RULE)**: Bans all astrology jargon from user-facing output.

## Product
- **Mobile App**: Daily Rashifal, Panchang, Kundli Milan, Muhurat finder, Numerology, Remedies, Vastu tips, Dosha analysis, personalized forecasts ("Risk Radar"), Life Mastery Report.
- **AI-driven Insights**: AI-powered interpretations with anti-hallucination pipelines, RAG for classical Vedic knowledge, and structured output modes.
- **Localization**: Supports 25 languages with region detection.

## User preferences
- The user wants the AI to act as a replit coding agent.
- The user prefers a strict development methodology: `engine.py file (sahi folder mein)`, `Unit test (test_engine.py)`, `locked_facts.py wiring`, `Rule entry in system prompt`, `Deterministic post-injector`, `3x smoke test on real chart (/tmp/k.json, /tmp/req.json, /tmp/qm.json)`, `Architect review (code_review skill)`, `replit.md entry` are mandatory for every new engine.
- The user has provided a clear roadmap for project phases and expects strict adherence to it. Deviations require explicit user confirmation.
- The user expects the AI to avoid hallucinating, especially regarding timing-related questions.
- The user wants the AI to integrate classical Vedic knowledge through RAG for opinion-based questions, but strictly NOT for timing questions.
- The user has defined a brand voice: "Powered by Advanced Cosmic Intelligence" and explicitly states "AI never named; AI = language layer only; backend = facts; post-injectors = mandatory citation safety net."
- The user prefers deterministic post-injectors to ensure mandatory facts are cited even if the AI misclassifies a topic.
- The user prefers that the AI refine language for opinion questions but must cite engine facts.
- The user wants medical outputs to include a mandatory medical disclaimer banner and a 3-confirmation rule for severity tiers.
- The user wants financial outputs to include a mandatory financial disclaimer banner and a tiered risk profile.
- The user wants remedies outputs to include a mandatory disclaimer ("Remedies SUPPLEMENT, never substitute action") and provide both free and paid alternatives.
- The user wants Astrocartography outputs to include a mandatory disclaimer ("energetic affinity, not guaranteed luck").
- The user expects ethical and safety disclaimers for sensitive topics like medical, lifespan, death prediction, and destiny vs. guidance.
- The user prefers a "Numerology PDF Pro" branding for the report, with a hybrid numerology + Vedic Astrology content, framed with numerology language.

## Gotchas
- **Static Gate Parity**: Any new engine/gate added to `/api/ask` MUST simultaneously be added to `/api/ask/stream`.
- **Cache Invalidation**: Any policy change requires bumping the cache namespace (`_vX`).
- **Vedic Vocab Stripping**: The validator strips Vedic terms unless explicitly opted in or the user's question contains a "tech-request" keyword.
- **Topic-Lock Conflicts**: When multiple injection points compete, the latest injected point in the user message order wins.
- **DB-Load Enforcement**: For authenticated users, `kundli` is always loaded from the database; client-supplied `kundli` is for anonymous demo mode only.
- **LLM Temperature**: The `temperature` parameter is often rejected by `gpt-5` models.
- **Telemetry Best-Effort**: Telemetry inserts are wrapped in `try/except` and silently drop rows on contention.

## Pointers
- **Relevant Skills**: `code_review`, `telemetry`, `test_generation`
- **External Docs**:
    - [pnpm workspaces](https://pnpm.pnpm.io/workspaces)
    - [Expo React Native](https://docs.expo.dev/)
    - [Flask Documentation](https://flask.palletsprojects.com/en/latest/)
    - [Pyswisseph Library](https://pyswisseph.readthedocs.io/en/latest/)
    - [OpenAI API Documentation](https://platform.openai.com/docs/api-reference)