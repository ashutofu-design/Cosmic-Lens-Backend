# Cosmic Lens
A mobile Vedic Astrology application delivering accurate astrological analyses and AI-driven interpretations to users.

## Run & Operate
- **Run**: `pnpm dev`
- **Build**: `pnpm build`
- **Typecheck**: `pnpm typecheck`
- **Codegen**: `pnpm codegen`
- **DB Push**: `pnpm db:push`
- **Required Env Vars**: `OPENAI_API_KEY`, `DATABASE_URL`, `HEALTH_LLM_MODEL`, `HEALTH_ROUTER_MODEL`, `FINANCE_STATIC_BYPASS`, `HEALTH_STATIC_BYPASS`, `LEAN_PACK_MODE`, `LAYER2_FUZZY`, `LAYER3_LLM`

## Stack
- **Frameworks**: React Native (Expo), Flask (Python)
- **Runtime Versions**: Node.js 24, TypeScript 5.9, Python 3.10+
- **ORM**: SQLAlchemy
- **Validation**: Pydantic
- **Build Tool**: pnpm

## Where things live
- `apps/mobile`: Expo React Native application.
- `artifacts/api-server`: Flask backend.
- `packages/`: Shared utilities and components.
- **DB Schema**: `artifacts/api-server/database.py`
- **API Contracts**: Implicitly defined by Flask routes in `flask_app.py`
- **Theme Files**: `apps/mobile/src/theme/`
- **User Memory Layer (UCML)**: `artifacts/api-server/user_memory.py`
- **Engines**: `artifacts/api-server/event_timing/{health,marriage,finance,travel,baby}/`, `artifacts/api-server/remedy/`
- **LLM Orchestration**: `artifacts/api-server/openai_helper.py`, `artifacts/api-server/reply_cosmo/engine_locked_to_llm/locked_facts.py`

## Architecture decisions

### Foundational
- **Monorepo Structure**: pnpm workspaces for dependency management and code sharing.
- **AI Orchestration**: `openai_helper.py` manages fact injection, verbatim citation, and deterministic post-injectors.
- **User Context Memory Layer (UCML) v1**: Silent, deterministic memory module (`user_memory.py`) building per-user profiles from question history with three tiers: `user_facts` (L1 atomic), `user_behavior` (L2 aggregates), `user_personality` (L3 vector). Engine accesses DB; LLM never touches it.
- **User-Facing Translation Layer (GOLDEN RULE)**: Bans all astrology jargon from user-facing output.

### Engine Lineage (Phases 2.2 → 2.5.11, May 2026 — consolidated)
Five timing engines (Health v1, Marriage v2.4, Finance v1, Travel v1, Baby v1) all follow the same 9-step pipeline (FILTER → VERIFY → ACTIVATE → TRIGGER): D1 lean filter → D9 dignity → divisional chart (D7 babies / D2 finance / D4 travel; **Health: D30 layer REMOVED in 2.5.11.9 — KP CSL of 6/8 cusps + D9 dignity now carry the disease-verify load**) → KP CSL of topic cusps → weighted ranking (default D1·30 + D9·20 + Dx·25 + KP·15 + karaka·10; **Health rebalanced post-2.5.11.9 to D1·40 + D9·30 + KP·20 + karaka·10**) → dasha activation (AD=5, PD=6, MD=1; signed promoter vs blocker) → transits (Jupiter/Saturn/Rahu/Sade-Sati on topic houses) → SAV bindus on topic houses → topic-specific yogas. Each engine emits a `▸ <TOPIC>` row to `locked_facts.py` + mandatory `llm_directives` (disclaimers + `SEVERITY_TIER`) + thread-local cache (`get_last_<topic>_result()`). Every engine has a paired `test_<topic>_engine.py`. Highlights: **Baby engine** (2.5.0→2.5.10) added cross-chart filter (D1∩D9∩D7), KP 2-5-11 significator gate, K.N.Rao Double Transit, gestation-aware re-rank (delivery-chart check + monthly safety scan), outcome-based gate fallback, mandatory `NO_GENDER_PREDICTION` directive (PCPNDT compliance) — 60/60 tests green. **Remedy Engine v1.1** (2.2): hybrid 3-tier (PRACTICAL → AYURVEDIC → VEDIC last) covering health/marriage/career/money/business × 9 grahas; anti-superstition rules, every entry carries KPI + cost + caveats. **Dasha Timeline Block** (2.5.11): rewrote `_format_dasha_block` to emit current MD/AD/PD + every dasha transition over next 5 years, handles both shape-A `{planet,startDate,subDashas}` and shape-B `{lord,start,antardashas}`, hard-cap 60 lines, three-tier graceful fallback. **Marriage Engine v2.4**: multi-format DOB parsing + practical-age floor for marriage windows.

### Active Development (latest)
- **Travel Engine Moon-Floor + KP-Dasha-Gate + Past-Window Diversification — Phase 2.5.11.16 (May 7 2026)**: User ground-truth: actual foreign trip happened **Sep 2025** (Moon-Moon-Mercury 10-Sep→23-Oct), but engine had returned ABSENT/buried-rank. Root-cause split into **4 surgical bugs**: (1) Moon dropped from STEP1 D1 filter — Sagittarius lagna's Moon (in 1H, lord of Cancer = 8H) had no travel-house lordship/occupancy; entire 10-yr Moon-MD silently scored 0 in dasha activation. (2) STEP3.5 KP layer was a binary "is 12CSL travel-yes/no" gate — never propagated KP signification info into PER-DASHA scoring (Moon NL→SB→SS chain hits H9+H12; Mercury chain hits H9+H12 — engine ignored both). (3) `past_dasha_windows[:3]` truncated 30+ candidate windows down to top-3 by raw score (Sun-MD windows scoring 18-21 monopolized everything). (4) STEP6 transit checks only generic Saturn/Rahu/Jupiter — never asked "is the *current dasha lord* itself transiting a travel house right now?" — Mercury (PD lord Sep 2025) transited Virgo H10/Libra H11/Scorpio H12 across the window; signal was invisible to the engine.

  **Surgical fixes**: (a) `_KARAKA_FLOOR_SURVIVORS = {Moon, Rahu, Mercury, Jupiter}` constant + STEP1 always promotes them to `in_filter=True` with `karaka-floor` link tag (no chart can skip a natural travel karaka anymore). (b) New `_kp_dasha_signifies_travel(kp, planet)` helper — unions `pl + sl + sb_houses + ss_houses` from KP significations, intersects with `_TRAVEL_HOUSES=[3,9,12]`, returns `{hits, score, layers}`. New `_step3_6_kp_dasha_map(kp)` precomputes for all 9 vimshottari planets. STEP5 `_step5_dasha_activation` gained `kp_dasha_map` kwarg — for each MD/AD/PD lord, applies a fixed **KP boost** independent of D1 ranking: `_KP_DASHA_BOOST_MD=0.4`, `_AD=1.2`, `_PD=1.5` × hit count. Each window now carries `kp_boost` + `kp_hits` fields. (c) `past_dasha_windows[:3]` → diversified `[:12]` with **MD-diversity cap of 4** (no single MD lord may occupy more than 4 of the 12 slots — prevents 6yr Sun-MD from monopolizing past_windows and burying Moon-MD's KP-fortified entries). (d) STEP6 `_step6_transits` gained `current_dasha_lords` kwarg — orchestrator now finds the active MD/AD/PD lords from chain at `now`, looks up each lord's live sky position via swisseph, and emits `dasha_lord_X_in_travel_h` triggers when any role's lord transits 3/9/12 (weighted MD=0.4, AD=0.7, PD=1.0). Output dict gained `dasha_lord_transits` list.

  **Wiring**: `engine_arch` label updated `FILTER→VERIFY→ACTIVATE→TRIGGER` → `FILTER→VERIFY→KP-GATE→ACTIVATE→TRIGGER` (5 occurrences). New LLM directive `KP_DASHA_FORTIFIES_TRAVEL` auto-appended whenever any past/current/next window has `kp_boost > 0`. `locked_facts.py` Travel block: cap `past_t[:3]` → `past_t[:12]` (line 1848); next/past/current trace lines now annotate with `| KP-DASHA: hits=[9,12] boost=N.N` whenever boost > 0.5. **Tests**: 50/50 pass (7 new in `TestPhase2_5_11_16_KpDashaGate` — Moon karaka-floor survival, KP-dasha helper correctness, past cap=12, MD-diversity cap=4, kp_boost field presence, KP-promotes-window, STEP6 dasha_lord_transits field, engine_arch label updated).

  **Live verification on /tmp/raj.json** (Sagittarius lagna, Sun+Mercury+Rahu in 12H foreign stack): past_windows now returns 12 entries with **Sep 2025 Moon-Moon-Mercury** visible at slot 12 (sc=15.71, kpb=6.2, hits=[9,12], dt=PARTIAL_J). Pre-fix it was buried at rank 30+. Sun-MD (4 slots), Venus-MD (3 slots), Moon-MD (4 slots — including Sep-2025) now coexist in the trace. Smoke 3x: anon past Q ("Last time foreign kab gayi thi"), generic Q ("Foreign settlement"), future Q ("Next travel window") all return 200 OK with engine-cited windows including 2025-onward narration. **Trade-off**: Sep-2025 still ranks behind 4 Sun-MD windows by raw score (Sun's KP signifies all 3 travel houses, Moon only 9+12 = honest astrological signal). The MD-diversity cap is the architectural answer — it forces the LLM to see Moon-MD slate alongside Sun-MD, and the LLM can use the `KP_DASHA_FORTIFIES_TRAVEL` directive to weigh recency × KP-fortification correctly. Pattern reusable for finance/health/baby/marriage past lookups (same kp_dasha_map + MD-diversity scaffold).
- **Double-Transit Aspect-Math Bug Fix + Framework Validation Failure — Phase 2.5.11.15-c (May 7 2026)**: User ground-truth feedback: actual foreign travel happened **Sep 2025**, but engine had returned May-2020/Jun-2023/Oct-2020 as top past windows. Two findings: **(1) Bug**: `_aspects_house()` had off-by-one — formula `((planet_house + off - 1) % 12) + 1` treated 1-indexed houses as 0-indexed. Example: Jupiter in H7, off=5 returned H12 (real 5th-aspect from H7 = H11). Every house-aspect was off by one. **Fix**: `((planet_house - 1 + off - 1) % 12) + 1`. Verified: J@H7 now correctly aspects H7(occ)/H11/H1/H3 ✓. 42/42 tests still pass (existing tests didn't catch the bug because they only checked verdict labels, not specific house aspects). **(2) FRAMEWORK FAILURE**: post-fix re-evaluation shows Sep 2025 (actual travel) = **ABSENT** (J in Gemini + S in Pisces — neither aspects Leo H9 or Scorpio H12). Conversely the STRONG past windows (May-2020/Jun-2023/Oct-2020) — user confirmed NO travel happened in any of them. **Conclusion**: K.N.Rao Jupiter+Saturn double-transit rule on H9/H12 is **NOT a reliable predictor** for foreign travel for this chart. Possibilities to investigate next: (a) Travel events need Rahu transit on H12 axis (not Jupiter), (b) MD/AD/PD lord directly transiting H3/H9/H12 carries more signal than J+S, (c) dasha lord's natural-significator transit (e.g. Sep 2025 dasha lord's position vs travel houses), (d) the universal J+S rule may apply to LIFE EVENTS (marriage/baby/career) but NOT to travel which is a more transit-driven event. **Status**: universal rule in `replit.md` preferences is RETAINED for now (user's mandate), but Travel's wired version is empirically failing — needs framework re-design before rolling out to health/finance/marriage. **Action pending user direction**: which alternative timing model to test.
- **Universal Double-Transit (K.N.Rao) — Phase 2.5.11.15 (May 7 2026)**: User mandate — "Jab bhi event prediction ki baat aayega — past/present/future — Jupiter+Sani ka double transit COMPULSORY he, concerned house aur lords ke upar." Lifted baby_engine_v1 STEP 6's classical Jupiter+Saturn check into a generic engine-agnostic helper `event_timing/_shared/double_transit.py::check_double_transit(kundli, target_date, lagna_si, planets_d1, concern_houses)`. Returns `{verdict ∈ STRONG/PARTIAL_J/PARTIAL_S/ABSENT/UNAVAILABLE, score 0-100, anchors[], jupiter:{sign,touches[]}, saturn:{sign,touches[]}}`. Vedic full-strength aspects (J: 5/7/9; S: 3/7/10) checked against (a) concern-house signs AND (b) concern house-lords' natal signs — STRONG when both planets each touch ≥1 concern axis. Single source of truth `CONCERN_HOUSES = {travel:[3,9,12], health:[1,6,8,12], finance:[2,5,9,11], marriage:[7,2,8,11], baby:[5,11], career:[10,6,2,11]}` + `midpoint(start,end)` helper for window evaluation. **Travel engine wired** (first rollout): every past_window / current_window / next_3_windows entry now carries `double_transit` field computed at the window's midpoint date (NOT today — past windows evaluated against the historical sky); new directive `DOUBLE_TRANSIT_TIMING_RULE_APPLIED` always appended. **locked_facts.py travel block** annotates each trace line with `| DOUBLE-TRANSIT: {verdict} (score N; anchor1; anchor2)` AND appends a hard "RULE: STRONG=sky-confirmed; PARTIAL=opportunity-only; ABSENT=unlikely fructification" reminder so the LLM narrates correctly. **Verification**: 42/42 travel tests pass (5 new in `TestDoubleTransit` — universal directive presence, past/current/next field presence, shared-helper well-formedness). Live Rajalaxmi smoke (Sagittarius lagna, foreign 12H stack): past windows now ranked May-2020 (STRONG·100 — J→H3+H9, S→H3-lord), Jun-2023 (STRONG·70 — J→H12, S→H9-lord), Oct-2020 (PARTIAL_S·30 — Saturn-only, likely no actual travel); future Aug-2035/Jan-2036 STRONG·80, Aug-2026 PARTIAL_J·35. Engine can now distinguish "dasha-favorable" from "actually fructifying". **Pending rollout**: identical wiring in health, finance, marriage engines (baby already had the original); each just needs to import the helper, pick its CONCERN_HOUSES preset, and annotate its window outputs.
- **Travel Past-Window Architect Follow-up — Phase 2.5.11.14-b (May 7 2026)**: Architect review of 2.5.11.14 caught 2 real issues. **Fix 1 (filter logic bug)**: Past filter was `w["end"] >= now or w["start"] < window_lo` — `start < window_lo` would silently drop long MD windows that started >15y ago but ended recently (e.g. a 19-year Venus MD that started 1985 and ended 2024 would be invisible). Changed to interval-overlap `w["end"] >= now or w["end"] <= window_lo` — gates on END only, so any window ending inside the lookback range is captured regardless of when it started. **Fix 2 (LLM wiring missing)**: `locked_facts.py` Travel block was only emitting `next_3_windows` trace, never `past_windows` — so past lookback was engine-only, never reached LLM context. Added `past_t = t.get("past_windows")` block after future-trace, prefixed with hard-line "**FAVORABLE OPPORTUNITY ONLY — NOT confirmed travel; UCML must verify**" to reinforce the `PAST_WINDOW_IS_OPPORTUNITY_NOT_EVENT` directive at narration layer. **Verification**: 37/37 tests still pass; live anon Q "Last time foreign kab gayi thi mai? Past me kab favorable tha?" returned TOPIC=timing / SOURCE=raw_passthrough_timing with answer naming the actual engine windows ("late 2023 se mid-2025...especially Mar 2025 ke baad"). Past-lookback now end-to-end functional. **Deferred** (architect noted, accepted for now): naive `datetime.utcnow()` + `_parse_iso()` may drift on `±HH:MM` ISO offsets — same pre-existing issue across all 5 engines, requires monorepo-wide tz normalization phase.
- **Travel Engine Past-Window Lookback — Phase 2.5.11.14 (May 7 2026)**: User asked "agar koi puchega last time foreign kab gaye the?" — current engine was forward-only (`STEP 5` line `if w["end"] < now: continue` skipped past windows). Solution: same engine, same pipeline, **only STEP 5 time-filter flipped via new kwarg**. **Changes**: (a) `_step5_dasha_activation()` gained `direction="future"` (default) + `lookback_years=15` kwargs; when `direction="past"`, filter inverts to `now - lookback_years → now` and sort flips to `(-score, -start.timestamp())` so MOST RECENT high-score past window comes first (recency wins ties); (b) orchestrator now calls STEP 5 twice (`direction="future"` for `next_3_windows`, `direction="past"` for new `past_windows`); (c) output schema gained `past_windows: [...]` (top 3 historical favorable windows, same shape as `next_3_windows` minus `severity`); (d) new LLM directive `PAST_WINDOW_IS_OPPORTUNITY_NOT_EVENT` auto-appended whenever past_windows non-empty — guards against LLM hallucinating "aap gaye the" when engine only said "favorable tha". UCML must confirm actual events. **Tests**: 37/37 pass (4 new in `TestPastWindowLookback` — output-key presence, strict-past time check, opportunity-directive presence, direct unit-level direction-kwarg filter check). **Live smoke** on Rajalaxmi (Sagittarius lagna, Sun+Mercury+Rahu in 12H foreign stack): past_windows correctly returned 2020-May (Sun-Rahu-Rahu, score 11.89), 2023-Jun (Sun-Mercury-Rahu, 11.58), 2020-Oct (Sun-Rahu-Mercury, 11.51) — all `kind=long_or_foreign`. **Trade-off**: Engine still cannot say "you DID travel" — only "favorable opportunity existed". Real event-confirmation requires UCML L1 fact lookup or user input. Pattern reusable for finance/baby/marriage past lookups (same single-line filter flip).
- **Finance KP-as-Final-Filter Hard Gate — Phase 2.5.11.12-b (May 7 2026, architect follow-up)**: Architect review of 2.5.11.12 caught that KP was still only a *weighted contributor* in `_step4_rank`, not the "last filter" the user asked for. Fix: `_step4_rank` now builds a `kp_qualified` set (D1 survivors whose KP signification touches at least one wealth house 2/5/9/11) and uses it as the ranking pool. Safe fallback: if KP data is empty (`kp.significations` AND `kp.cusps` both missing) OR no survivor qualifies, falls back to all D1+D9 survivors so the engine never returns empty on a chart with valid wealth lords. Each ranked entry now carries `kp_qualified: bool` for downstream tracing. Stale metadata cleanup: `engine_arch` label updated `FILTER→VERIFY→ACTIVATE→TRIGGER` → `FILTER→VERIFY→KP-GATE→ACTIVATE` (5 occurrences); orchestrator docstring "9-step" → "5-step Finance Timing Engine v1 pipeline (Phase 2.5.11.12)"; orphaned `compute_d2` import removed. **Tests**: 28/28 pass (3 new — `test_kp_gate_narrows_pool_to_qualified_planets` confirms Saturn/Jupiter dropped when only Venus+Mercury signify wealth; `test_kp_empty_falls_back_to_all_survivors` + `test_kp_no_qualifier_falls_back` confirm safe-fallback paths). Live anon finance Q returned 200 OK clean Hinglish.
- **Finance Engine Trim to 5-Step — Phase 2.5.11.12 (May 7 2026)**: User decision mirroring health 2.5.11.11 cleanup — **D2 Hora (STEP 3) + transits (STEP 6) + Ashtakavarga (STEP 7) + Yogas (STEP 9) all permanently removed** from `finance_engine_v1.py`. Pipeline reduced 8-step → **5-step**: D1 filter → D9 verify → KP layer (FINAL FILTER, was STEP 3.5) → weighted rank → dasha activation. User intent: "kis planet sabse acha he score me paisa dene me" — KP CSL of 2H (income) + 11H (gains) is now the last filter that picks the wealth-giving planet, then weighted rank decides order, then STEP 5 emits upcoming AD/PD windows for that planet ("upcoming dasha check karna he bolna he"). **Changes**: (a) `_step3_d2_hora` + `_step6_transits` + `_step7_ashtakavarga` + `_detect_yogas` all renamed `_DELETED_*` (orphaned); (b) `_step3_5_kp_layer` renamed `_step3_kp_layer` (KP promoted 3.5 → 3); (c) `_step4_rank` signature lost `d2_scores` param, weights rebalanced from `D1·30 + D9·20 + D2·25 + KP·15 + karaka·10` → `D1·40 + D9·30 + KP·20 + karaka·10` (D2's 25 redistributed: +10 D1, +10 D9, +5 KP); (d) `_severity_of_window(score, transit_load=0.0)` — transit_load kept as no-op default for back-compat; (e) `_derive_verdict(top_score, wealth_band="MEDIUM", yogas=None, transit_load=0.0)` — yogas/transit/band kwargs all no-op defaults, verdict keys off score only (≥8 → HIGH_LEAK, ≥4.5 → STRESSED, <2 → WEALTH_PROMISED, else STABLE); (f) output schema dropped `transits` + `ashtakavarga` + `yogas` keys; (g) risk_flags `LOW_SAV_WEALTH_BAND` + `DARIDRA_OR_KEMADRUMA_YOGA` + `HEAVY_TRANSIT_LOAD` removed (KP_2CSL_DUSTHANA + KP_11CSL_DUSTHANA preserved); (h) protection_windows yoga-skip check removed; (i) docstring + STEP-numbering comments rewritten. **Test updates**: `_step3_5_kp_layer` import → `_step3_kp_layer`; `_detect_yogas` import dropped; TestYogas class entirely removed (4 tests); `test_derive_verdict_*` use 1-arg signature; `test_severity_bands` asserts transit_load is no-op; `test_high_leak_only_when_stress_is_actually_high` rewritten without yogas; `test_protective_yoga_rescues_from_high_leak` deleted (yogas gone). **Verification**: 25/25 tests pass; live anon finance Q ("mere finance kab improve honge") returned 200 OK clean Hinglish answer naming upcoming window (mid-2026 → early-2027 — Saturn AD inside Venus MD). **Trade-off**: Lost Parashara's dedicated D2 Hora wealth-chart + Dhana/Daridra yoga signals — KP CSL of 2/11 + D9 dignity now carry the wealth-verify load alone. Live transits already injected globally via openai_helper's `current_transits` block (zero loss for the LLM). User accepted the trade for cleaner deterministic pipeline that directly answers "konsa planet paisa dega aur kab".
- **Health Engine STEP 6/7 Permanent Removal — Phase 2.5.11.11 (May 7 2026)**: User decision — **STEP 6 (transit triggers) + STEP 7 (Ashtakavarga SAV bindus) permanently removed**. Pipeline reduced 8-step → **6-step**: D1 → D9 → KP convergence → weighted rank → dasha activation → yogas. Rationale: live-sky transits already injected globally via openai_helper's `current_transits` block (zero loss for the LLM); SAV vitality bands rarely flipped severity tiers in practice. **Changes**: (a) `_step6_transits` + `_planet_sign_at` + `_step7_ashtakavarga` helpers renamed `_DELETED_*` and orphaned (kept as commented archival code, never called); (b) orchestrator STEP 6/7 blocks deleted; STEP 8 (yogas) renumbered → STEP 6 (factors line now `STEP6 yogas=...`); (c) `_severity_of_window(score, transit_load=0.0)` — `transit_load` kept as no-op default for back-compat, severity now keys off score only; (d) `_derive_verdict(top_score, yogas)` signature reduced from 4 args → 2 (dropped `ashta_band` + `transit_load`); HIGH_RISK_WINDOW now requires `score≥8 AND has_arishta`, VULNERABLE = `score≥5`; (e) output schema dropped `transits` + `ashtakavarga` keys; (f) risk_flags `LOW_SAV_LAGNA_VITALITY` + `HEAVY_TRANSIT_LOAD` removed; (g) docstring + STEP-numbering comments updated. **Test updates**: `test_severity_bands` now asserts transit_load is no-op; `test_derive_verdict_*` use new 2-arg signature; pipeline expected-fields list dropped `transits`/`ashtakavarga`. **Verification**: 23/24 tests pass (TestFullPipeline env-only fail — `/tmp/k.json` fixture missing). Live anon health Q returned 200 OK clean answer post-restart. **Trade-off**: lost engine-internal transit boost on dasha severity scoring — if a malefic dasha + transit combo previously bumped a window from "moderate" to "serious", it now stays "moderate". User accepted this since the LLM still sees current transits via the global block and can narrate them qualitatively.
- **Health/Finance Engine Trims & KP Convergence — Phases 2.5.11.9 → 2.5.11.12-b (May 7 2026, consolidated)**: Four TRIM→AUDIT→FIX waves on the deterministic timing engines. **2.5.11.9** (Health D30 removal): Trimshamsa layer permanently dropped; pipeline 9→8 step; weights rebalanced D1·40 + D9·30 + KP·20 + karaka·10 (D30's 25 redistributed +10/+10/+5). **2.5.11.10** (Health STEP 3 v2 KP convergence): expanded simple 2-cusp verdict into 3-layer at-risk planet filter — Layer A added 12th CSL (`verdict_12 ∈ HOSPITAL_YES/NO/NEUTRAL`), Layer B per-survivor KP dusthana signification (composite NL→sub→sub-sub), Layer C active-CSL gate (CSL "fires" only when its planet runs MD/AD/PD now). Final `at_risk_planets` sorted by `convergence_score = 20(D1) + 20(D9) + 30(KP-dusthana) + 30(active-CSL)`. New flags `KP_12CSL_DUSTHANA`, `KP_CSL_ACTIVE_NOW`, `KP_CONVERGENCE_STRONG`. **2.5.11.10 Option A** (cross-wiring, no merge): STEP 4 ranked entries gained `convergence_score` + `at_risk` cross-ref via O(1) lookup from `kp_layer["at_risk_planets"]`; backward-compat preserved (kp_layer=None → 0/False). SOLID separation kept (STEP 3 = binary qualifier gate, STEP 4 = continuous ranker). **2.5.11.11** (Health STEP 6/7 removal): transit triggers + Ashtakavarga SAV permanently dropped; pipeline 8→6 step (D1→D9→KP→rank→dasha→yogas); rationale: live transits already injected globally via openai_helper's `current_transits`; `_severity_of_window`/`_derive_verdict` signatures simplified (transit_load/ashta_band kwargs no-op for back-compat). **2.5.11.12** (Finance trim): mirrored Health 11 — D2 Hora + transits + Ashtakavarga + Yogas all permanently removed; pipeline 8→5 step (D1→D9→KP→rank→dasha); weights rebalanced D1·40+D9·30+KP·20+karaka·10. **2.5.11.12-b** (Finance KP-as-final-filter hard gate): architect follow-up — `_step4_rank` now builds `kp_qualified` set (D1 survivors whose KP signifies wealth houses 2/5/9/11) as ranking pool with safe fallback (empty KP OR no qualifier → fall back to all D1+D9 survivors); each ranked entry carries `kp_qualified: bool`; engine_arch label updated `FILTER→VERIFY→KP-GATE→ACTIVATE`. **Tests across all 4**: 23/24 health, 28/28 finance pass (all `TestFullPipeline` env-only failures from `/tmp/k.json` fixture missing). All live anon Qs returned 200 OK clean Hinglish post-restart. **Trade-off accepted**: lost engine-internal D30 disease-chart + Ashtakavarga vitality + D2 Hora wealth-chart + yogas signals — KP CSL + D9 dignity now carry verify load alone; LLM still sees live transits via global block.
- **Ask-flow Sensitivity & Marriage Bridges — Phases 2.5.11.4 → 2.5.11.6 (May 7 2026, consolidated)**: Three TEST→AUDIT→FIX waves hardening `openai_helper.py` ask-flow. **2.5.11.4** (sensitivity): expanded `_SENSITIVE_STATIC_RE` with parent-illness (40-char filler-gap, both directions), anxiety/panic/suicidal/depression/addiction/job-loss patterns; new `_LONG_STORY_RE` + `long_story_rule` (35-50w acknowledge→why→action) for "N saal se" pattern Qs; `sensitive_depth_rule` hard-capped at MAX 1 planet + mandatory safety-net referral. **2.5.11.5** (marriage psychology): added 5 marriage-emotional trigger groups to sensitive regex (breakup/toxic/age-anxiety/family-force/one-sided); new `_MARRIAGE_DOMAIN_RE` + `marriage_psychology_rule` with 6 framing protocols (DELAY-type classifier, BREAKUP-phase, PARTNER-trait non-gendered, SPECIFIC-PERSON nuance, FAMILY-CONFLICT autonomy, AGE-ANXIETY reassurance) — 45-65w normal / 65-90w emotional, sensitive supersedes. **2.5.11.6** (specific-partner synastry bridge): new `_SPECIFIC_PARTNER_RE` + `_lookup_partner_profile(user_id)` DB query routes "mere BF/wife/pati" Qs to (a) real synastry with `partner_compat_rule` (5-dimension scan, 70-110w) when partner kundli saved, (b) `requires_partner_profile` CTA card (early-return, no LLM call) when missing, (c) `marriage_psychology_rule` fallback for anon. Mobile: `profile-edit.tsx` accepts `?relation=` param; `ask.tsx` renders inline CTA button. Hardening: `_AMBIGUOUS_PARTNER_RE` + `_ROMANTIC_INTENT_RE` reject biz-partner; `_sanitize_partner_label()` strips control chars; mutual-exclusion with sensitive/marriage rules. **Verification**: 14/14 + 6/6 + 23/25 (92%) stress-battery pass; 2 genuine misses (abandonment/existential-fear) flagged for next wave.

## Product
- **Mobile App**: Daily Rashifal, Panchang, Kundli Milan, Muhurat finder, Numerology, Remedies, Vastu tips, Dosha analysis, personalized forecasts ("Risk Radar"), Life Mastery Report.
- **AI-driven Insights**: AI-powered interpretations with anti-hallucination pipelines, RAG for classical Vedic knowledge, and structured output modes.
- **Localization**: Supports 25 languages with region detection.

## User preferences
- The user wants the AI to act as a replit coding agent.
- The user prefers a strict development methodology: `engine.py file (sahi folder mein)`, `Unit test (test_engine.py)`, `locked_facts.py wiring`, `Rule entry in system prompt`, `Deterministic post-injector`, `3x smoke test on real chart (/tmp/k.json, /tmp/req.json, /tmp/qm.json)`, `Architect review (code_review skill)`, `replit.md entry` are mandatory for every new engine.
- The user has provided a clear roadmap for project phases and expects strict adherence to it. Deviations require explicit user confirmation.
- The user expects the AI to avoid hallucinating, especially regarding timing-related questions.
- **UNIVERSAL TIMING RULE (Phase 2.5.11.15, May 7 2026 — MANDATORY for ALL engines)**: For ANY event-prediction question — past, present, OR future ("kab hoga / kab gaya tha / kab jaaunga / when") — Jupiter+Saturn DOUBLE TRANSIT (K.N.Rao classical rule) is COMPULSORY on the **concerned house AND concerned house lords**. Dasha alone is never sufficient. The shared helper `event_timing/_shared/double_transit.py::check_double_transit(kundli, target_date, lagna_si, planets_d1, concern_houses)` returns verdict ∈ {STRONG, PARTIAL_J, PARTIAL_S, ABSENT, UNAVAILABLE} + 0-100 score + anchor list. Concern-house presets (single source of truth): travel=[3,9,12], health=[1,6,8,12], finance=[2,5,9,11], marriage=[7,2,8,11], baby=[5,11], career=[10,6,2,11]. Every timing engine MUST annotate past_windows / current_window / next_windows with `double_transit` field AND append the `DOUBLE_TRANSIT_TIMING_RULE_APPLIED` directive. locked_facts trace MUST surface the verdict + anchors so the LLM narrates accordingly (STRONG = sky-confirmed fructification; PARTIAL = opportunity-only; ABSENT = dasha-favorable but unlikely to fructify). Travel engine wired in 2.5.11.15; health/finance/marriage rollout pending in next phases.
- The user wants the AI to integrate classical Vedic knowledge through RAG for opinion-based questions, but strictly NOT for timing questions.
- The user has defined a brand voice: "Powered by Advanced Cosmic Intelligence" and explicitly states "AI never named; AI = language layer only; backend = facts; post-injectors = mandatory citation safety net."
- The user prefers deterministic post-injectors to ensure mandatory facts are cited even if the AI misclassifies a topic.
- The user prefers that the AI refine language for opinion questions but must cite engine facts.
- The user wants medical outputs to include a mandatory medical disclaimer banner and a 3-confirmation rule for severity tiers.
- The user wants financial outputs to include a mandatory financial disclaimer banner and a tiered risk profile.
- The user wants remedies outputs to include a mandatory disclaimer ("Remedies SUPPLEMENT, never substitute action") and provide both free and paid alternatives.
- The user wants Astrocartography outputs to include a mandatory disclaimer ("energetic affinity, not guaranteed luck").
- The user expects ethical and safety disclaimers for sensitive topics like medical, lifespan, death prediction, and destiny vs. guidance.
- The user prefers a "Numerology PDF Pro" branding for the report, with a hybrid numerology + Vedic Astrology content, framed with numerology language.

## Gotchas
- **Static Gate Parity**: Any new engine/gate added to `/api/ask` MUST simultaneously be added to `/api/ask/stream`.
- **Cache Invalidation**: Any policy change requires bumping the cache namespace (`_vX`).
- **Vedic Vocab Stripping**: The validator strips Vedic terms unless explicitly opted in or the user's question contains a "tech-request" keyword.
- **Topic-Lock Conflicts**: When multiple injection points compete, the latest injected point in the user message order wins.
- **DB-Load Enforcement**: For authenticated users, `kundli` is always loaded from the database; client-supplied `kundli` is for anonymous demo mode only.
- **LLM Temperature**: The `temperature` parameter is often rejected by `gpt-5` models.
- **Telemetry Best-Effort**: Telemetry inserts are wrapped in `try/except` and silently drop rows on contention.

## Pointers
- **Relevant Skills**: `code_review`, `telemetry`, `test_generation`
- **External Docs**:
    - [pnpm workspaces](https://pnpm.pnpm.io/workspaces)
    - [Expo React Native](https://docs.expo.dev/)
    - [Flask Documentation](https://flask.palletsprojects.com/en/latest/)
    - [Pyswisseph Library](https://pyswisseph.readthedocs.io/en/latest/)
    - [OpenAI API Documentation](https://platform.openai.com/docs/api-reference)
