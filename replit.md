# Cosmic Lens
A mobile Vedic Astrology application delivering accurate astrological analyses and AI-driven interpretations to users.

## Run & Operate
- **Run**: `pnpm dev`
- **Build**: `pnpm build`
- **Typecheck**: `pnpm typecheck`
- **Codegen**: `pnpm codegen`
- **DB Push**: `pnpm db:push`
- **Required Env Vars**: `OPENAI_API_KEY`, `DATABASE_URL`, `HEALTH_LLM_MODEL`, `HEALTH_ROUTER_MODEL`, `FINANCE_STATIC_BYPASS`, `HEALTH_STATIC_BYPASS`, `LEAN_PACK_MODE`, `LAYER2_FUZZY`, `LAYER3_LLM`, `COMPAT_PREMIUM_POLISH`, `COMPAT_PREMIUM_MODEL`

## Stack
- **Frameworks**: React Native (Expo), Flask (Python)
- **Runtime Versions**: Node.js 24, TypeScript 5.9, Python 3.10+
- **ORM**: SQLAlchemy
- **Validation**: Pydantic
- **Build Tool**: pnpm

## Where things live
- `apps/mobile`: Expo React Native application.
- `artifacts/api-server`: Flask backend.
- `packages/`: Shared utilities and components.
- **DB Schema**: `artifacts/api-server/database.py`, `artifacts/api-server/models.py`
- **API Contracts**: Flask routes in `artifacts/api-server/flask_app.py`
- **Theme Files**: `apps/mobile/src/theme/`
- **User Memory Layer (UCML)**: `artifacts/api-server/user_memory.py`
- **Question History**: `artifacts/api-server/question_history.py`
- **Engines**: `artifacts/api-server/event_timing/{health,marriage,finance,travel,baby}/`, `artifacts/api-server/remedy/`, `artifacts/api-server/vedic/compat/`
- **Shared Engine Helpers**: `artifacts/api-server/event_timing/_shared/` (KP scan, double-transit)
- **LLM Orchestration**: `artifacts/api-server/openai_helper.py`, `artifacts/api-server/reply_cosmo/engine_locked_to_llm/locked_facts.py`
- **Engine Lineage & Phase History**: `docs/engines.md`

## Architecture decisions
- **Monorepo Structure**: pnpm workspaces for dependency management and code sharing.
- **2-Path Routing**: Live ask endpoint splits into `raw_passthrough_timing` (engine-locked, deterministic) and `raw_passthrough_static` (LLM-driven, jargon-free). `RAW_PASSTHROUGH_MODE=1` is the only live path; legacy paths kept for fallback.
- **AI Orchestration**: `openai_helper.py` manages fact injection, verbatim citation, and deterministic post-injectors.
- **User Context Memory Layer (UCML) v1**: Silent, deterministic memory module (`user_memory.py`) building per-user profiles from question history with three tiers: `user_facts` (L1), `user_behavior` (L2), `user_personality` (L3). Engine accesses DB; LLM never touches it.
- **User-Facing Translation Layer (GOLDEN RULE)**: Bans all astrology jargon from user-facing output.
- **Engine Pipeline (5 timing engines)**: FILTER → VERIFY → KP-GATE → ACTIVATE → TRIGGER. Health v1, Marriage v2.4, Finance v1, Travel v1, Baby v1. Companion: Remedy Engine v1.1, Dasha Timeline Block, Ask-flow Sensitivity layer. Full per-engine specifics, weights, KP cusps, and shared helpers in `docs/engines.md`.

## Latest Phase
- **Phase 2.5.11.24-fix9 — Kundli Milan Pro PDF: astrology depth + analysis hierarchy + memorable closer (May 8 2026)**: Second ChatGPT critique on top of fix8 said: visual density solved, but report still feels like "AI psychology" rather than "deeply read kundli" — users want VISIBLE Vedic reasoning (planet → meaning → effect), a clear analysis hierarchy proving the methodology, and the two psychologically-charged sections users remember most (WHY THIS BOND FORMED + THE ONE THING THAT COULD QUIETLY DAMAGE IT). Five focused additions, all renderer-only (LLM contract `kya_dikh`/`kya_matlab`/`kya_dhyan`/`grounding` UNCHANGED): **(1) New P3 — "How Your Marriage Energy Was Analysed"** (`_pro_analysis_layers_page`): 9-layer Vedic methodology checklist (7th House Dynamics ✓, 7th Lord Condition ✓, Venus & Emotional Harmony ✓, Navamsa Marriage Stability ✓, Bhakoot & Nadi Compatibility ✓, Trust & Conflict Indicators ✓, Family-Life Compatibility ✓, Physical + Emotional Chemistry ✓, KP Marriage Promise ✓) — each with a 1-line plain-language sub-explanation, plus a small "D1 vs D9 — why both matter" explainer block at the bottom. Built BEFORE the interpretive chapters so trust is established up front. Deterministic content (intentionally identical every report — proves the methodology). **(2) Per-chapter "CHART LAYER →" chip** (`_pro_chart_layer_chip` + `_CHAPTER_ASTRO_FACTORS` map): small gold-accented chip injected between the chapter score row and pull-quote naming the actual Vedic factors driving that chapter — emotional → "Moon (mind), 4th House (inner home), Gana-koota"; trust → "Jupiter (dharma), 7th Lord, Bhakoot-koota"; chemistry → "Venus (rasa), Mars (drive), Yoni-koota"; etc. Provides visible astrology grounding without raw jargon dump. **(3) "QUIET PATTERNS YOU MIGHT NOT NAME" callout on Hidden Truth (P4)** (`_pro_hidden_truth_page_with_patterns` wrapper + `_derive_invisible_patterns`): purple-bordered card appended to the Hidden Truth page carrying 1-3 deterministically derived "viral truth" lines from koot/manglik asymmetries — Bhakoot weak + Maitri strong → "click as friends, different long-term life maps"; Yoni mismatch + Gana strong → "emotional sync, physical timing differs"; Nadi 0 → "subtle hard-to-name fatigue"; manglik asymmetry → "one carries Mars-driven intensity the other doesn't"; always-on fallback → "expecting other to understand without being asked". **(4) New post-blueprint page — "Why This Bond Formed + The One Thing That Could Quietly Damage It"** (`_pro_attraction_and_challenge_page` + `_derive_attraction_line` + `_derive_core_challenge_line`): one dense page with WHY THIS BOND FORMED block (purple wash card, derived from top-2 strongest koot scores — gana=shared inner emotional rhythm, bhakoot=aligned long-term life directions, yoni=instinctive physical comfort, etc.) + THE ONE THING block (gold-accented warning card, derived from weakest koot — bhakoot weak triggers "without one honest yearly conversation about the next 5 years, you'll wake up at 35 in two parallel lives"; nadi weak triggers "ritual care, gentle routines, not blame"; gana weak triggers "stop converting each other's mood, make space for both rhythms"; etc.). Sits right before Final Verdict so closing arc reads: blueprint → why-formed/risk → verdict. **(5) Memorable closing line in Final Verdict** (extension of `_pro_final_verdict_page`): purple-bordered hero quote card with one screenshot-worthy closing truth keyed by total/max ratio — ≥0.78 → "make space for each other to be different and that single habit is rarer than every grand romantic gesture combined"; 0.58-0.78 → "slowly learn each other's emotional language"; 0.40-0.58 → "patience over pride during the hardest weeks"; <0.40 → "stop expecting agreement, start practising acknowledgement — that is the real Vedic remedy here". **Page count**: synthetic fixture exactly 19 pages (locked by tests), live LLM-polished content ~24-26 pages because rich `kya_dikh` + `kya_matlab` naturally spill the 7 chapter pages onto a second page each — that spillover is content-driven (never boilerplate). **Live verified** end-to-end via mobile-shaped POST `/api/kundli-milan/pro-pdf` for Vikram/Sanya: 26-page valid PDF (50 KB), HTTP 200. **Tests**: 49/49 PASS. Updated 4 page-count assertions 17→19. New `test_pro_pdf_carries_fix9_depth_blocks` regression locks all 5 fix9 additions into contract — asserts "HOW YOUR MARRIAGE ENERGY WAS ANALYSED" + "7th House Dynamics" + "Navamsa" (D1/D9 explainer) + ≥5 "CHART LAYER" chips + "QUIET PATTERNS YOU MIGHT NOT NAME" + "WHY THIS BOND FORMED" + "THE ONE THING THAT COULD QUIETLY DAMAGE THIS MARRIAGE" + verdict-closer band words. Pre-existing CSL-jargon assertion respected: replaced "(7th CSL)" methodology-checklist label with plain "KP Marriage Promise" so the no-raw-KP-jargon contract holds. All fix8 visual blocks (REAL-LIFE MOMENT, WHY THIS APPEARS IN YOUR CHARTS, Readiness & timing) preserved. Marriage Blueprint, Practical Married Life, Koot Decoded, Special/Damage bullets, Closing all intact.

- **Phase 2.5.11.24-fix8 — Kundli Milan Pro PDF: visual-storytelling rewrite, 25→17 pages (May 8 2026)**: User's first ChatGPT critique flagged content depth as good but VISUAL STORYTELLING weak — too many thin, formulaic chapter pages reading like an audit report. Rewrote `_pro_chapter_pages` in `milan_pdf.py`: 7 chapters now ONE dense rich page each (was 2 thin pages). Added 4 new visual helpers: `_extract_quote` (first-sentence pull-quote extractor, handles Hindi `।` separator), `_pro_quote_block` (purple-tint card with 3pt left accent rule), `_pro_real_life_moment` (gold-tint box wrapping `kya_matlab` as immersive scene), `_pro_why_in_charts` (slate-bg koot chips with strength/damage tone colors green/amber/red). New `_CHAPTER_KOOT_MAP` mapping 7 chapters → 2-3 canonical koot keys for the chips. Standalone Timing Sync page dropped — its prose folded into Final Verdict as "Readiness & timing" block (was pure boilerplate, zero engine signal). New colors local to milan_pdf.py: `_BG_QUOTE` `#F4EEFF`, `_BG_MOMENT` `#FFF8EC`, `_BG_CHARTS` `#F1F5F9`, `_LINE_QUOTE` `#A78BFA`, `_LINE_GOLD` `#E8B86A`. Net 25 → 17 pages. Live smoke verified en + hi: 17-page valid PDFs (~36KB, ~65s end-to-end). Visual screenshot of P4 Emotional Compatibility confirms purple quote card, gold REAL-LIFE MOMENT box, slate WHY-IN-CHARTS chips (`Gana 6/6` green, `Bhakut 0/7` amber, `Graha Maitri 0/5` amber). Tests: 48/48 green — `test_milan_pro_pdf.py` updated 4 page-count assertions 24→17, new `test_pro_pdf_carries_fix8_visual_blocks` regression locks the new visual labels into contract; pre-existing stale soul-v1 fallback assertion ("Honest dialogue", "mutual respect") updated to current soul-v4 vocabulary ("ek partner", "dusra", "Pyaar yahaan", "5-minute") that v2/v3/v4 explicitly enforce. LLM contract (kya_dikh/kya_matlab/kya_dhyan/grounding) UNCHANGED — all changes renderer-only. Backward-compat: `_pro_chapter_pages` keeps `num_b` parameter unused for call-site stability.

- **Phase 2.5.11.24-fix — Kundli Milan Pro PDF: end-to-end native-script polish for 5 verified Indian langs (May 8 2026)**: Live testing of Phase 2.5.11.24 (13-lang picker) surfaced 4 root causes preventing the LLM polish from producing native script in regional languages — fixed all 4 in this patch. **(1) Renderer font-fallback** (`milan_pdf.py`): added `_INDIC_RANGES` Unicode-block map (Devanagari/Bengali/Tamil/Telugu/Gujarati/Kannada/Malayalam/Gurmukhi/Oriya), `_has_indic(text)` detector, `_pick_body(text, native_style, lang)` selector, and a `body_latin` Helvetica style. The chapter renderer now picks Helvetica per-paragraph when the deterministic Hinglish fallback fires under a non-Latin lang (no more blank pages with NotoBeng trying to render Roman). Translated 3 hardcoded Hinglish sub-headings ("Aapke chart me kya dikh raha hai" → "What your chart shows here", etc.). **(2) Validator scrub** (`vedic/compat/premium_chapters.py`): added `_RAW_CHART_TOKEN_PATTERNS` regex + `_scrub_raw_chart_tokens_in_place()` — strips "P1 7L afflicted", "P2 1L weak", standalone "7L"/"1L" leaks from polished output BEFORE validation, so the `RAW_ASTRO_LEAKS` denylist no longer false-positives on incidental abbreviations. RAW_ASTRO_LEAKS check now also Latin-slice-restricted (only inspects Latin chars in non-Latin-lang prose to avoid Unicode false positives). **(3) Validator lang-gating** (`_validate_premium` now takes `lang=`): three Hinglish-only checks gated to en/hn output langs — `_rhythm_variation_ok` ("ek partner ... dusra" formula detector), `_reflection_endings_ok` ("yahaan asli", "samajh lena" tail markers), and the strict `hallucinated_pair:X/10` check. Non-Latin langs accept any X/10 in [0, 10] (LLM cites real engine signals like synastry 7.5/10, maturity 6/10 not in chapter scores), but koot pairs (denom != 10) stay STRICT — fabrication like "7/8 Nadi" when engine emitted "0/8" still rejects. **(4) Lang-script lock** (new `_lang_script_directive(lang)` helper + injection into user prompt): per-lang directive names the target script in its OWN characters ("देवनागरी", "বাংলা", "தமிழ்", "ગુજરાતી", "ଓଡ଼ିଆ" etc.) and explicitly tells the model "if you find yourself about to write a Roman/Latin sentence, STOP and re-emit it in {script}". This forced gpt-5-mini to switch input mode from the first token — without it, hi/gu output came back in Hinglish/Roman. Also added one-shot retry on empty LLM body (`raw == ""`) — gpt-5-mini intermittently returns empty body when reasoning eats all tokens; retry recovers gu/or. `_PREMIUM_VERSION` bumped p3 → p6 (4 cache-busts during the fix cycle). **Live verified end-to-end via mobile-shaped POST `/api/kundli-milan/pro-pdf`** for Vikram/Sanya across 5 representative scripts — page 4 visual check confirms native-script body prose, koot citations, partner names, and "6.8/10 BALANCED" score band rendering correctly: Bengali ✅ "এই সম্পর্কটা একটা…" (66.8 KB), Hindi/Devanagari ✅ "प्यार यहाँ ज़ोर से नहीं बोलता…" (65.4 KB), Gujarati ✅ "પ્રથમ નુખમાં આ ભૂમિકા…" (58.4 KB), Tamil ✅ "போட்டோகிராப் மாதிரியாக…" (67.8 KB), Oriya ✅ "ପ୍ରଥମ ଭାବେ, ସମ୍ପର୍କ ଏଠାରେ ଲାଉଡ଼ ନୁହେଁ…" (51.3 KB). Validator log clean — 0 rejects on the final pass. **Regression tests** (`test_premium_chapters_soul.py`): 2 cache-version assertions widened (`("p2","p3")` → `("p2","p3","p4","p5","p6")`); **42/42 tests green**. Backward compat preserved: en/hn polish path still enforces all v3+v4 soul rules (RAW_ASTRO_LEAKS denylist, advice 4-gram uniqueness, rhythm formula, reflection endings, marriage_blueprint shape) — no behavioral change for English output. Brand voice + AI-never-named + deterministic fallback safety net intact.

- **Phase 2.5.11.24 — Kundli Milan Pro PDF: 13-language picker + 8 Indian-script font registration (May 8 2026)**: User flow change: tapping "Unlock Full Analysis" on the Kundli Milan Pro paywall now opens a language-picker modal FIRST (before the existing "Confirm Details" modal); user picks one of 13 Indian languages (matches `INDIA_LANG_CODES` from `lib/i18n.ts`: en, hn, hi, bn, mr, ta, te, gu, kn, ml, pa, or, as), and the Pro PDF is then generated end-to-end in that script — LLM polish writes in the chosen language and the renderer uses the matching Noto font. **Backend** (`artifacts/api-server/milan_pdf.py`): replaced single-Devanagari registration with `_INDIC_FONT_FAMILIES` map covering 9 Noto font families (Devanagari, Bengali, Tamil, Telugu, Gujarati, Kannada, Malayalam, Gurmukhi, Oriya); fonts auto-discovered via `_find_noto_dirs()` (early-break scandir on `/nix/store/*noto-fonts-extra*`); `_LANG_TO_FONT` dict maps each ISO lang → font alias (hi/mr/ne/sa→Devanagari; bn/as→Bengali — Assamese uses Bengali script + 2 extra letters Noto covers; pa→Gurmukhi; ta/te/gu/kn/ml/or → respective). `_font_pair(lang)` lookup is the single chokepoint already used by every page in the renderer (h1/h2/h3/body/muted/ground/score/chapter/etc styles). Backwards-compat: `_DEVA_REG`/`_DEVA_BOLD` symbols preserved as aliases of the resolved Devanagari pair so any downstream import doesn't break. Oriya only ships display weights on this nix snapshot — uses `NotoSansOriya-Black.ttf` for both regular and bold (only readable variant). Helvetica fallback still kicks in for any unsupported lang (latin/zh/ar/etc) — never crashes. Verified at import: all 9 families register; `_font_pair('bn')→NotoBeng/NotoBeng-Bold`, `('ta')→NotoTaml/NotoTaml-Bold`, `('or')→NotoOrya/NotoOrya-Bold`, `('en')→Helvetica`. **Frontend** (`artifacts/cosmic-lens-mobile/app/kundli-milan.tsx`): added `langPickerVisible` + `selectedPdfLang` state (default = current UI `t.lang`); rewired `confirmAndDownloadProPdf()` to open the picker first, picker's "Continue" button then opens the existing Confirm Details modal (250ms delay so iOS doesn't swallow the second modal); `handleDownloadProPdf()` POST body now sends `lang: selectedPdfLang||t.lang||"en"` instead of hardcoded `t.lang`. New picker modal: gradient-bordered card matching brand purple/pink/amber, globe icon header, scrollable list of 13 languages with native script labels (हिन्दी, বাংলা, தமிழ், తెలుగు, ગુજરાતી, ಕನ್ನಡ, മലയാളം, ਪੰਜਾਬੀ, ଓଡ଼ିଆ, অসমীয়া) + English transliteration sub-label, radio-style selection check, Cancel + Continue actions. Pre-selects current app UI language for one-tap default. **Backend endpoint unchanged** — `/api/kundli-milan/pro-pdf` already accepted `lang` and passed it through `polish_premium_chapters(..., lang=lang)` + `render_milan_pro_pdf(..., lang=lang)` from prior phase. **Smoke verified**: render-only smoke through `render_milan_pro_pdf()` for 5 representative scripts (Bengali 48.9 KB, Tamil 46.6 KB, Telugu 46.5 KB, Odia 35.6 KB, Punjabi 46.5 KB, Gujarati 40.4 KB) — all return valid PDF bytes, no crash, no tofu fallback. Mobile typecheck `pnpm typecheck` clean (0 errors). API health 200 after restart. Saved-PDF list card (`app/my-reports.tsx`) cleaned up in same session: removed `subtitle` ("0/36" score) + `kb` size; subMeta now shows ONLY `${date} · ${time}` from `r.createdAt` (real generation timestamp via `toLocaleDateString` + `toLocaleTimeString` en-IN 12-hr).

- **Phase 2.5.11.23-soul-v4 — Kundli Milan Pro PDF Rhythm + Reflection + Chart-aware (May 8 2026)**: Four-axis rewrite driven by ChatGPT critique of v3 (8.7/10) flagging: (1) every chapter opens with the same "ek partner...dusra..." asymmetry formula — after 2-3 chapters the template signature becomes visible; (2) every `kya_dhyan` ends with a ritual/check-in/scheduled practice — feels like therapy homework; (3) too many universal truths (less chart-grounded); (4) astrology not deeply felt in user-facing language. **Code changes** (all in `artifacts/api-server/vedic/compat/premium_chapters.py`): (a) `_PREMIUM_VERSION` p2→p3 (cache-bust). (b) New `_RHYTHM_FORMULA_OPENER_RE` regex (catches `dusr[aeio]` + `doosr[aeio]` transliteration variants after `\bek\s+partner\b` anchor) + `_rhythm_variation_ok()` helper — validator rejects `rhythm_formula_uniform:N` if ≥5/7 chapters open with the formula in first 180 chars of `kya_dikh`. (c) New `_REFLECTION_END_MARKERS` (17 phrases: "samajh lena", "asli reward", "asli intimacy", "yahaan asli", "honour ho sakte", "thakaata sabse zyada", "is bond ka", "understand", "realize", etc.) + `_reflection_endings_ok()` helper — validator rejects `reflection_endings_low:N` if <2 of 7 chapters' `kya_dhyan` ends with a reflection marker in last 180 chars (architect-flagged hardening — prompt-only enforcement insufficient). (d) `SYSTEM_PROMPT_PREMIUM` v4 sections added: **RHYTHM VARIATION LAW** (rotate 4 of 5 opening shapes: METAPHOR / CONCRETE SCENE / DIRECT OBSERVATION / BITTERSWEET TRUTH / CHART-AWARE BRIDGE — examples baked in), **REFLECTION-NOT-ALWAYS-ADVICE LAW** (≥2 chapters end in reflection not action; suggested ch1+ch6 = reflection, ch3+ch5 = action, ch4+ch7 = ALLOWED_REMEDIES), **CHART-AWARE LANGUAGE LAW** (subtle "one chart..." / "this chart..." bridge permitted as astrology-felt language without raw vocab; raw vocab still banned by RAW_ASTRO_LEAKS). (e) Rewrote 12 `_CH_SOUL` `kya_dikh` openers (ch1/ch3/ch5/ch7 × 3 bands) varying opening shapes: ch1.HIGH=metaphor ("Pyaar yahaan loud nahi hai — emotional language ek background hum ki tarah"), ch1.MID=concrete scene ("Imagine 9 baje ki chai, dono sofa pe, ek bole 'main theek hoon'"), ch3.LOW=bittersweet ("Wahi Tuesday raat, wahi bartan, wahi line — fight ka topic surface pe naya lagta hai, andar same purana hurt"), ch7.HIGH=chart-aware bridge ("One chart leans toward outward, visible expansion; the other carries a quieter, inward growth"). (f) Added reflection endings to ch1.HIGH/MID/LOW + ch6.HIGH/MID/LOW `kya_dhyan` so any score-band combination yields ≥2 reflections in fallback (ch1.HIGH="Yahi is bond ka secret hai", ch1.MID="isko translate karna seekhna hi yahaan asli intimacy hai", ch6.LOW="silent fairness koi nahi dekhta; jo dikha ke acknowledge hota hai, wahi long-run me asli reward"). (g) `max_tokens` 3500→8000 for `gpt-5*` model family in `polish_premium_chapters` — gpt-5 reasoning models consume completion tokens for hidden reasoning; 3500 was too small and led to empty body / JSON parse fail / silent fallback (root-caused via live smoke). **Tests**: 11 new v4 regression tests appended to `test_premium_chapters_soul.py` (version-bump, prompt v4 marker presence, rhythm regex matches/doesn't-match exact patterns + doosra variant, validator rejects rhythm_formula_uniform:5+ and accepts ≤4, validator rejects reflection_endings_low:0/1 and accepts ≥2, fallback emits varied openers across bands, fallback emits ≥2 reflection endings across all band combos). **All 42/42 tests green** (31 prior soul-v3 + 11 new soul-v4). **Live smoke verified (gpt-5-mini, Vikram/Sanya, 51.8s)**: gpt-5-mini polish now returns valid JSON (vs. previous empty body — 8000-token fix worked); validator rejected with hallucinated_pair (LLM cited 6.0/10 not in chapter scores) → fallback fired (deterministic safety net per spec); fallback metrics: **formula=0/7 ✓, reflection=2/7 ✓, chart-bridge=1 ✓, raw leaks=0 ✓, marriage_blueprint present with all 6 fields ✓**. **Architect review**: PASS, ship-ready. Two flagged hardenings (deterministic reflection validator + doosra regex variant) implemented same-phase. Backward compat preserved: RAW_ASTRO_LEAKS denylist, advice_uniqueness 4-gram check, marriage_blueprint shape/name/leak validation, ALLOWED_REMEDIES contract in ch4/ch7 — all intact.

- **Phase 2.5.11.23-soul-v3 — Kundli Milan Pro PDF Voice + Marriage Blueprint (May 8 2026)**: Two-level rewrite driven by ChatGPT critique pointing out (a) raw chart vocab was leaking into user-facing prose ("P1 Venus enemy-sign in D9", "Jupiter debilitated", "7L in 8th house") and (b) chapter advice was repeating across chapters, killing the report's intelligence; plus a NEW deep-character section the user wanted — Marriage Blueprint. **Level 1 — Voice/polish hardening**: (1) `RAW_ASTRO_LEAKS` denylist (~67 entries) added — divisional-chart names ("d9 chart", "navamsa chart"), "Nth house" phrasings, planet+dignity raw tags ("venus debilitated", "jupiter exalted"), lord references ("lagna lord", "7th lord"), raw partner-tag prefixes ("p1 venus"), KP/dasha vocab ("vimshottari dasha"). Validator rejects any leak appearing in foreground prose OR inside the new blueprint. (2) `_advice_uniqueness_ok()` 4-gram check — extracts substantive (non-stopword, ≥3-char) tokens from each chapter's `kya_dhyan`, builds 4-grams, counts pairwise overlap; if ≥2 distinct chapter-pairs share ≥2 substantive 4-grams, response is rejected as formulaic. (3) `_PREMIUM_VERSION` bumped `p1` → `p2` to invalidate L1+L2 cache. (4) `SYSTEM_PROMPT_PREMIUM` v3 sections added: **INTERPRET-NEVER-QUOTE LAW** ("the chart is the SOURCE — never the SUBJECT") with ❌/✅ examples ("P1 Venus enemy-sign in D9 → low marital harmony" ❌ vs "Affection here doesn't arrive as a confident, easy stream — it arrives in careful gestures that need acknowledgment" ✅); **UNIQUE BEHAVIOURAL ANCHOR LAW** with per-chapter inspiration mapping (ch1=daily dialogue ritual, ch2=transparency micro-habit, ch3=cool-down rule, ch4=long-horizon anchor+remedy, ch5=intimacy rhythm, ch6=quarterly boundary review, ch7=5-year map session+remedy). **Level 2 — Marriage Blueprint section** (NEW P22 in PDF): 6 prose fields — `p1_marriage_nature`, `p2_marriage_nature`, `interaction_dynamic`, `what_p1_needs_from_p2`, `what_p2_needs_from_p1`, `blueprint_takeaway`. Backend pulls D9 lagna-lord + Venus/Jupiter dignity + `marriage_maturity_0_10` + sync.lagna_lord_relation/seven_lord_relation, but **never quotes them in prose** (interpreted into pure relational character language). Validator: each field must be 60-700 chars, both partner names must appear ≥1× across the blueprint, no raw astrology leaks. New `_safe_fallback_blueprint(d9, p1n, p2n)` deterministic helper translates lagna-lord → innate marriage nature ("Mars=leads with directness and protective energy", "Venus=leads with affection and aesthetic care", etc.), maturity score → tone qualifier, sync relations → interaction phrase ("friendly+friendly = same emotional language", "hostile = different languages, asymmetry as growth fuel"). `_safe_fallback` signature extended to accept `d9_marriage`; `polish_premium_chapters` passes it through and emits `marriage_blueprint` in polished output. New `BLUEPRINT_BACKEND_SIGNAL` block injected into the user prompt. Defensive: blueprint helper handles None / empty / malformed d9 inputs without raising. **Renderer**: new `_pro_marriage_blueprint_page` in `milan_pdf.py` (chapter eyebrow + 6 labeled prose blocks + takeaway grounding card); inserted as P22, shifted Timing Sync→P23, Final Verdict→P24, Closing→P25; docstring updated "exactly 24 pages" → "exactly 25 pages". **Tests**: 10 v3 regression tests appended to `test_premium_chapters_soul.py` — version bump check, prompt v3 markers, validator rejects each RAW_ASTRO_LEAK entry, validator rejects repeated identical advice, uniqueness accepts diverse advice, fallback emits blueprint with 6 valid string fields, blueprint references both names, helper handles missing d9 gracefully, helper differentiates Mars vs Saturn nature, validator rejects malformed/missing/short/leak-laden/name-missing blueprints. **31/31 tests green** (21 prior soul-v2 + 10 new soul-v3). **Smoke verified live (gpt-5-mini polish path)**: 3 fresh charts (Vikram/Sanya 83s/43.7KB, Aarav/Riya 55s/40.7KB, Aditya/Ishita 57.6s/43.6KB) — all 25-page PDFs, MARRIAGE BLUEPRINT eyebrow + all 6 sub-blocks visible, "How both of you interact day-to-day" heading present, "What X needs from Y" headings present for both partners, **ZERO raw astro leaks** across all 3 PDFs (no D9/navamsa/lagna lord/Vimshottari/etc). Architect review: zero critical/high blockers (theoretical edge cases noted around hidden_truth bare-word leaks don't trigger — denylist uses specific phrases like "d9 chart" not bare "d9"). Cache invalidated cleanly via `_PREMIUM_VERSION="p2"` namespace bump in fingerprint. Brand voice preserved; AI never named; deterministic fallback safety net intact for any LLM validator-reject.

- **Phase 2.5.11.23-polish-live — gpt-5-mini Polish Live via Replit Proxy (May 8 2026)**: Flipped `COMPAT_PREMIUM_POLISH=1` and routed premium polish to `gpt-5-mini` via Replit AI proxy (`AI_INTEGRATIONS_OPENAI_BASE_URL`) — no user OPENAI_API_KEY consumed, charges go to Replit credits. `OPENAI_TIMEOUT=120` set (default 30s timed out gpt-5.4 at ~50-60s). Validator hardening: `_validate_premium()` `hallucinated_pair` check now allows ±0.5 tolerance ONLY for chapter-style "X/10" pairs (LLMs naturally drift "7.5" → "7.8" when citing chapter scores — not hallucination). Koot pairs (Bhakut, Gana, Nadi, etc.) and total/max stay STRICT (still catches genuine fabrication like "7/7" when engine emitted "0/7"). Live verification: 4 fresh charts (Vikram/Sanya, Arjun/Meera, Rohan/Priya, Aditya/Ishita), 24-page PDFs in 56-77s end-to-end (engine compute ~10s + gpt-5-mini polish ~50-60s). Aditya/Ishita PDF shows full LLM signal: chart-specific ("Aditya jab support dikhata hai to wo consistent rehta hai; Ishita ka trust build up dheere hota hai"), real koot citations ("Bhakut clean (7/7) aur Gana strong (6/6)"), engine-grounded findings ("manglik mismatch — only Aditya manglik"), appropriate Vedic remedies ("Mangal Shanti puja"), Why-we-say lines cite real engine facts ("Based on nakshatra_matches 1, Nadi 0/8, and Maitri 3/5"). Validator-reject path still fires v2 fallback (deterministic safety net) — no degraded UX. 21/21 regression tests still green. Cost note: ~5400-7500 tokens per Pro PDF, L1+L2 cache wired so repeat requests for same chart return instant. Phase 2.5.11.23-soul-v2 SYSTEM_PROMPT_PREMIUM + 63 fallback templates unchanged from prior phase.

- **Phase 2.5.11.23-soul-v2 — Kundli Milan Pro PDF SOUL v2 (ChatGPT-critique-driven, May 8 2026)**: Killed the formulaic-template feel that v1 still had under the hood. Six big shifts driven by the user's ChatGPT critique: **(1) Archetype change** — `SYSTEM_PROMPT_PREMIUM` swapped from "wise family elder" to "experienced modern relationship astrologer — Vedic-rooted but contemporary in voice. Sharp, warm, emotionally honest — never preachy, never motivational." Removes risk of preachy/old-fashioned/spiritual-lecture tone. **(2) `EMOTIONAL REALISM RULES` section added** — explicitly bans perfect-balance language ("dono naturally same boat me", "both equally invested"), allows asymmetry ("ek partner zyada invest karta hai, dusra silently sambhalta hai"), allows contradiction ("Love is present here, but reassurance arrives in different emotional languages"), allows timing mismatch + silent expectations + bittersweet truths. **(3) `SIGNATURE INSIGHT RULE` added** — every chapter MUST contain ≥1 high-specificity emotional observation that feels impossible to say generically. Examples baked in: "One person waits quietly instead of asking directly", "Distance here grows more through silence than anger". **(4) `THERAPY-CLICHE BAN` added** — banned: "honest dialogue", "mutual respect", "consistent care", "open communication", "communicate openly", "build trust", "show appreciation", "active listening", "be patient with each other", "express your feelings", "make time for each other". Validator allows ≤1 cliche in passing, rejects ≥2 (density check). **(5) Per-chapter name density dropped** — v1's ≥3 names PER CHAPTER over-constrained creativity. v2 keeps global ≥3 in validator (already enforced) but drops the per-chapter quota — names land naturally. **(6) 3-layer structure preserved internally but de-segmented in prose** — JSON keys (kya_dikh/kya_matlab/kya_dhyan) unchanged for renderer compatibility, but prompt explicitly says "treat them as a natural narrative arc, NOT as labeled bullet sections — the reader must NOT feel Layer 1, Layer 2, Layer 3". New constants: `THERAPY_CLICHES` (12 phrases) + `PERFECT_BALANCE_PHRASES` (6 phrases). Validator extended with both checks; rejects "perfect_balance_phrase:*" instantly + "therapy_cliche_density:N" when N≥2. **`_CH_SOUL` library fully rewritten — 63 templates (7 ch × 3 bands × 3 blocks)** with v2 principles baked in: every template carries asymmetric "ek partner...dusra..." language, ≥1 signature insight per chapter (e.g. ch3.LOW: "fight 'jeetne' se raahat nahi aati, kyunki jeeti hui baat woh nahi thi jo actually hurt kar rahi thi"; ch6.HIGH: "invisible labor karne wala dheere thakega, aur uski thakaan dikhegi nahi jab tak woh bahut baad me bahar nahi aati"; ch7.MID: "ek baat khaane ki table pe sirf weather pe hoti hai"), zero therapy cliches, zero perfect-balance language, free-flowing prose with concrete moments ("Sunday raat ko jab dono thake hain", "5-minute joint silence", "20-minute walk bina phone"). ch4 + ch7 kya_dhyan still satisfy `ALLOWED_REMEDIES` whitelist ("Joint daily prayers", "yearly anniversary ritual"). Defensive fix: `_safe_fallback` `grounding` field now populated ("Reading anchored to {title} layer — band {band}, {p1n} & {p2n} chart cross-reference") so fallback is self-consistent against validator. **8 new v2 regression tests** appended to `test_premium_chapters_soul.py`: therapy-cliche density absence in fallback, perfect-balance language absence in fallback, validator rejects therapy-cliche density ≥2, validator rejects each PERFECT_BALANCE phrase, validator accepts 1 isolated cliche (no over-rejection), fallback carries asymmetry markers in ≥6/7 chapters, SYSTEM_PROMPT carries all v2 sections + new archetype + dropped v1 archetype, signature-insight specificity heuristic per chapter. **21/21 tests green; architect review clean (zero critical/high, one medium addressed — fallback grounding self-consistency).** POLISH=0 still in env (gpt-4o not firing), so v2 deterministic fallback is what users currently read. Brand voice preserved; AI never named; backend = facts; post-injectors = mandatory citation safety net. Phase 2.5.11.23-soul (v1) entry archived in `docs/engines.md`.

- **Phase 2.5.11.23-soul — Kundli Milan Pro PDF SOUL pass (May 8 2026)**: Killed the "audit-report" feel. `_safe_fallback()` rewritten with 3-layer prose architecture: kya_dikh (signal in plain emotional language) → kya_matlab (real-life behavioural pattern with concrete moment) → kya_dhyan (one specific anchor + grounding ritual). New `_CH_SOUL` library: 7 chapters × 3 score-bands (HIGH ≥7 / MID 4-6.9 / LOW <4) × 3 blocks = 63 name-anchored Hinglish prose templates. New `_special_bullet`/`_damage_bullet` generators replace generic "no friction detected" lines. **Polish-path validator hardened (deterministic enforcement of prompt laws)**: new `SOUL_BAN_PHRASES` denylist baked into `_validate_premium()` so gpt-4o output that regresses to audit-tone ("engine driver", "no significant friction", "natural baseline compatibility", etc) is rejected; name-density rule lifted from ≥1 to ≥3 mentions per partner; SYSTEM_PROMPT_PREMIUM mirrors the same hard-ban + 3-layer + specificity laws. `_safe_fallback()` defensively normalizes malformed `milan_facts` / `chapter_scores` (string-typed `total`, list-typed `chapters`, missing dicts, None) — never raises. `milan_pdf.py` placeholder also rewritten to soul tone. New `test_premium_chapters_soul.py` (13 regression tests): banned-phrase absence in fallback prose, 3-layer presence, name density ≥5, score citation, HIGH→special / LOW→damage routing, ≥1400 words, concrete-practice keyword in kya_dhyan, validator accepts soul payload, validator rejects every entry in SOUL_BAN_PHRASES, validator rejects low name density, fallback survives 8 malformed `chapter_scores` shapes + 8 malformed `milan_facts` shapes. **26/26 tests green; 3-lang live smoke clean: en/hn ≈2,618 words & 24pp, hi 1,358w & 24pp (Devanagari); zero banned phrases across all 3 PDFs.** Word density ~2.2× the audit-feel version. Phase 2.5.11.23 backend (5 `vedic/compat/` modules, dual-shape endpoint, mobile single-call wiring) unchanged. Full prior phase history in `docs/engines.md`.

## Product
- **Mobile App**: Daily Rashifal, Panchang, Kundli Milan, Muhurat finder, Numerology, Remedies, Vastu tips, Dosha analysis, personalized forecasts ("Risk Radar"), Life Mastery Report.
- **AI-driven Insights**: AI-powered interpretations with anti-hallucination pipelines, RAG for classical Vedic knowledge, and structured output modes.
- **Localization**: Supports 25 languages with region detection.

## User preferences
- The user wants the AI to act as a Replit coding agent.
- **Strict development methodology** for every new engine: engine.py file (correct folder), unit test (`test_engine.py`), `locked_facts.py` wiring, rule entry in system prompt, deterministic post-injector, 3x smoke test on real chart (`/tmp/k.json`, `/tmp/req.json`, `/tmp/qm.json`), architect review (`code_review` skill), and `replit.md` entry are all mandatory.
- The user has provided a clear roadmap for project phases and expects strict adherence. Deviations require explicit user confirmation.
- The user expects the AI to avoid hallucinating, especially regarding timing-related questions.
- **UNIVERSAL TIMING RULE (MANDATORY for ALL timing engines)**: Jupiter+Saturn DOUBLE TRANSIT (K.N.Rao classical rule) is COMPULSORY on the concerned house AND concerned house lords for ANY event-prediction question. Dasha alone never sufficient. Helper + concern-house presets + caveat in `docs/engines.md`.
- The user wants the AI to integrate classical Vedic knowledge through RAG for opinion-based questions, but strictly NOT for timing questions.
- Brand voice: "Powered by Advanced Cosmic Intelligence". AI is never named; AI = language layer only; backend = facts; post-injectors = mandatory citation safety net.
- The user prefers deterministic post-injectors to ensure mandatory facts are cited even if the AI misclassifies a topic.
- The user prefers that the AI refine language for opinion questions but must cite engine facts.
- Mandatory disclaimers per domain: medical (banner + 3-confirmation rule), financial (banner + tiered risk profile), remedies ("SUPPLEMENT, never substitute action" + free + paid alternatives), Astrocartography ("energetic affinity, not guaranteed luck"), and ethical/safety disclaimers for sensitive topics (lifespan, death prediction, destiny vs. guidance).
- Numerology PDF Pro branding: hybrid numerology + Vedic Astrology content, framed with numerology language.

## Gotchas
- **Static Gate Parity**: Any new engine/gate added to `/api/ask` MUST simultaneously be added to `/api/ask/stream`.
- **Cache Invalidation**: Any policy change requires bumping the cache namespace (`_vX`).
- **Vedic Vocab Stripping**: The validator strips Vedic terms unless explicitly opted in or the user's question contains a "tech-request" keyword.
- **Topic-Lock Conflicts**: When multiple injection points compete, the latest injected point in the user message order wins.
- **DB-Load Enforcement**: For authenticated users, `kundli` is always loaded from the database; client-supplied `kundli` is for anonymous demo mode only.
- **LLM Temperature**: The `temperature` parameter is often rejected by `gpt-5` models.
- **Telemetry Best-Effort**: Telemetry inserts are wrapped in `try/except` and silently drop rows on contention.
- **Anon Q&A**: `UserQuestion.user_id` is NOT NULL — anonymous Asks are intentionally NOT persisted.
- **DB Migrations**: `db.create_all()` does NOT add columns to existing tables. Use `ALTER TABLE` directly.
- **Naive datetime drift**: `datetime.utcnow()` + `_parse_iso()` may drift on `±HH:MM` ISO offsets across all 5 engines (deferred — needs monorepo-wide tz normalization).

## Pointers
- **Engine reference**: `docs/engines.md` (lineage, per-engine specifics, shared helpers, UNIVERSAL TIMING RULE detail, full phase history)
- **Relevant Skills**: `code_review`, `telemetry`, `test_generation`
- **External Docs**: [pnpm workspaces](https://pnpm.pnpm.io/workspaces) · [Expo](https://docs.expo.dev/) · [Flask](https://flask.palletsprojects.com/en/latest/) · [Pyswisseph](https://pyswisseph.readthedocs.io/en/latest/) · [OpenAI API](https://platform.openai.com/docs/api-reference)
