# Cosmic Lens — Workspace

## Overview

Cosmic Lens is a pnpm workspace monorepo using TypeScript for a mobile Vedic Astrology application. It aims to provide accurate astrological analysis, including kundli calculations, numerology, Vastu, and AI-driven interpretations. The project's vision is to achieve over 97% accuracy through a robust calculation engine, a sophisticated AI, and continuous learning, ultimately creating a comprehensive astrological and numerological life mastery system.

## User Preferences

- The user wants the AI to act as a replit coding agent.
- The user prefers a strict development methodology: `engine.py file (sahi folder mein)`, `Unit test (test_engine.py)`, `locked_facts.py wiring`, `Rule entry in system prompt`, `Deterministic post-injector`, `3x smoke test on real chart (/tmp/k.json, /tmp/req.json, /tmp/qm.json)`, `Architect review (code_review skill)`, `replit.md entry` are mandatory for every new engine.
- The user has provided a clear roadmap for project phases and expects strict adherence to it. Deviations require explicit user confirmation.
- The user expects the AI to avoid hallucinating, especially regarding timing-related questions.
- The user wants the AI to integrate classical Vedic knowledge through RAG for opinion-based questions, but strictly NOT for timing questions.
- The user has defined a brand voice: "Powered by Advanced Cosmic Intelligence" and explicitly states "AI never named; AI = language layer only; backend = facts; post-injectors = mandatory citation safety net."
- The user prefers deterministic post-injectors to ensure mandatory facts are cited even if the AI misclassifies a topic.
- The user prefers that the AI refine language for opinion questions but must cite engine facts.
- The user wants medical outputs to include a mandatory medical disclaimer banner and a 3-confirmation rule for severity tiers.
- The user wants financial outputs to include a mandatory financial disclaimer banner and a tiered risk profile.
- The user wants remedies outputs to include a mandatory disclaimer ("Remedies SUPPLEMENT, never substitute action") and provide both free and paid alternatives.
- The user wants Astrocartography outputs to include a mandatory disclaimer ("energetic affinity, not guaranteed luck").
- The user expects ethical and safety disclaimers for sensitive topics like medical, lifespan, death prediction, and destiny vs. guidance.
- The user prefers a "Numerology PDF Pro" branding for the report, with a hybrid numerology + Vedic Astrology content, framed with numerology language.

## System Architecture

The project is a pnpm workspace monorepo using Node.js 24 and TypeScript 5.9.

**Mobile App (Expo React Native):**
- **UI/UX**: Full-screen nebula background, card glow effects, glassmorphism, zodiac-based accent theming, and dark/light mode with bottom tab navigation.
- **Features**: Daily Rashifal, Panchang, Kundli Milan, Muhurat finder, Numerology, Remedies, Vastu tips, Dosha analysis, personalized forecasts ("Risk Radar"), and a Life Mastery Report.
- **Localization**: Supports 25 languages with region detection and a two-layer architecture for UI and Vedic content.

**Backend (Python Flask):**
- **Architecture**: Modular engines for Vedic astrological calculations (e.g., `planet_strength.py`, `ashtakavarga.py`).
- **CLE (Cosmic Lens Engine) Format**: Every new domain engine must include D9 Navamsa cross-check, a domain-specific D-chart, KP cuspal sub-lord, Jaimini karaka mapping, a 3-bucket tense detector, and brand-safety guards.
- **LOCKED FACTS Protocol**: `locked_facts.py` centralizes deterministic data for AI responses.
- **AI Orchestration**: `openai_helper.py` injects facts, enforces verbatim citation, and includes deterministic post-injectors for safety.
- **RAG (Retrieval-Augmented Generation)**: Uses `pgvector` for classical Vedic knowledge, bypassed for timing questions.
- **Engine-Aware Anti-Hallucination Pipeline**: Ensures timing answers are correctly sourced from engines, with a smart timing-validator that softens when engine data is unavailable.
- **Supertype Narrator Contract**: Classifies answers into 5 supertypes (PLANET_QUERY, PROBLEM_QUERY, TIMING_QUERY, DECISION_QUERY, GENERAL_ANALYSIS) with strict per-type response rules.
- **Question-Intent Classifier**: A deterministic function (`_classify_ask_intent`) identifies 13 intent categories, providing a canonical source of truth for routing.
- **AI-ONLY Question Understanding**: Replaced multi-layer understanding with a single LLM classifier call for intent, topic, and confidence.
- **Emotional Treatment Playbook**: A cross-engine module defining a `(emotional_tone × domain) → directive` matrix for nuanced AI responses.
- **Structured-Output Mode**: For specific engines (e.g., wealth), forces strict JSON output with defined schemas, disclaimers, and an "EMPATHY SANDWICH" narrative pattern.
- **Health Engine**: Deterministic Vedic + KP health/illness engine with 25 layers, 12 question buckets, and critical brand-safety guards including medical disclaimers and helpline information.
- **Wealth / Finance Engine**: Deterministic Vedic + KP wealth/finance engine with 25 layers, 12 question buckets, and critical brand-safety guards including financial disclaimers and advisor recommendations.
- **Stock Market Engine**: Deterministic Vedic-rule-based stock/trading/investment guidance with 17 core natal layers and a 10-category question classifier, enforcing anti-speculation policies.
- **Love vs. Arrange Engine**: Pure-Python deterministic scorer using classical Vedic rules for love vs. arrange marriage verdicts, with confidence-ratio-based public verdicts.
- **Yoga Registry**: Wires existing yoga detectors (Dhana, Raj, Vipareeta, etc.) into the minimal-prompt path, providing structured yoga facts.
- **Dosh Engine**: Wires `dosh_engine.analyze_doshas()` into the minimal-prompt path, presenting structured `DOSH_FACTS`.
- **Health Pipeline Orchestration**: Uses `health_topic_matcher.py`, `health_recipe_composer.py`, and `health_rules.py` to derive structured `phase76_findings` for health questions based on 132 topics and 34 rules.
- **LLM Full Chart Mode** (`LLM_FULL_CHART_MODE`, default ON since 30 Apr 2026): Env-gated passthrough that hands the LLM a comprehensive Hinglish chart-context block built by `kundli_full_context.build_full_chart_context`. Sections: 1.Birth/Lagna, 2.Grahas (with dignities), 3.Bhavas, 4.Current Dasha, 5.Upcoming Vimshottari (remaining antardashas + next 5 mahadashas), 6.Navamsha D9 (with vargottama detection), 7.Yogas/Doshas/Sade-Sati/Gochar (sourced from `chart_intelligence.analyze_chart`), 8.Niyam (anti-hallucination + Hinglish rails). The passthrough call lives in `openai_helper.ai_ask` and uses Replit AI Integrations' OpenAI proxy (`AI_INTEGRATIONS_OPENAI_*` env vars) with model from `OPENAI_MODEL` (default `gpt-5.4`). For gpt-5 / o-series models the `temperature` parameter is conditionally dropped (those models reject it).
- **Phase 2.5 Rule 18 — Deep-Chart Framework** (01 May 2026): User-driven follow-up to Phase 2.4 — ek live demo me father-relationship question ka audit kiya: 7-layer classical BPHS framework (Karaka / Bhava / Bhavesh / Karaka-se-Bhava / D9 / Dosh / Timing) me se Cosmo sirf 4/7 layers strongly cover kar raha tha. Missing: (D) Karaka-as-mini-lagna (Bhavat Bhavam), (E) D9 Navamsha confirmation, (F) Explicit Dosh check (Pitr / Grahan / Mangal / Kala-Sarpa). Fix: added a single new **Rule 18 DEEP-CHART FRAMEWORK** to `_PT_SYS_INTRO` (`openai_helper.py` ~L306, after Rule 17). The rule prescribes the 7-layer scholarly checklist explicitly (Layer A KARAKA, Layer B BHAVA, Layer C BHAVESH, Layer D KARAKA-SE-BHAVA / Bhavat Bhavam, Layer E D9 NAVAMSHA, Layer F DOSH check with named yogas, Layer G TIMING) but with two crucial constraints: (i) bullets stay capped at 3-4 per Rule 5 — layers are **blended** into bullets, not numbered as headers visible to the devotee (internal scaffolding only); (ii) layers that can't be confidently derived from the kundli (e.g. D9 missing) are silently skipped — Rule 11 (no hallucination) remains supreme; (iii) single-fact answers per Rule 1 are exempt — they stay 1-line. Header comment updated (17 → 18 rules) with Phase 2.5 line. System prompt grew ~13.8 KB → ~16.7 KB (still well within model limits). Live demo (same father question used for the audit) re-ran post-Rule-18 — quality jump confirmed: response now explicitly includes "Surya ko mini-lagna maan kar dekhen to Surya se 9th Makar padta hai" (Layer D), "D9 mein Surya 8th mein Kanya gaya hai" (Layer E), and "Surya-Shani conjunction nahi hai, isliye classical Pitr Dosh ka seedha yog nahi banta" (Layer F — explicitly ruled out, exactly as a real Vedic Jyotishi would). Structure stayed at exactly 4 bullets with Pranam opening, 2026 August dasha-link, and forward-looking Shani-MD timing. Single-fact regression intact: "Mera lagna kya he" → "Aapka Lagna Mithun hai." (23 chars / 1 line); "Mera nakshatra" → "Aapka Janma Nakshatra Dhanishtha hai, pada 3." (45 chars / 1 line).
- **Phase 2.4 Tier-1.5 Topic-Lock Focus** (01 May 2026): Quality-test follow-up to Phase 2.3 surfaced a **question→house mapping accuracy gap**: when devotee asks topic-specific questions, the LLM was citing adjacent / classically-unrelated houses (test 5 topics → 2/5 pass; friend-circle question pulled 9H, younger-sibling pulled 11H, children pulled 7H). Goal: **strict bhava focus + dasha-anchored timing** without rewriting the rule engine. Two-layer fix:
  - **Prompt-level expansion** (`openai_helper.py` `_PT_SYS_INTRO`, expanded from 15 → 17 rules, header comment updated): Rule 6 EXPANDED from a 5-topic cheat-sheet (Health/Career/Marriage/Wealth/General) to a **17-topic BPHS-aligned classical bhava+karaka mapping** (added: friend circle → 11H+Guru; elder sibling → 11H+Mangal; younger sibling → 3H+Mangal; children → 5H+Guru putra-karaka; mother → 4H+Chandra matr-karaka; father → 9H+Surya pitr-karaka; home/property → 4H+Mangal/Shukra; education → 4H+5H+Budh+Guru; higher studies → 9H+5H+Guru gyaan-karaka; court/mukadma → 6H+8H+Mangal+Shani; foreign travel → 12H+9H+Rahu videsh-karaka; spirituality → 9H+12H+5H+Guru+Ketu moksha-karaka). Added Rule 16 STRICT FOCUS (if a TOPIC-LOCK block appears at the top of the user message — see helper layer below — it OVERRIDES Rule 6: cite ONLY the listed houses/lords/karakas, NEVER cite anything in the "DO NOT cite" list, even if it seems classically interesting; clarity over breadth). Added Rule 17 DASHA-FIRST TIMING (every detailed answer must explicitly link the current Mahadasha–Antardasha lord to the topic-house or topic-lord — `"Aap [MD]–[AD] mein chal rahe ho. [MD] aapka [N]L hai, isliye agle [period] tak [topic-area] mein [effect] hoga"` — single-fact answers per Rule 1 are exempt; if dasha is unknown, skip the dasha-link entirely). System-prompt is now ~13.8 KB (was 7709 chars after Phase 2.3 → ~13856 after the Rule 6 expansion + Rules 16/17).
  - **Topic-detector + TOPIC-LOCK injection** (`openai_helper.py` Phase 2.4 block, ~290 lines after `_PT_SYS_INTRO` close): Five new module-level constants/functions. (a) `_TOPIC_RULES` — ordered list of 17 topic dicts with `topic_id`, human `label`, compiled regex `pattern` (Hindi/English/Hinglish keywords incl. `bade bhai`, `chhote bhai`, `bachhe`, `dost`, `mukadma`, `videsh`, `padhai`, `higher stud(y|ies)` etc.), `houses` (e.g. `[11]` for friends, `[3]` for younger sibling, `[12, 9]` for foreign travel), `karakas` (Sanskrit-named graha karakas with role hint), and `banned` (houses classically unrelated to that topic); rule order matters — `higher_studies` is placed BEFORE `education_basic` because the generic "study/studies" pattern would otherwise eat questions like "higher studies videsh mein possible?". (b) `_detect_topic(question)` — first-match scan over `_TOPIC_RULES`, returns the rule dict or None (None → topic uncertain → no lock injected, LLM falls back to expanded Rule 6 alone). (c) `_topic_lagna_sign_idx(kundli)` — extracts lagna sign 0-11 from `kundli.ascendant`/`kundli.lagna`. (d) `_topic_house_lord(kundli, house_num)` — whole-sign lord (BPHS Ch.5) for that house given user's lagna. (e) `_topic_current_dasha(kundli)` — current MD-AD planet pair from `kundli.dashas` Vimshottari tree (mirror of `kundli_full_context._section_future_dasha` lookup but returns just the lord names). (f) `_build_topic_lock(rule, kundli)` — composes the user-facing TOPIC-LOCK block in Hinglish with `━━━ TOPIC-LOCK ━━━` ASCII boundary, substituted house lords (e.g. for Dhanu lagna friend question: `"Cite SIRF: 11H + 11L (Shukra)"`), karaka graha line, current-dasha line with mandatory "in elements ka topic-house/lord se rishta MANDATORILY explain karein" instruction, explicit "DO NOT cite" line listing the banned houses by number, plus a Rule 16/17 reminder. Returns `""` on any failure (graceful — chart-context still ships, only the lock is skipped). Wired at all 3 passthrough emit sites (`_build_messages` ~L2755, sync `ai_ask` ~L13760, stream `ai_ask_stream` ~L16947) — the lock is **PREPENDED to the user message** (not the system prompt) so the system prompt stays cacheable across requests while the topic instruction still gets primary attention right before the question. Each wire site emits `topic_lock` + `lock_chars` in the `PASSTHROUGH.MESSAGES_BUILT` trace event for production telemetry.
  - **Architect-driven hardening (3 follow-up fixes, post-review)**: A code-review pass surfaced three issues that were patched in the same phase before sign-off. (i) **HIGH — multi-intent ambiguity**: `_detect_topic` originally returned the first matching rule, so `"shaadi aur bachhe kab honge?"` would single-route to `marriage` and Rule 16 would then forbid the 5H (children). Now `_detect_topic` collects ALL matches and (a) returns None if >1 distinct `topic_id` matched, or (b) does a secondary anchor-scan when a conjunction (`aur`, `and`, `&`, `plus`, `saath saath`, `tatha`, `evam`) is present and another strong topic noun appears, returning None in that case too. The intent: a half-right strict-focus lock is worse than no lock for mixed asks; deferring to Rule 6 keeps the answer broad. Verified live — `shaadi aur bachhe` now logs `topic_lock: null, lock_chars: 0`. (ii) **MEDIUM — dasha shape mismatch**: `_topic_current_dasha` only walked `kundli["dashas"]`, but `kundli_full_context._section_dasha` reads `kundli["currentDasha"]` (with `cd.maha` / `cd.antar`) for current-dasha display. The lock therefore often emitted `"(unknown — skip dasha-link)"` even when a current dasha was present. Now the function reads `currentDasha.maha`/`currentDasha.antar` first (parity with `kundli_full_context.py:311`), then falls back to the dashas-tree walk for legacy charts. Verified live — `lock_chars` for the same career question went 510 → 584 because the dasha-line is now populated. (iii) **LOW — `?` lord guard**: previously, if lagna was unknown, `_topic_house_lord` returned `"?"` and `_build_topic_lock` still emitted a lock with `"11L (?)"` strings — Rule 16 would then forbid the banned houses without offering a positive substitute. Now `_build_topic_lock` aborts (returns `""`) if `_topic_lagna_sign_idx` is None or any house-lord lookup yields `"?"`. Verified by unit test.
  - **Smoke test (17/17 PASS)**: 17 unit tests for `_detect_topic` cover all topic patterns including the higher_studies/education_basic order-dependent fix — 17/17 pass. End-to-end on the live API with a Dhanu lagna demo kundli: 17/17 leak-detection checks pass. For each topic the test confirms (a) at least one expected topic-house IS cited in the answer (regex matches `\b{N}H\b`, `\b{N}th\b`, `\b{N} bhav(aav|)\b`, `\b{N} ghar\b`) AND (b) NONE of the banned houses leak. Tests covered: elder_sibling cites 11H but not 3/5/7/9; younger_sibling cites 3H but not 5/7/11; siblings_general cites 3+11 but not 5/7; friend_circle cites 11H but not 3/5/7/9; children cites 5H but not 3/7/11; mother cites 4H; father cites 9H; home_property cites 4H; education_basic cites 4+5; higher_studies cites 9+5; court_case cites 6+8 but not 4/5/7/9/11; foreign_travel cites 12+9; spirituality cites 9/12/5; marriage cites 7/5/8; career cites 10/6/11; wealth cites 2/11/5; health cites 1/6/8/12 but not 7/11. Production telemetry confirms lock fires for all 17 topic_ids with `lock_chars` in the 485-576 range.
- **Phase 2.3 Tier-1 Anti-AI-Feel** (30 Apr 2026): User feedback after Phase 2.2 — persona Cosmo set ho gaya, but agar reply mein "As an AI", "consult a professional astrologer", "Hope this helps" type tells leak ho jayein, ya hedging ("shayad", "ho sakta hai") ya generic "tum" address use ho, to user ko turant lag jata hai "yeh AI hi hai". Goal: aisa lage ki ek **highly trained Vedic guru** real-time mein unki kundli padh raha hai. Two-layer fix:
  - **Prompt rules** (`openai_helper.py` `_PT_SYS_INTRO`, expanded from 11 → 15 rules): Added Rule 12 GURU-TONE (devotee ko warm "aap"/"jajman" se address; bade detailed answers mein occasional opening "Pranam" / "Suniye jajman" / "Dekhiye"; robotic tone aur dry bullet-listing forbidden). Added Rule 13 LAGNA-AWARE PERSONALIZATION (har detailed answer mein user ki lagna / rashi / nakshatra ka subtle reference — "Aap Dhanu lagna ke jatak hain, isliye Guru ka role primary hai" — taki devotee feel kare ki Cosmo unhe personally jaanta hai, generic answer nahi de raha; turn-wise alag angle rotate karna chahiye). Added Rule 14 NO-HEDGING (hedge words "shayad", "ho sakta hai", "may be", "might", "perhaps", "kind of", "possibility hai" forbidden; jab evidence clear hai → confident "hoga", "dikhta hai", "prabal hai", "saaf nazar aata hai" use karo; sirf jab actual evidence nahi hai (Rule 11) tab "iska clear sanket abhi nahi mil raha" — yeh weak nahi, scholarly humility hai). Added Rule 15 NO-AI-TELLS (Rule 2 ka defense-in-depth — explicit forbidden phrases list: "As an AI...", "I'm a language model...", "I cannot help with...", "Please consult a professional astrologer/qualified expert/real astrologer/practitioner/doctor/financial advisor/legal expert", "Hope this helps!", "Let me know if you have any questions", "I recommend/suggest/advise consulting/seeing/speaking to a professional"; explicit reminder ki "Tum Cosmo ho — Vedic Jyotishi KHUD ho. Aap hi 'professional astrologer' ho devotee ke liye"). System-prompt ab 7709 chars (5982 → 7709 over Phases 2.2 + 2.3).
  - **Defensive post-processor** `_scrub_ai_tells(text)` (new module-level function in `openai_helper.py` after `_PT_SYS_INTRO`): Belt-and-suspenders safety net. Even with Rules 2 + 15 in the prompt, the LLM may occasionally slip in a tell. The scrub uses (a) a fast-path substring check (~50 common AI-tell triggers like "as an ai", "hope this helps", "consult a professional", "chatgpt", "i recommend") to skip work in the common case, then (b) a slow-path of 9 compiled regex patterns (identity tells, capability disclaimers, "consult a professional X" with both bare + gerund verb forms, sycophantic closures, brand names) that drop any sentence containing a tell from the final reply. Sentence splitter handles English `.!?` plus Devanagari danda `।`. Idempotent + fail-safe — on any error returns input unchanged so the answer pipeline never breaks. If EVERY sentence is a tell (rare worst case), returns persona-neutral fallback "Iss point pe aapki kundli mein clear sanket abhi nahi mil raha — koi aur swaal puchhiye". Wired into all 3 LLM emit points: `ai_ask` sync passthrough return, `ai_ask_stream` final emit, and `ai_ask_stream` partial-final emit (mid-stream OpenAI failure salvage path). Each scrub call emits a `PASSTHROUGH(.SCRUBBED|stream).SCRUBBED` trace event when it actually trims, so production telemetry can track how often the safety net fires (ideally ~0%; rises = prompt rules need tightening).
  - **Smoke test (14/14 PASS)**: 11 unit tests for `_scrub_ai_tells` cover identity tells, sycophant tells, mid-sentence consult-professional, brand-name leaks (ChatGPT/OpenAI/Claude/Gemini/GPT-4), gerund verb forms ("I suggest seeing a qualified jyotishi"), all-tell fallback, and pass-through for benign text — 11/11 pass. End-to-end on the live API: 5/5 identity questions return canned Cosmo line via `shortcut:intro` (zero LLM cost, ~10ms); 6/6 Tier-1 checks on a real detail-grade career answer ("Career mein next 6 months") — confirmed reply contains explicit lagna reference ("Aap Dhanu lagna ke jatak hain, aur abhi Guru mahadasha–Rahu antardasha chal rahi hai"), zero AI/GPT/Cosmic-Lens leaks, zero hedge words (shayad/might/may be/perhaps), zero "consult a professional", zero "Hope this helps / Let me know if", and uses "aap"/"aapki" only (no "tum"); regression intact: "Mera lagna kya he" still 1-line "Aapka Lagna Dhanu hai." and "Mera nakshatra" still "Aapka Janma Nakshatra Ardra hai."; source attribution intact: "Yeh apko kaise pata" → "Aapki kundli mere paas hai, isliye seedha mujhse pata chalta hai." with no Section/JSON/raw-degree leak.
- **Phase 2.2 Persona + Source Attribution** (30 Apr 2026): User feedback that the assistant was answering "Yeh apko kaise pata?" by exposing internal data ("Section 1, Section 3, Lagna: Dhanu 268.92°") instead of naturally saying "Aapki kundli mere paas hai". Also that any identity question must use the **Cosmo** brand voice — never the words "AI", "GPT", "bot", "language model", etc. Two-part fix:
  - **Prompt rules** (`openai_helper.py` `_PT_SYS_INTRO`, L77): Added Rule 2 PERSONA — IDENTITY (name = "Cosmo", a "highly trained advance cosmic intelligence", with an explicit denylist of forbidden words: AI / A.I. / artificial intelligence / ChatGPT / GPT / language model / LLM / OpenAI / Anthropic / Google / bot / chatbot / machine / program / software / model / computer). Added Rule 3 SOURCE ATTRIBUTION — when devotee asks "yeh aapko kaise pata / source kya hai / kaise dekha / proof kya hai", reply naturally with "Aapki kundli mere paas hai" / "Aapke janma-vivran mere saamne hain", NEVER expose "Section N", raw degrees (`268.92°`), JSON, "data field", "context block", "system prompt", file/database names. Renumbered Rules 4–11 (and fixed a Phase 2.1 numbering bug where two rules were both labelled "4" — FOCUS + HISTORY). Two new rules push the system-prompt size from 5982 → 8623 chars; still well under any model limit. Updated module-header comment to document the 11-rule structure and the Phase 2.2 additions.
  - **Shortcut layer** (`shortcuts.py`): The `_INTRO` canned reply (used by the `shortcut:intro` fast-path) previously read "I'm Cosmic Lens — a Vedic astrology AI" — i.e. it leaked the forbidden word "AI" before the LLM ever saw the question. Rewrote all three locale variants (en/hi/hn) to use the new persona — the Hinglish text now reads verbatim: *"Main ek highly trained advance cosmic intelligence hun. Mera naam Cosmo hai. Aapki kundli mere paas hai — career, shaadi, health, paisa, current dasha — kuch bhi puchhiye, aapke chart ke aadhar pe jawab dunga."* Also expanded `_INTRO_RX` to catch additional phrasings: `kya tum (AI|bot|chatbot|robot) ho`, `(tum|aap|tu) (ai|bot|...) ho`, `are you (an? )?(ai|bot|chatbot|robot|human|real)`, `(tumhara|aapka|tera) naam kya hai`, `what(s|'s|\s+is) your name`, `your name kya hai`, plus romanised `kon` alongside `kaun`. Now any identity question short-circuits at the route layer (no LLM call, no quota slot, ~10 ms) with the exact Cosmo line.
  - **Smoke test (10/10 PASS)**: 8 identity variants ("Tum kaun ho", "aap kaun ho", "kya tum AI ho", "tumhara naam kya hai", "Aapka naam kya hai", "are you a bot", "who are you", "what is your name") all return the canned Cosmo reply via `source=shortcut:intro` with NO leakage of AI/GPT/ChatGPT/bot/Cosmic Lens. Source-attribution test: "Yeh apko kaise pata" (after a lagna setup turn) → "Aapki kundli mere paas hai, isliye seedha mujhse pata chalta hai." with NO Section/JSON/raw-degree leak. Regression: "Mera lagna kya he" still returns the Phase 2.1 one-liner "Aapka Lagna Dhanu hai."
- **Phase 2.1 Length-Match Fix** (30 Apr 2026): User feedback that single-fact questions like "Mera lagna kya he" were getting full TL;DR + 4-bullet treatment when they only deserve a 1-line answer ("Aapka Lagna Dhanu hai."). Added a NEW Rule 1 ("LENGTH MATCH KARO") to `_PT_SYS_INTRO` (`openai_helper.py` L80) at the TOP of the rules list, with explicit examples: single-fact factual queries (lagna / nakshatra / rashi / current dasha / shubh time / lagna lord / yog) → 1 line ONLY, no TL;DR prefix, no bullets, no breakdown, no CTA. Detail/scope/explain queries continue to use the existing TL;DR + 3-4-bullet structure (Rules 2-3 renumbered, all other rules pushed by +1). Smoke test: "Mera lagna kya he" → 22 chars / 1 line / 0 bullets ✓; "Mera janma nakshatra" → 39 chars / 1 line ✓; "Career next 6 months" still gets TL;DR + 4 bullets / 1003 chars ✓. The model now adapts answer length to question complexity automatically.
- **Phase 2 Output Polish** (30 Apr 2026): Two-part follow-up to Phase 1 — (a) **Prompt extraction**: the Phase 1 8-rule guide is now a single module-level constant `_PT_SYS_INTRO` at `openai_helper.py` L73; the three passthrough call-sites (`_build_messages` ~L2018, `ai_ask` ~L13007, `ai_ask_stream` ~L16158) all reference the same constant, so any future tuning is a single-edit change. (b) **Real SSE streaming**: `ai_ask_stream` (previously a one-shot envelope wrapper) was rewritten to perform genuine token-by-token streaming. It builds the full chart-context block locally (via `kundli_full_context.build_full_chart_context`), opens an OpenAI `stream=True` chat completion (timeout = module-scope `_PRIMARY_LLM_TIMEOUT_S=25s`, model from `OPENAI_MODEL`), yields `{"kind":"delta","text":...}` per OpenAI chunk, then a final `{"kind":"final", source:"ai_passthrough_stream", text, topic, follow_ups}` event. The Flask SSE route at `flask_app.py` L5953 forwards delta events as `data: {"delta": "..."}` and the final event as `data: {"done": true, "text": "...", ...}`. Smoke test: 374 delta chunks, first-delta at 7.7s, final at 10.9s, source `ai_passthrough_stream`. (c) **Mobile chat UX**: `ask.tsx` assistant messages now show an action row under the markdown reply with three buttons — `Sun lo` (existing voice TTS), `Copy` (uses `expo-clipboard`, flips to "Copied" + check icon for 1.4s), and `Share` (uses RN `Share` API for the native share sheet). All three use shared `assistantActionsRow` / `actionPlainBtn` styles; voice button keeps the accent border to remain the primary affordance.
- **Phase 1 Prompt Polish** (30 Apr 2026): The passthrough `_sys_intro_pt` system prompt (in `openai_helper.py`, applied to BOTH streaming L1957+ and non-streaming L13007+ paths) was upgraded from a 4-line minimal intro to an 8-rule output style guide. Rules: (1) TL;DR-first 1-line answer, (2) 3-4 bullet length cap, (3) question-relevant house/planet focus filter with quick-reference table for Health/Career/Marriage/Wealth/General, (4) reuse last 6 conversation turns instead of repeating facts, (5) ban generic Sun-sign/rashi-falit phrasing ("Mithun rashi ke hisaab se" → forbidden) — always personalize as "aapki kundli ke context mein" / "aapke Lagna se", (6) rotate CTA wraps instead of repeating "Agar chaho to...", (7) cite D9 navamsha placement for marriage/long-term-health/career/spiritual questions and call out vargottama planets, (8) anti-hallucination — only classical Vedic relationships (dignity, drishti, listed yogas, dasha, nakshatra) are permitted. Applied to both streaming and non-streaming passthrough. Smoke-test confirmed: TL;DR present, ~187 words / 11 lines (vs previous wall-of-text), no generic-rashi phrasing, no "Agar chaho to..." CTA, ~8s latency (down from ~30s).
- **Phase 2.8.17 — ARUDHA + ASHTAKAVARGA + SHADBALA + ARGALA added as Sections 10/11/12/13** (1 May 2026): User audited LLM-context items #22-#30 ("Vargas/Karaka/Bhava-strength/Argala/Arudha/AV/SB/Longevity/Event-prediction") and chose to ship #26 (Arudha), #27 (Ashtakavarga), #28 (Shadbala), #25 (Argala) immediately — all items where the LLM cannot derive results from training (pure chart-specific computation). All four underlying engines already existed (`jaimini.compute_arudha_padas`, `ashtakavarga.compute_ashtakavarga` + `format_sav_summary`, `shadbala.compute_shadbala`, `argala.compute_argala` + `format_argala_summary`) — work was 100% wiring + READING RULE footers + defensive adapters in `kundli_full_context.py`. Added `_section_arudha(kundli, lagna_sign_idx)` (~L1226-1295), `_section_ashtakavarga(kundli, lagna_sign_idx)` (~L1297-1356), `_section_shadbala(kundli, lagna_sign_idx)` (~L1366-1450), `_section_argala(kundli, lagna_sign_idx)` (~L1459-1525). Renumbered KP `## 10` → `## 14` and NIYAM `## 11` → `## 15`. **Section 10 ARUDHA**: cherry-picks the 4 most-actionable padas (A1=AL public image, A7=spouse perception, A10=career image, A12=UL marriage signature) with Hinglish sign rendering via `_sign_name`; preserves the classical "adjusted from X" note when Arudha falls on/opposite the house itself. **Section 11 ASHTAKAVARGA**: SAV-per-house table with verdicts (V/S/A/W bands), highlights VERY STRONG and WEAK houses, total 337/337 cross-check; reading rule lists all 4 SAV bands (32+/28-31/25-27/<25) per architect calibration. **Section 12 SHADBALA**: per-planet 6-fold strength sorted strongest→weakest, with strength_pct + STRONG/ADEQUATE/WEAK verdict and explicit Rahu/Ketu-not-evaluated note; required a `longitude→lon` adapter because `shadbala.compute_shadbala` expects key `lon` but our chart payload uses `longitude` (architect approved local adapter over wider module change). **Section 13 ARGALA**: uses `format_argala_summary(topic="general", max_houses=4)` for H1/H10/H7/H4 detail PLUS a cross-house scan that flags any STRONG-MALEFIC and STRONG-BENEFIC overall verdicts so LLM is warned topic-agnostic. **Architect code review found 2 real bugs + 1 calibration — all fixed**: (1) `_section_arudha` and `_section_argala` initially used the unsafe `kundli.get("ascendant") or kundli.get("lagna")` short-circuit pattern that was already explicitly fixed in `build_full_chart_context` for `lagna_idx` (regression risk: truthy-but-unparseable ascendant dict like `{"degree": 12.5}` would silently skip both sections even with valid `lagna`); fix: changed both signatures to take `lagna_sign_idx: Optional[int]` and added new `_ENGLISH_SIGNS` constant tuple to derive canonical English sign name from idx (since `jaimini.compute_arudha_padas` and `argala.compute_argala` only recognise English zodiac labels — passing Hinglish "Mithun" would silently fail). (2) Hinglish-payload regression: payloads with `ascendant="Mithun"` now correctly render Sections 10/13 because lagna_idx is computed via `_sign_idx` (which accepts both English+Hinglish) and converted back to English internally — verified with explicit test. (3) Ashtakavarga READING RULE only mentioned 32+ and <25 bands originally; expanded to mention all 4 bands (32+ VERY STRONG / 28-31 STRONG / 25-27 AVERAGE / <25 WEAK) for internal consistency with the legend. Verification: 28/28 negative+positive+architect-fix tests PASS — including Hinglish-ascendant rendering, dict-ascendant lagna fallback, all 4 SAV bands in reading rule, sb sorted desc, argala STRONG-MALEFIC scan flagging H4 in demo, arudha showing AL+UL. Demo Mithun chart total context size: 8712 → 10302 chars (+1590 chars / ~400 tokens). API server clean restart, no errors. **What this unlocks for the LLM**: (a) Arudha — "log aapko career mein kaisa dekhte" type questions now answerable via A10 sign+lord; "shaadi ke baad kaisa partner image bnega" via A7. (b) Ashtakavarga — concrete bindu numbers ("aapka H10=38 — career bhava VERY STRONG hai, even mediocre dasha mein bhi promotions aaenge") instead of vague "10th house is strong". (c) Shadbala — "kya aapka Mercury aapko answer dega?" now has a number ("Mercury 30.7% — well below 80% minimum, education/communication themes deliver only with sustained effort"). (d) Argala — "kaun aapke career path mein interfere kar raha hai?" answerable via H10's argala signal table. Cost: ~+₹0.00006 per query for full Jaimini+BPHS chart-specific computation awareness. Items #22, #29, #30 still pending (Vargas D10, Longevity intentionally excluded per ethical policy, Event-prediction framework is prompt-level not data-level).

- **Phase 2.8.16 — DASHA + TRANSIT OVERLAY added as Section 9** (1 May 2026): Highest-leverage timing-context block. After Phase 2.8.15 added Section 8 (gochar of slow planets), the LLM still had Sections 4 (active dasha) and 8 (gochar) as TWO disconnected facts and had to do the synthesis itself — which it cannot do for fast-moving dasha lords (Mer/Ven/Mars/Sun/Moon) since their current sky position simply isn't in its training data. User said "#20 start" (Item #20 of the LLM-context audit). Added `_section_dasha_transit_overlay(kundli, intel, birth)` in `kundli_full_context.py` (~L1031-1187 of the new file) plus a small inline `_planet_lon_now(name, when_utc)` swisseph helper covering ALL 9 grahas (fast+slow, with Ketu derived as Rahu+180°). The section reads MD/AD/PD planet names from `kundli.currentDasha`, computes RIGHT-NOW sidereal Lahiri longitude for each, derives transit sign + degree + house-from-natal-Lagna, and emits 5 deterministic flags per dasha level: (1) ECHO amplification ("X ki dasha + X ka transit = themes amplify"), (2) supportive-houses flag (H1/H5/H9 = kendra/trikona, H11 = labha-bhava, H10 = karma-bhava intensification), (3) restrictive-houses flag (H6/H8/H12 = dusthana, hidden hurdles), (4) **swagriha-like overlay** (transit planet sitting in same sign as natal placement = STRONGEST possible echo, lord on home turf), (5) combust check for inner planets (Mer/Ven/Mars within ±8° of current Sun → dasha effects MUTED). Section ends with explicit OVERLAY READING RULE footer: dasha lord's current transit position is the timing trigger, supportive vs restrictive house lists, combust mute caveat, and "use overlay for SHORT-TERM timing within active dasha". Wired in `build_full_chart_context` AFTER `_section_gochar` and BEFORE `_section_kp` so live-sky context (Section 8) → dasha-overlay synthesis (Section 9) → KP fructification (Section 10) flows logically. Renumbered KP `## 9` → `## 10` and NIYAM `## 10` → `## 11` (2 label changes only). Self-contained `_OVERLAY_SWE_IDS` map declared locally with try/except guard so module import never fails when swisseph is unavailable; module-level `_OVERLAY_HAS_SWE` flag short-circuits the section to return "" cleanly. IST timezone display + UTC astronomy (same pattern as Section 8). Local `_SIGN_NAMES_OUT` list keeps section self-contained for ADD-ONLY policy. Verification on demo Mithun lagna 14/06/1990 06:30 AM Delhi: Jupiter MD active until 2026-08-22 + Jupiter currently transiting Mithun 24.75° (= H1 from Lagna) + native has natal Jupiter ALSO in Mithun → triple overlay correctly flagged "Transit is in same sign as natal Jupiter — STRONGEST possible echo (lord on home turf)" with H1 supportive flag. Rahu AD active + Rahu transiting Kumbh = H9 → kendra/trikona supportive flag. **6/6 negative tests PASS**: (1) no currentDasha→empty, (2) garbage planet names→empty, (3) MD-only renders (no AD/PD blocks)→clean, (4) Ketu MD position correctly derived as Rahu+180°, (5) no-lagna→empty, (6) all-None lords→empty. Section size: ~1218 chars (~300 tokens) on 2-level dasha demo. Total context size: 7494 → 8712 chars (+1218). Section header sequence verified: 1→2→3→4→5→6→8→9→10→11. API server clean restart, no errors. **Architect code review found 1 astrological-correctness bug — fixed**: my flag wording "swagriha-like (lord on home turf)" was technically wrong because *swakshetra* / *swagriha* mean "planet in the sign IT RULES" (e.g. Sun in Leo, Mars in Aries/Scorpio), but my check was purely "transit returning to natal sign" — a positional resonance condition that has zero implication for sign rulership/dignity. A native could have Saturn natal in Aries (debilitation sign) and Saturn currently transiting Aries would still trigger the flag, which would be falsely labelled as "home turf" — astrologically incorrect. Renamed flag text → "NATAL-SIGN RESONANCE — {planet} transit is in same sign as natal {planet} (positional return; STRONG dasha-echo regardless of dignity)" and added explanatory comment block in the natal_sign dict-build site warning future contributors not to revert. Also applied 2 cheap polish fixes from architect: (a) case-insensitive natal-planet name lookup (uppercase "JUPITER" payload still matches lowercase "jupiter" key now) — verified with extra test T7 PASS; (b) explicit combust-orb policy disclosure in OVERLAY READING RULE footer — tells the LLM "we use uniform 8° orb (strict) for Mer/Ven/Mars; classical orbs are wider per-planet so flag absence does not rule out near-combust influence". This makes the synthesis layer more honest about its own simplifications. **What this unlocks for the LLM**: nuanced answers like "Aapki Jupiter MD active hai aur Jupiter abhi aapke natal Mithun mein hi transit kar raha hai, jo aapka Lagna bhi hai. Yeh teen-tarfa overlay (Jupiter dasha + Jupiter return + Lagna par hi sthit) next 4 months mein dharma/wisdom themes ko peak deta hai" — previously impossible because LLM had no way to know Jupiter's current sky position OR that it coincided with the native's natal placement. Cost: ~+₹0.00004 per query.

- **Phase 2.8.15 — GOCHAR (live transits) added as dedicated Section 8** (1 May 2026): User audited which of the 20-item LLM-context checklist items reach the model. Item #19 (Transit/Gochar analysis) was the biggest gap — only the Sade-Sati phase string was reaching the LLM via Section 7, and even that only fired when the kundli payload pre-computed it. User said "Pehele 19 add karo." Added `_section_gochar(kundli, intel, birth)` in `kundli_full_context.py` (L807-906 of new file) that **reuses the production `transits.compute_transits()` helper** (already powering marriage_engine + career_engine) — no duplicate astronomy code. Section dumps current sidereal sign + house-from-natal-lagna for the four slow-moving planets only (Saturn/Jupiter/Rahu/Ketu); fast planets deliberately excluded because their transit changes within hours/days and would make any cached LLM answer go stale. Independent natal-lagna fallback (`ascendant` first, then `lagna` — same defensive pattern as Section 2's FN column). Natal Moon sign auto-extracted from `kundli.planets[]` for Sade-Sati phase computation. Optional DOB datetime built defensively from `birth` dict for Saturn-return / Jupiter-return age-window flags. All `compute_transits()` narrative flags surfaced when emitted: `sade_sati_phase`, `saturn_caution_flag` (Saturn over natal H8/H12), `jupiter_lucky_flag` (Jupiter aspecting natal kona/kendra), `rahu_theme_flag` (Rahu over natal H1/H7/H10), `saturn_return`, `jupiter_return`, plus always-on Jupiter aspect houses. Section ends with explicit "GOCHAR READING RULE" footer reminding LLM that **transits fructify only when active dasha-bhukti aligns** — forces the model to cross-check Section 4 instead of giving naive "Saturn is in your H10 so career trouble" predictions. Wired in `build_full_chart_context` BEFORE `_section_kp` so live-sky context is fresh when LLM descends into KP fructification analysis. Renumbered KP from `## 8` → `## 9` and NIYAM from `## 9` → `## 10` (2 label changes only, no behaviour delta). Cleaned Section 7 header from "YOGAS / DOSHAS / SADE-SATI / GOCHAR" → "YOGAS / DOSHAS / SADE-SATI" since gochar now has its own dedicated section (legacy `kundli.transits` dict fallback inside Section 7 still runs untouched for backward compatibility). Verification on Mithun lagna 14/06/1990 06:30 AM Delhi demo chart: Saturn=Pisces (H10 ✓ from Mithun), Jupiter=Gemini (H1 ✓), Rahu=Aquarius (H9 ✓), Ketu=Leo (H3 ✓), Sade-Sati=Last phase (Saturn 2nd from natal Moon ✓), Jupiter return window age 35.9 ✓. Section size: ~890 chars (~220 tokens). Section header sequence verified: 1→2→3→4→5→6→8→9→10 (Section 7 still conditionally rendered when intel/kundli has yogas/doshas data; absent in pure-positions demo chart, pre-existing behaviour, not a regression). Negative tests: empty birth dict → gochar still renders (DOB-return windows lost only); ascendant=garbage string → empty section (clean defensive return); no birth dict → still renders since lagna+moon come from kundli not birth. API server clean restart, no errors. Total context grew from ~4321 (without KP) → ~7494 chars (with both KP + new Gochar) on demo chart. Cost impact: ~+₹0.00003 per query. **What this unlocks for the LLM**: real-time "abhi Saturn aapke H10 par chal raha hai, Mahadasha bhi Saturn ki hi hai — toh next 18 months career mein heavy responsibility, but stable" type answers that require BOTH natal context AND live sky — previously impossible because LLM has zero real-time astronomy in its training data.

- **Phase 2.8.14 — FUNCTIONAL BENEFIC/MALEFIC (lagna-based) added to Section 2** (1 May 2026): User audited the 8-item checklist (items 3-10) and confirmed 8/10 going to LLM. The two real gaps were Item 5 (12 signs nature & traits — declined as luxury since LLM has it from training) and **Item 8 (Functional Benefic/Malefic)** — REAL gap because functional nature is computed PER LAGNA (e.g. for Mithun lagna Mars=FM but for Karka lagna Mars=YK), and the LLM cannot derive this without explicit chart-context injection. Encoded `_FUNC_NATURE` table at `kundli_full_context.py` L79-104 — 12-tuple of dicts (one per lagna sign 0=Mesh...11=Meen), each mapping the 7 visible planets → `'FB'` (Functional Benefic) / `'FM'` (Functional Malefic) / `'YK'` (Yogakaraka, kendra+trikona lord) / `'N'` (Neutral). Following standard Parashari (BPHS) classification: lagna lord + trikona (5L/9L) lords → FB; trika (6L/8L/12L) lords → FM; planet ruling BOTH a kendra (1/4/7/10) AND trikona (1/5/9) → YK; natural benefic (Jup/Ven/Mer) ruling ONLY a kendra → kendra-adhipati dosha → FM; mild houses (2L maraka, 3L mild malefic, 11L mixed) → N unless they double up. Added helper `_functional_nature(planet, lagna_sign_idx)` at L242-258 returning `'-'` for Rahu/Ketu (no rulership) and `''` for invalid lagna_sign_idx — with explicit `isinstance(..., bool)` guard since `bool` is a subclass of `int` in Python and `True`/`False` would otherwise map to Vrish/Mesh lagnas. Updated `_section_grahas` (L373-466) signature with new `lagna_sign_idx` kwarg, added 2-char "FN" column between "Rules" and "Status", and appended a one-line legend `"FN legend: FB=Functional Benefic | FM=Functional Malefic | YK=Yogakaraka (kendra+trikona lord) | N=Neutral | Rahu/Ketu=shadow malefics (no rulership)"` after the table when any planet row was rendered. Wired in `build_full_chart_context` (L935-941) — computes `lagna_idx = _sign_idx(kundli.get('ascendant'))` and falls back independently to `_sign_idx(kundli.get('lagna'))` if the first is None (the prior `or` short-circuit had a real fallback bug when ascendant was a truthy-but-unparseable dict like `{"degree": 12.5}`). **Architect code review found 4 issues — all fixed**: (1) `_FUNC_NATURE[0]['Sun']` was `'YK'` but Mesh Sun rules only H5 (trikona, no kendra) → corrected to `'FB'`; (2) `_FUNC_NATURE[5]['Venus']` was `'YK'` but Kanya Venus rules H2+H9 (no kendra) → corrected to `'FB'`; (3) Meen Sun was `'N'` but Sun rules H6 (Simh) for Meen lagna → corrected to `'FM'`; (4) bool-passes-isinstance(int) edge case + ascendant-or-lagna short-circuit fallback both fixed. Verification: 6/6 canonical Yogakaraka tests PASS (Karka→Mars, Simh→Mars, Vrish→Saturn, Tula→Saturn, Makar→Venus, Kumbh→Venus); YK invariant test confirms ONLY these 6 entries are labeled YK across the full 12×7 = 84-cell table (no extras); 5/5 negative tests PASS (Mesh-Sun, Kanya-Venus must NOT be YK; Meen-Sun must be FM); bool guard PASS (`True`→empty, `False`→empty); dict-fallback PASS (truthy unparseable ascendant + valid lagna string → correctly resolves to lagna). Demo Mithun chart Section 2 now shows: Mer=FB ✓, Ven=FB ✓, Mars=FM ✓, Jup=FM ✓, Sun=N ✓, Moon=N ✓, Sat=N ✓, Rahu/Ketu=- ✓ (matches standard Parashari). Section 2 size: 1252 → ~1400 chars (+150 chars / +35 tokens). API server clean restart, no errors. Cost: ~₹0.00002 extra per query for chart-specific functional nature awareness — eliminates LLM's blind-spot on lagna-dependent benefic/malefic classification (e.g. it can now correctly say "Mars is your YK and rules 5+10, so its dasha brings career + intellect highs together" for Karka lagna users without inventing this from generic templates).
- **Phase 2.8.13 — GRAHAS TABLE: Naks-Pada + Naks-Lord + Rules + Aspect-Targets** (1 May 2026): User audited what currently goes to LLM in full chart context (`build_full_chart_context` in `kundli_full_context.py`), found Section 2 (SAARE 9 GRAHAS) had 4 critical gaps: (1) **Nakshatra-Pada column EMPTY** — table headers said "Nakshatra-Pada" but planet objects don't carry pre-computed nakshatra fields so all rows showed blank. (2) **Naks-Lord column EMPTY** — same root cause. (3) **No "Rules" column** — LLM saw Section 3 (Bhavas) with house→lord mapping but had no per-planet reverse mapping, so it didn't know that e.g. Mercury rules H1+H4 for Mithun lagna without manual lookup. (4) **Aspects column was bare house numbers only** ("asp H6, H10") — missing the actual planet targets, so LLM couldn't see "Mars aspects Jupiter in H1" type drishti. User's directive: *"Nakshtra pada add karo, Dignity status add karo, Aspect bhi add karo konsa planet kisko dekh raha he, Konsa planet kis ghar ka lord he woh sab bhi."* Added 3 new helpers in `kundli_full_context.py`: (a) `_NAKSHATRAS` constant — 27-tuple of `(nakshatra_name, vimshottari_lord)` pairs in standard sidereal order from 0° Aries; (b) `_naks_pada_lord(longitude)` (L145-166) — pure-stdlib computation: `nak_idx = lon // (360/27)`, `pada = (lon mod 13.333) // 3.333 + 1`, returns `(name, pada, lord)` triple, defensive on bad input returns `("", 0, "")`; (c) `_lordship_lookup(intel)` (L169-187) — builds reverse mapping `planet → sorted list of houses ruled` from `intel.house_lords` (e.g. `{"Mercury": [1,4], "Saturn": [8,9], ...}`), Rahu/Ketu absent (no sign rulership); (d) `_aspects_lookup(kundli)` (L190-235) — calls `aspects.compute_aspects(planets, lagna_sign_idx)`, renders rich strings like `"H10(Mars),H2(Ketu),H5"` showing both aspected house AND aspected planet name(s), defensive returns `{}` on import error / bad data. Updated `_section_grahas` (L304-388) to accept new `lord_lookup` and `asp_lookup` params, replaced "Nakshatra-Pada" header with tighter "Naks-Pada" (20 chars), tightened "Naks-Lord"→"NL" (7 chars), added new "Rules" column (8 chars showing e.g. "H6,H11" for Mars or "-" for Rahu/Ketu); status column now prefers rich aspect strings from `_aspects_lookup` and falls back to `dignity.aspects_houses` bare-house list when unavailable; nakshatra/pada/lord columns prefer pre-computed planet fields then fall back to `_naks_pada_lord(longitude)` so KP and Lahiri ayanamsa flows both work. Wired both new lookups in `build_full_chart_context` (L848-859) so they're built once per request and threaded into the section. **Smoke test (production simulation with `analyze_chart` intel + birth dict)**: all 9 planets now show Naks-Pada (e.g. Sun→Mrigashira-2, Moon→Dhanishtha-3, Saturn→Uttara Ashadha-2), all show Naks-Lord (Mars/Saturn/Sun/Jupiter etc.), all 7 sign-ruling planets show Rules (Sun→H3, Mars→H6+H11, Mercury→H1+H4, Jupiter→H7+H10, Saturn→H8+H9 for Mithun lagna), aspects column upgraded from "asp H6" → "asp H1(Jupiter),H4,H5" type rich drishti showing Mars aspecting Jupiter, Saturn aspecting Mars and Ketu, Rahu aspecting Sun+Mercury cluster. Section 2 size: 903 → 1252 chars; full context: 6140 → 6754 chars (+614 chars / ~+150 tokens per request). All 9 sections still render. Failsafes verified: no-intel/no-birth → 3707 chars (lighter, still works); empty kundli → 0 chars (proper no-op). **Architect code review found 2 critical issues — both fixed**: (1) `_naks_pada_lord` had float-division boundary drift (e.g. lon=10.0° was returning pada 3 instead of pada 4 because `10.0 / (360/108) = 10.0 / 3.3333333335 = 2.9999...` floored to 2), fixed by switching to integer arc-second math (`arcsec = int(L * 3600.0); pada_idx = (arcsec // 12000) % 108; nak_idx = pada_idx // 4; pada = pada_idx % 4 + 1`) — uses floor (not round) so EXACT boundaries (10.0°, 13.333...°, 40/3°) classify into the next pada/nakshatra while just-below values (3.3333°, 13.3333°) correctly stay in the previous one; 14/14 boundary regression tests now PASS including all 4 architect-flagged repro cases. (2) `_lordship_lookup` could AttributeError on non-string lord values (e.g. `intel.house_lords[i]['lord'] = 123` would crash on `.strip()`), fixed by upfront `isinstance(lord_raw, str)` check before strip — now silently skips bad rows; tested against 7 malformed intel shapes (int lord, None lord, string house, non-list field, None row, None intel, empty intel) — all return safely without raising; full context build with malformed intel still produces 5538 chars instead of crashing. API server clean restart, no errors. Cost: ~₹0.00006 extra per query for full graha-level chart awareness.
- **Phase 2.8.12 — KP FULL VISIBILITY in chart context** (1 May 2026): User flagged that when full kundli is sent to LLM (`build_full_chart_context` in `kundli_full_context.py`), KP (Krishnamurti Paddhati) data was NOT included — it only flowed via topic-filtered `_kp_context()` in `openai_helper.py` L2999 which surfaces 4-6 houses based on topic. User requirement: *"Sublord and nakshatra lord and planet ke saath number bhi aana chahiye — har CSL ke sath, 1st csk kon he 8th csk kon he, pura."* Added new **Section 8 (KP — FULL CUSPS + PLANETS)** to full chart context via new `_section_kp(birth)` helper at `kundli_full_context.py` L566-636, wired into `build_full_chart_context` at L758-763 (after Section 7 Yogas, before NIYAM). Renumbered NIYAM from "8." → "9." Per-cusp output: 12 rows showing `Hse | Degree (formatted + numeric longitude) | Nakshatra | Sign-L | Nak-L | Sub-L (CSL) | Sub-Sub`. Per-planet output: 9 rows showing `Planet | Degree (formatted + numeric longitude) | House | Nakshatra | Sign-L | Nak-L | Sub-L | Sub-Sub`. Plus Krishnamurti ayanamsa value and 4-line KP reading-rule reminder ("CSL = final deciding authority for that house matter; matter fructifies during DBA of planets that signify relevant houses; CSL signifies positive houses → YES, signifies negation → NO"). Defensive: returns "" if birth dict is None / missing required fields (day/month/year/hour/minute/ampm/lat/lon/tz) / `kp_engine.calculate_kp` raises / cusps+planets both empty — chart context never breaks. **ADD-ONLY: old topic-filtered `_kp_context()` stays untouched** — it provides the deeper DBA-vs-significators analysis for the question topic; new Section 8 provides full visibility on every question regardless of topic. **Smoke test**: standalone `_section_kp(birth)` produces 2688 chars correctly formatted; full context grows from ~4500 → 6140 chars (+1700 chars / ~+450 tokens per request); all 4 defensive paths PASS (None, partial, bad data, normal). Cost: ~₹0.0002 extra per query for total KP visibility.
- **Phase 2.8.11 — SOFT REASON ANCHOR (Rule [3b])** (1 May 2026): User shared an external analysis (5-point breakdown) that perfectly matched the shipped Soul Checklist 4 of 5 — but flagged a missing "Reason" point: real astrologers add a soft causal anchor (e.g. "(current dasha me emotional fluctuation active hai)") so devotee feels their feel has a *real underlying cause*, not random. User's one-liner: *"User ko answer nahi chahiye — unko 'samjha hua' feel chahiye."* Currently Mode 1 strictly bans all chart-tech, so the model was producing observation bullets but no causal anchors. Added new **Rule [3b] SOFT REASON ANCHOR** to `_BREVITY_MODE_BLOCK` (`openai_helper.py` ~L652-675): allowed soft anchors WITHOUT planet/house/varga jargon — phrasings like "abhi ki dasha mein emotional fluctuation active hai", "chal rahe daur mein over-thinking pattern uthta hai", "is samay rishton mein testing-energy chal rahi hai", "chal rahi dasha mein over-thinking active hai", "current phase mein heart-mind sync zyada hilta hai"; explicit BAN on Saturn/Rahu/Jupiter/7th-lord/D9-navamsha jargon stays strict; placement preference 1st-CHOICE = verdict line as parenthetical, 2nd-CHOICE = bullet end, 3rd-CHOICE = closing line; mandatory whenever Rule [2] empathy is active. Updated GOLD-STANDARD example to show the parenthetical anchor INSIDE the verdict line ("Haan, aapka feel sahi pakad raha hai — abhi ka phase genuinely thoda tough hai (chal rahi dasha mein emotional fluctuation active hai), par yeh permanent nahi hai") instead of inside a bullet (which collided with the 15-word bullet cap and was getting picked up only 1/3 of the time). Added a 6-anchor reference list ("MORE SOFT-REASON-ANCHOR EXAMPLES") so the model has variety to draw from. Updated FINAL SELF-CHECK to add a 4th checkpoint specifically for the reason anchor, and appended user's quote *"Devotee ko answer nahi chahiye — unko 'samjha hua' feel chahiye"* as the closing directive. **Test results (5/5 PASS, all 4 emotional Q's hit all 5 Soul Checklist items)**: screenshot Q now produces "(is samay rishton mein testing-energy chal rahi hai)" mid-verdict; abstract vulnerability Q gets "(chal rahe daur mein emotional fluctuation active hai)"; relationship pain Q gets "(is samay rishton mein testing-energy chal rahi hai)"; career anxiety gets "(chal rahi dasha mein over-thinking active hai)" — each anchor unique to the Q context (not template-copied), conceptually grounded but jargon-clean (zero planet/house/varga leak across all 5 tests). Pure factual Q ("shaadi 2027?") still correctly skips both empathy preface AND reason anchor (exemption working). Mobile streaming endpoint verified producing identical 5/5 quality as sync.
- **Phase 2.8.10 — SOUL CHECKLIST TIGHTENING** (1 May 2026): User flagged a 2nd screenshot showing Phase 2.8.9 Soul Checklist was being inconsistently applied — bot replied to "kya yeh **normal** phase hai?" with "Haan, abhi ka phase hila hua lagta hai, aur yeh is waqt **normal** hai" — three failures: (a) PARROTING — used user's word "normal" in verdict; (b) EMPATHY weakened — missing "aapka feel sahi hai" anchor + "par permanent nahi" reassurance, replaced with cold "yeh is waqt normal hai"; (c) FORWARD SIGNAL generic — "yeh phase guzrega" instead of specific timeline like "August 2026 ke baad". Confirmed mobile app uses streaming codepath (`ai_ask_stream` L17685) which references the same `_PT_SYS_INTRO + _BREVITY_MODE_BLOCK` as sync (L14463) — so the inconsistency was LLM stochasticity (temp=0.5), not codepath divergence. Tightened the Soul Checklist from soft guidelines into **strict execution recipes**: (a) Rule [1] NO PARROTING now has a **hard banned-word list** ('normal', 'unstable', 'breakup', 'confused', 'tension', 'stress', 'akela', 'pareshaan', 'gadbad', 'tough') with explicit "verdict likhne se pehle khud check karo — devotee ne jo problem-word use kiya, woh aapke verdict mein hai? Agar haan, reword karo" pre-write directive + 2 ❌/✅ pairs (one of which uses the exact 'normal' Q from the screenshot); (b) Rule [2] EMPATHY now requires a **2-part structure** — (i) ACKNOWLEDGMENT from 5 sample phrasings ('aapka feel sahi hai' / 'aapne sahi pakda' / etc) AND (ii) REASSURANCE from 5 sample phrasings ('par permanent nahi' / 'yeh tootne wala daur nahi' / etc), both MUST appear in verdict's first line, "agar dono mein se ek bhi missing hai → output reject karo aur dobara likho"; (c) Rule [4] FORWARD SIGNAL now requires **concrete timeline** (specific year/month/period like "2026 ke baad" or "agle 6 mahine") OR specific actionable advice ("filhaal khud pe focus karo") — generic "yeh phase guzrega" / "sab theek hoga" explicitly **REJECTED**; (d) added a new **FINAL SELF-CHECK block** at the end of the Soul Checklist with 3 verification checkpoints (parroting word-scan, empathy 2-part presence, concrete-signal presence) and instruction "agar koi check fail ho raha hai, output rewrite karo. Speed se zyada quality important hai." **Test results (5/5 PASS, including streaming endpoint that mobile app uses)**: screenshot Q on stream now returns "Haan, **aapka feel sahi hai** — abhi daur hilta hua hai, **par permanent nahi**" + personalized bullets + "**August 2026 ke baad** rishton mein zyada seedhapan" + natural CTA — words 'normal' and 'unstable' fully banned from verdict; consistency check (same Q, 2 separate stream runs) both produced Soul-compliant outputs with different surface wording but identical structure proving rules are now sticky under stochasticity; vulnerability Q with banned word "tension" → "tension" replaced with "bhaari" + full empathy + Aug 2026 timeline + "expectations halka rakhiye" action; pure-factual Q ("shaadi 2027?") still correctly SKIPS empathy preface (exemption intact). Both `ai_ask` (sync at L14478) and `ai_ask_stream` (L17695, mobile codepath) verified producing identical Soul-grade output — the prompt-level fix propagates to both endpoints.
- **Phase 2.8.9 — MODE 1 SOUL CHECKLIST** (1 May 2026): User flagged a screenshot showing technically-correct but soul-less output for "Meri love life abhi kaisi chal rahi hai? Sab thoda unstable lag raha hai, kya yeh normal phase hai?" — bot replied "Abhi aapki love life unstable phase mein chal rahi hai" (parroting), generic bullets ("Attraction strong hai..."), no empathy, no timeline, mechanical CTA. User asked "kya yeh thik hai latest model aisa ans dega" — fair critique. Added a new **MODE 1 SOUL CHECKLIST** block to `_BREVITY_MODE_BLOCK` (`openai_helper.py` ~L609-660) with 5 named guardrails + ❌/✅ examples: **[1] NO PARROTING** — never restate user's exact word in verdict (e.g. "unstable lag raha hai" → don't reply "haan unstable hai"); **[2] EMPATHY-FIRST** — if user shares vulnerability (pareshaan/dard/samajh nahi/sab gadbad/breakup/akela), warm acknowledgment MUST precede verdict ("Beta, aapka feel sahi hai" / "Yeh phase genuinely tough hai par...") — pure factual Q's like "shaadi 2027 hogi?" can skip empathy preface; **[3] PERSONALIZED bullets** — each bullet must read chart-specific ("aapka mann jaldi judta hai"), not relationship-column generic ("attraction strong hai"); **[4] FORWARD SIGNAL** — for pain/uncertainty Q's, include relief/timeline cue ("2026 ke baad clarity wapas aati hai" / "Filhaal khud pe focus karo, rishta apni jagah pakda lega") — without this output feels cold; **[5] CTA NATURAL** — bracket/italics blend ("(Chaho to step-by-step bata du ki kundli mein yeh phase kis wajah se ban raha hai.)") instead of mechanical "Agar chaho to..." literal append. Updated Mode-1 GOLD-STANDARD example to use the exact Q from user's screenshot showing the full empathy → personalized bullets → forward signal → natural CTA recipe. **Test results (5/5 PASS)**: screenshot Q now produces "Haan, aapka feel sahi hai — abhi ka phase hila hua hai, par permanent nahi" + personalized bullets ("Dil se jodne ki ichchha strong hai...") + forward signal ("2026 tak on-off pattern rahega; uske baad cheezein zyada pakki hongi") + natural CTA in brackets; vulnerability Q ("dimaag chal raha hai, gadbad") gets empathy + practical close ("neend, routine aur akelapan ko lightly mat lijiye"); pure-factual Q ("shaadi 2027?") correctly SKIPS empathy preface; indirect vulnerability ("partner support nahi mil raha") detects emotional context without explicit "pareshaan" keyword. Casual exemption ("thanks bhai" → "Aashirvaad.") still intact. The model now reads emotional context from the question and modulates warmth/timeline/personalization accordingly — guru-grade transformation while keeping format spec intact.
- **Phase 2.8.8 — LLM-TRUST MODE SELECTION** (1 May 2026): User feedback that 2.8.7's deterministic keyword-trigger detector (`_is_transparency_query` matching substrings like `kaise/kyun/explain/...`) is brittle — it fails on natural phrasings without trigger words (e.g. "samajh nahi aa raha" demands explanation but has no keyword) and over-triggers on long paragraphs. User instruction: *"LLM latest model hai, usko pura power do question samjhane ke liye — chahe 1 line ho chahe 20 line paragraph, woh pura samjhega."* Refactored: (a) `_is_transparency_query()` now always returns `False` — the function is dead code (deprecated docstring kept) but signature retained for the 6 backward-compat callsites at L3419, L14429, L17647 etc; (b) `_TRANSPARENCY_DIRECTIVE` is no longer injected (since detector returns False, the `if _is_transparent else ""` guards always evaluate to empty); (c) `_BREVITY_MODE_BLOCK` Mode 2 section rewritten to explicitly tell the LLM *"Tum latest GPT ho — keyword-list mat dhundo, INTENT samjho. Question chahe 1 word ho ya 20 line paragraph, tumhe poori samajh hai"* with intent signal list and explicit "even if exact word list mein nahi hai, INTENT pakdo" override; (d) Decision flow rewritten: *"Trust your own language understanding. Rigid template/keyword pe mat fasna"*; (e) `brevity_tail` final reminder mirrored same shift; (f) `max_tokens` unified to 600 at both sync (L14478) and stream (L17695) callsites — was previously `600 if _is_transparent else 450`, now single budget since LLM picks mode dynamically. **Test results (live API)**: 7/7 effective PASS — HC1 "samajh nahi aa raha relationship kyon nahi chal raha" → Mode 2 ✓ (LLM caught semantic intent without keyword); HC2 30-word breakup paragraph → Mode 2 ✓ (full Step 1/2/3/Final with chart-tech); HC3 ambiguous "matlab?" → Mode 1 with clarification ask ("Kripya apna prashn saaf likhiye") which is actually CORRECT human-like behavior; QM1-QM3 (yes/no, binary, casual) all Mode 1 ✓; QM2 also fired the optional CTA "Agar chaho to main iska reason step-by-step samjha sakta hoon"; EDGE "kyunki main pareshaan hoon" → Mode 1 ✓ (LLM understood "kyunki" as "because" connector, NOT a why-question — pure semantic understanding); explicit Mode 2 with "kyun + explain karo step by step" → perfect Step format.
- **Phase 2.8.25 — POPULATE `love_or_arrange.py` WITH 25-RULE CLASSIFIER + 3 TRUST LAYERS** (1 May 2026): Per user direction *"abhi A ko start karo 600 lines code"* (Option A from a 3-option deliberation: A=25 rules + 3 trust layers / 6 hr / 90% trust, B=40 rules + 6 trust layers / 18 hr / 97% trust, C=hybrid phased mega). Populated the previously-stub `marriage_engine/love_or_arrange.py` (Phase 2.8.23 left it returning `{}`) with a **890-line classifier** implementing **3 independent astrological engines + 3 trust layers**. **THE 3 ENGINES**: (1) **Parashari D1** — 10 classical rules: 5L→7H (Pancham-Saptam), 7L→5H reverse, Venus+Rahu conjunction, Venus+Mars conjunction, Rahu in 7H, Moon+Venus conjunction (LOVE side); Jupiter aspect on 7H (in 1/3/11), 9L in 7H or conjunct 7L, 2L conjunct 7L, Saturn in 7H without Rahu (ARRANGED side). (2) **D9 Navamsha** — 4 navamsa-vargottama rules: Venus+Rahu same D9 sign, 5L+7L same D9 sign, 7L conjunct Rahu in D9 (LOVE); 7L exalted/own in D9 (ARRANGED). Calls `divisional_charts.compute_d9()` lazily so missing data → graceful UNKNOWN. (3) **KP CSL (Cuspal Sub-Lord)** — 4 core levels (7th CSL signifies 5H, 7th CSL star lord, sub-sub lord, 5th CSL signifies 7+11) + 7 advanced rules (RuleA: 7th CSL star = planet posited in 5H; RuleB: 5H CSL == 7H CSL = definite love; RuleC: Rahu involvement = inter-caste; RuleD: Venus = 7th CSL signifying 5H = pure romance; RuleE: 11th CSL signifies 5+7 = desire fulfilled; KN1+KN2: NEGATIONS for 6/8/12 dominance and 5th CSL→6H breakup). Total **25 distinct rule firings**. **THE 3 TRUST LAYERS**: (1) **Self-disclosure NLP parser** — `_detect_self_disclosure(question)` parses Hinglish/English/Devanagari for stated marriage type ("love marriage hua tha", "ghar walon ne karwayi", "had a love marriage" etc.) + tense (past/present/future) + self-reference; sets mode = EXPLAIN/VERIFY/PREDICT/GENERAL. **Critical guarantee**: when user has STATED their marriage type, engine NEVER contradicts — it switches to EXPLAIN mode and pulls supporting reasons but yields the user-stated verdict (devotee trust preservation). (2) **Multi-engine consensus check** — `_consensus_check([d1, d9, kp])` votes across 3 engines: ALL_AGREE → boost 1.2; MAJORITY (2/3) → boost 1.0; SPLIT (love+arr disagree) → boost 0.7 (penalize uncertainty); returns one of 6 consensus labels (STRONG_LOVE/LIKELY_LOVE/MIXED/LIKELY_ARRANGED/STRONG_ARRANGED/UNCERTAIN). (3) **5-band confidence calibration** — `_confidence_band(score, side)`: STRONG (≥90 = "spasht/clear/definite"), HIGH (≥75 = "strong tendency"), MODERATE (≥60 = "lean but not absolute"), WEAK (<60 with clear direction = "mild lean, can't rule out other"), MIXED (engines split = "love-cum-arranged honest verdict"). Each band has tone_words + verdict_text in Hinglish. **WEIGHTING**: D1=30%, D9=25%, KP=45% (KP-weighted because devotees value KP precision); ±consensus boost applied to dominant side. **Public API contract preserved**: `classify_marriage_type(kundli, intel, kp, birth, question="")` returns rich dict (type/confidence/band/verdict_text/love_score/arr_score/engines per-engine breakdown/consensus/self_disclosure/reasons_love list/reasons_arr list/mode/tone_words). Returns `{}` on missing-data inputs so `marriage_engine/__init__.py` orchestrator falls back to LLM-only mode cleanly. **3x SMOKE TEST PASS**: (1) empty inputs → `{}` clean fallback; (2) 8 NLP cases (Hinglish "love marriage hua tha", "ghar walon ne karwayi thi", English "I had a love marriage", Devanagari "प्रेम विवाह", future "kab shaadi hogi", general educational Q) all correctly classified into EXPLAIN/PREDICT/GENERAL modes with stated_type detection; (3) full pipeline on synthetic LOVE chart (Venus+Rahu in 7H + 7th CSL=Venus signifying 5H + 5L+7L same D9 sign) → CASE A predict mode = LIKELY_LOVE consensus + MODERATE band conf=61 + 10 love reasons across all 3 engines, CASE B "mera love marriage hua tha" → EXPLAIN mode conf=85 ("Bilkul, aapki kundli LOVE marriage ke signs deti hai"), CASE C user said ARRANGED but chart says LOVE → engine CORRECTLY honors user-stated ARRANGED with EXPLAIN-mode verdict (Trust Layer 1 working as designed — never contradict the devotee). API server clean boot on :8080 with zero import errors. Files: WROTE `artifacts/api-server/marriage_engine/love_or_arrange.py` (was 58-line stub, now 890 lines). NO changes to `openai_helper.py` (orchestrator pattern from 2.8.23 ensures backward compat). **ARCHITECT REVIEW 4 PATCHES applied same session**: (R1) `assess_marriage()` orchestrator now accepts + forwards `question` parameter so Trust Layer 1 wires through to production — `openai_helper.py` L3803 callsite updated to pass `question=question or ""`; was a SILENT BUG where EXPLAIN mode could never trigger in real Ask flow. (R2) Consensus enforcement: when `_consensus_check` returns MIXED/UNCERTAIN OR fewer than 2 engines available → `dominant` is FORCED to MIXED regardless of weighted score gap; previously a split vote could still produce LOVE/ARRANGED if weighted scores differed by 12+, hiding disagreement from devotee. (R3) KP CSL semantics fix — added `_csl_chain(kp, csl_planet)` helper that correctly looks up the CSL planet's OWN star/sub-sub from the planet table (not the cusp's nl/ss); rules K2/K3 now use proper KP theory; was overstating KP confidence. (R4) Confidence floors removed — WEAK band confidence floor of 35 and MIXED band floor of 40 deleted; `confidence` now reflects raw weighted score honestly so weak signals report low (e.g. 13) instead of inflating to 35+. CASE D smoke verified: only-D1 chart with no KP → forced MIXED conf=13 (correct: only 1-2 engines available = honest uncertainty). NEXT: Phase 2.8.26 will add 15 more rules + 3 more trust layers (multi-marriage detection, sannyasi yoga, transit Jupiter activation, era adjustment, sub-sub-sub lord depth, Saptamansha D7 cross-check).
- **Phase 2.8.24 — DELETE `love_engine.py` (3454 LINES) → REPLACE WITH LEAN STUB** (1 May 2026): Per user direction *"Delete love_engine.py delete it now first"* — same lean-rebuild pattern as Phase 2.8.21 (where 1480-line `marriage_engine.py` was deleted and replaced with an 80-line stub). The original `love_engine.py` (3454 lines) was permanently deleted. Reason: same bloat pattern — Vargottam scoring, Argala chains, KP per-planet trees, spouse-description blocks, Saturn-transit math etc. were being computed deterministically when the LLM (with full locked-facts whole-kundli context) was already producing higher-quality love verdicts. Replaced with a lean ~85-line `love_engine.py` stub that preserves the EXACT 5 public symbols `openai_helper.py` imports (`assess_love`, `format_verdict_for_prompt`, `extract_window_str`, `classify_love_question`, `LOVE_TONE_RULES`) so **zero `openai_helper.py` changes are needed**. All callable stubs return empty/neutral defaults: `assess_love(...)` returns `{}` so the `if love_verdict_obj:` check at L3975 short-circuits and the LLM whole-kundli flow handles love questions; `format_verdict_for_prompt(...)` and `extract_window_str(...)` return `""`; `classify_love_question(...)` returns `"general"`. **Brand-safety preserved**: `LOVE_TONE_RULES` kept as a meaningful 6-item tuple (no breakup-encouragement, no toxic-trait labels, no blame, empathy-first, no certainty-promises on love-marriage timing, etc.) so the L14224 fallback path AND this engine import yield the same minimum tone floor — devotee never gets a brand-unsafe love response. **Verified 3x smoke test**: (1) all 5 public symbols importable from new stub; (2) function return-types match what `openai_helper.py` checks (`{}`, `''`, `'general'`, len-6 tuple); (3) `openai_helper.py` L1726 `_love_engine()` lazy-loader pattern unpacks cleanly into 4 functions. API server clean boot on :8080 with zero import errors. Files: DELETED `artifacts/api-server/love_engine.py` (3454 lines), CREATED new `artifacts/api-server/love_engine.py` (~85 lines stub). Old 3454-line file recoverable via `git show dd08d19:artifacts/api-server/love_engine.py` if ever needed for reference.
- **Phase 2.8.23 — CONVERT `marriage_engine.py` → `marriage_engine/` PACKAGE WITH 2 SUB-ENGINES** (1 May 2026): Per user direction: *"marriage engine ke andar aur 2 engine rakho ya folder rakho — ek marriage timing, ek love or arrange marriage"*. Single-file `marriage_engine.py` (lean stub from 2.8.22) was deleted and replaced with a proper Python **package** at `artifacts/api-server/marriage_engine/` containing 3 files: (1) **`__init__.py`** — top-level orchestrator; defines `assess_marriage()` which calls both sub-engines and merges results; re-exports the 4 public functions (`assess_marriage`, `format_verdict_for_prompt`, `extract_window_str`, `extract_alt_window_str`) via `__all__` so backward-compat with `openai_helper.py` imports stays intact; (2) **`marriage_timing.py`** — TIMING sub-engine with public `compute_timing_window(kundli, intel, kp, birth)` stub; will host Dasha ∩ Jupiter transit window math (the "kab" answer); (3) **`love_or_arrange.py`** — CLASSIFIER sub-engine with public `classify_marriage_type(kundli, intel, kp, birth)` stub; will score LOVE indicators (Venus-Rahu/Mars conj, 5H-7H exchange, Moon-Venus link, Rahu in 7H) vs ARRANGED indicators (Jupiter aspect on 7H, 9L-7H link, Saturn aspect on 7H, 7L own/exalted) and return type/scores/confidence/reasons (the "kaise" answer). All three currently return `{}` stubs — user will populate incrementally. Architecture note: `assess_marriage()` orchestrator merges timing + classification into a single verdict dict; if both empty returns `{}` (LLM falls back to whole-kundli mode); if either populated, builds verdict with `next_window` from timing + `marriage_type` key from classifier. Sub-engines also importable directly via `from marriage_engine.marriage_timing import compute_timing_window` etc. for testing/standalone use. **No changes to `openai_helper.py`** — Python's package resolution makes `from marriage_engine import assess_marriage` route to `marriage_engine/__init__.py` automatically. **Verified**: API server clean restart on :8080; Python smoke test confirms (a) all 4 top-level public functions importable from `marriage_engine` package, (b) both sub-engine functions importable directly, (c) package metadata correct (`__path__`, `__all__`). Files: NEW `artifacts/api-server/marriage_engine/__init__.py`, NEW `artifacts/api-server/marriage_engine/marriage_timing.py`, NEW `artifacts/api-server/marriage_engine/love_or_arrange.py`; DELETED single-file `artifacts/api-server/marriage_engine.py`.
- **Phase 2.8.22 — RENAME BACK `marriage_timing.py` → `marriage_engine.py`** (1 May 2026): Per user direction immediately after 2.8.21: *"marriage timing ko rename karo marriage engine"*. The fresh lean scaffold file (created in 2.8.21 with safe-default stubs for the 4 public functions) was renamed back to its original filename `marriage_engine.py` so callers in `openai_helper.py` use the canonical name. Executed via `mv marriage_timing.py marriage_engine.py` + single `sed` pass to revert all 4 import statements (`from marriage_timing import` → `from marriage_engine import` at L48, L3844, L4294, L13291). File header docstring updated to reflect rename history (Phase 2.8.21 wipe + Phase 2.8.22 rename). **Net effect**: filename is back to `marriage_engine.py` BUT contents remain the lean ~80-line stub from 2.8.21 (NOT the original 1480-line bloated engine). Behavior identical to 2.8.21 — marriage Qs answered by LLM-only mode using whole-kundli context. **Verified**: API server clean restart on :8080; Python smoke test confirms all 4 stubs importable from `marriage_engine` and return correct types ({} dict / "" string). User will incrementally fill in the 3 cores (TIMING/VERDICT/REMEDY) into this lean `marriage_engine.py` file going forward.
- **Phase 2.8.21 — MARRIAGE ENGINE PERMANENT REMOVAL + RENAME TO `marriage_timing.py`** (1 May 2026): Per user direction (after architectural review revealed engine bloat — 22 sections / 6 focus-block layers / ~1070 tokens per verdict / 1480 lines of code), user decided **"keep marriage engine, just rename it marriage_timing, and remove 1480 lines — me new se shuru karunga"**. Old `marriage_engine.py` (1480 lines, 67KB — Phases 2.8.18 + earlier marriage logic) was **PERMANENTLY DELETED** from working tree (`rm artifacts/api-server/marriage_engine.py`). Replaced with **fresh lean `marriage_timing.py` scaffold (~80 lines)** that exposes the same 4 public functions (`assess_marriage`, `format_verdict_for_prompt`, `extract_window_str`, `extract_alt_window_str`) as **safe-default stubs** — `assess_marriage` returns `{}`, all formatters return `""`. This preserves the API contract `openai_helper.py` depends on, so server boots cleanly with **NO ImportError**. Updated all 4 `from marriage_engine import` statements in `openai_helper.py` (L48, L3844, L4294, L13291) to `from marriage_timing import` via single `sed` pass — verified, all routes intact. **Behavioral change**: while stubs are in place, marriage questions are answered by **LLM-only mode using whole kundli context** (no engine-locked dates / scores / remedies). LLM may produce vaguer dates and slightly inconsistent scores until user populates `marriage_timing.py` with new lean implementation. **Architectural rationale (user's insight)**: engine had bloated to handle 15+ Q types, but only **3 are truly irreducible** — (1) TIMING window math (Dasha ∩ Jupiter intersection — LLM hallucinates dates), (2) VERDICT/SCORE deterministic compute (LLM gives random scores), (3) REMEDY planet selection (LLM picks random planet). All other layers (Manglik intensity, Vargottam, Argala, SAV, KP per-planet scan, spouse description, Saturn transit blocking) can be handled by GPT-5.4 reading whole-kundli context already in the prompt. **User will incrementally add new lean implementation** focused on these 3 cores only. **Verified**: Python smoke test confirms all 4 stubs importable + return correct types; API server clean restart on :8080; mobile expo unaffected. **Git history preserves** the deleted 1480-line file (recoverable via `git show HEAD~1:artifacts/api-server/marriage_engine.py` if needed). Files: NEW `artifacts/api-server/marriage_timing.py`; DELETED `artifacts/api-server/marriage_engine.py`; updated `artifacts/api-server/openai_helper.py` (4 import lines).
- **Phase 2.8.20 — FUTURE DASHA WINDOW TRIM (12y → 7y)** (1 May 2026): Per user request (cost-optimization pass), `_section_future_dasha()` in `kundli_full_context.py` (L606+) ko narrow kiya 12-year horizon se 7-year horizon pe. New `_CUTOFF_YEARS = 7` constant date-based filter karta hai: antardashas aur next-mahadashas jinka `startDate > today + 7y` ho, prompt me skip ho jate hain. Section header bhi update — `"Vimshottari, future-only"` se `"Vimshottari, next 7 years"` (so LLM knows the boundary). Edge case: leap-day ke wajah se `replace(year=year+7)` agar fail kare, fallback `cutoff_iso = "9999-12-31"` use karke un-trimmed sequence dikha deta hai (graceful degradation, no crash). **Token impact (real chart test)**: long Moon MD ke saath 760 chars → 304 chars (~190 → ~76 tokens), **60% reduction** in Section 5; per-question average ~100-250 token saving depending on chart's current MD position. **Sufficiency rationale**: 7-year horizon covers ~95% of practical timing questions (marriage, career, health, finance) — 12-year window had diminishing returns aur LLM context budget bachata hai. **Underlying `dashas` tree untouched** — sirf prompt rendering filter hua, engines ko abhi bhi full 27-MD Vimshottari tree access hai. **Re-tune**: `_CUTOFF_YEARS` ki value badal ke window resize kar sakte ho (e.g. 5y for tighter, 10y for looser). **Verified**: API server clean restart on :8080; mock 27-MD chart test confirms cutoff working correctly (only ADs/MDs starting before 2033-04-30 included). Files: `kundli_full_context.py`.
- **Phase 2.8.19 — DISABLE WHOLE-KUNDLI SECTIONS 10-13 IN LLM PROMPT** (1 May 2026): Per user request, sections 10 (ARUDHA PADAS), 11 (ASHTAKAVARGA), 12 (SHADBALA), 13 (ARGALA/VIRODHARGALA) are **no longer sent to the LLM** in the whole-kundli context block built by `kundli_full_context.build_full_chart_context()`. Implementation: ADD-ONLY — function definitions (`_section_arudha` L1223, `_section_ashtakavarga` L1301, `_section_shadbala` L1362, `_section_argala` L1458) **untouched**; only the 4 call-site `try` blocks inside `build_full_chart_context()` are wrapped in a feature flag `_SEND_SECTIONS_10_TO_13 = False`. Re-enable by flipping flag to `True` and restarting api-server. **Archive folder created** at `artifacts/api-server/disabled_sections/` containing (a) `sections_10_to_13.py` — verbatim copy of the 4 disabled section functions for reference; (b) `README.md` — explains what's disabled, why, and how to re-enable. **Important:** the underlying engines (`ashtakavarga.py`, `argala.py`, jaimini code) are **still actively used** by topic-specific engines like `marriage_engine.py` (Phase 2.8.18 focus block computes SAV + Argala internally and injects them via the AUTHORITATIVE MARRIAGE VERDICT block). Only the **generic whole-kundli prompt sections** are gated off — locked numbers still reach the LLM via topic-specific verdict blocks. **Verified**: build_full_chart_context output no longer contains "ARUDHA"/"ASHTAKAVARGA"/"SHADBALA"/"ARGALA" headers; api-server restarted clean on :8080. Files: `kundli_full_context.py`, new `disabled_sections/README.md`, new `disabled_sections/sections_10_to_13.py`.
- **Phase 2.8.18 — MARRIAGE FOCUS-BLOCK LAYERS** (1 May 2026): Added 7 deterministic layers to `marriage_engine.py` (~1478 lines now) so the LLM narrates from locked numbers and cannot hallucinate marriage predictions. **6 new helpers** (~L289-584): (1) `_maybe_sav_marriage` — Sarvashtakavarga bindus on H7/H2/H11/H8/H12 via `ashtakavarga.compute_ashtakavarga`; (2) `_maybe_vargottam_marriage` — D1=D9 vargottam check for Venus/Jupiter/Mars/7L via `divisional_charts.compute_vargottama_matrix` (tiers: truly-powerful/vargottam/supported/weak); (3) `_maybe_argala_marriage` — Argala/Virodhargala on H7 via `argala.compute_argala` with paap-cancellation; (4) `_maybe_saturn_transit_h7` — current Saturn sidereal position via swisseph SIDM_LAHIRI checking 3rd/7th/10th aspects to natal H7; (5) `_per_planet_kp_scan` — KP YES/NO/MIXED/NEUTRAL per planet weighted (7L=2.0x, Venus/Jup=1.5x, others=1.0x) → STRONG-YES/TILTED-YES/BALANCED/TILTED-NO/STRONG-NO; (6) `_spouse_description` + `_SIGN_NATURE` lookup — synthesises 7H sign-nature + 7L placement + A7 (Darapada) + Venus/Jupiter signs + D9 7H for narration. **Wiring** in `assess_marriage()` between STEP 4 KP and STEP 5 SCORING; score adjustments BEFORE clamp: SAV(H7) >=28 +5 / <=18 -8, vargottam +3 each capped +6, weak vargas -2 each, argala STRONG-BENEFIC +4 / MILD-BENEFIC +2 / MIXED -3 / MILD-MALEFIC -3 / STRONG-MALEFIC -5, Saturn transit blocking -4, KP per-planet STRONG-YES +6 / TILTED-YES +2 / TILTED-NO -3 / STRONG-NO -8; confidence +5 if all 4 focus helpers ran clean. **All 6 helpers safe-fail** with `{}` or `{"_error": str}` so single-helper crash never breaks `assess_marriage`. **Return dict extended** with 6 new keys (`sav_marriage`, `vargottam_marriage`, `argala_h7`, `saturn_transit_h7`, `kp_per_planet`, `spouse_description`) — backward-compat with `openai_helper.py:3781-3905` consumer preserved. **New formatter** `_focus_block_lines()` renders all 6 layers as locked Hinglish-labelled numbers between `_jup_line` and `nw_line` in `format_verdict_for_prompt`. **`_MONTHS` constant restored** at L1228 (was lost in earlier focus-block rewrite, causing silent `_ym_to_human` degradation). **E2E test on 1995-08-15 12:00 Mumbai female chart**: all 6 layers populate, 3221-char focus block renders, score adjustments apply correctly, no exceptions. **Architect 2-pass review** (1st found 4 issues — score formulas off-spec, missing _MONTHS, ungated confidence bonus, partial safe-fail; all fixed in 2nd pass and verified PASS). Files: `marriage_engine.py`. Existing wiring in `openai_helper.py:3781-3905` automatically picks up new layers via `format_verdict_for_prompt`.

- **Phase 2.8.7 — 2-MODE OUTPUT SYSTEM** (1 May 2026): Final consolidation of brevity work (replaces earlier 6-type classifier from 2.8.5/2.8.6). User-provided framework: **CORE RULE = "By default = sirf RESULT. EXPLAIN only if user asks."** Implemented in `openai_helper.py` as binary mode-switch driven by `_is_transparency_query(question)`. **MODE 1 (Quick Answer, default)**: 1-line direct answer + EXACTLY 3 bullets ≤15 words each + optional CTA "Agar chaho to main iska reason step-by-step samjha sakta hoon"; chart-tech jargon (planet names / house numbers / dasha names / drishti / navamsha / D9 / karaka) STRICT BAN; "TL;DR:"/"Verdict:"/"Summary:" prefix labels BANNED. Two exemptions: casual chit-chat → 1 line ("Aashirvaad."), single-fact lookup → 1 line ("Aapka Lagna Mithun hai."). **MODE 2 (Explain Mode, triggered)**: switches when question contains `kaise/kyun/kyon/kyu/explain/reason/detail mein/vistaar/step by step/samjhao/samjha do/how do you know/why`; output is the literal "Step 1: <check> (<chart factor>) → <plain meaning> ／ Step 2 ／ Step 3 ／ Final: <one-line conclusion>" template — chart-tech ALLOWED but each step MUST pair with plain Hinglish meaning. **Trigger detector** (`_TRANSPARENCY_TRIGGERS` + `_is_transparency_query`, L433-484): uses `kyun ` / `kyon ` / `kyu ` (trailing-space form) to catch mid-sentence "kyun" without false-matching "kyunki" (= because, a connector); bare-Q edge case (`kyun?`, `kyu?`, `why?`) handled separately. **Three layers of jargon defense for Mode 1**: (a) Rule 19 prompt-side ban listing every planet/house/dasha term, (b) topic-lock final reminder restating the 2-mode flow, (c) code-side `_strip_verdict_label_prefixes()` regex scrubber wired into both sync (`ai_ask` ~L14358) and stream (`ai_ask_stream` ~L17683) callsites that strips any "TL;DR:" / "Verdict:" / "Summary:" / "Answer:" / "Conclusion:" / "Saaransh:" / "Nishkarsh:" prefix even if the model emits one. **Test results**: trigger-detector unit test 18/18 PASS (11 Mode-2 triggers + 7 Mode-1 non-triggers including "kyunki"/"kab"/"kaisa" false-positive guards); end-to-end live API 7/7 PASS (3 Mode-1 + 4 Mode-2). Mode-2 outputs read like ChatGPT+Astrologer mix: "Step 1: Pehle main pyaar ka base dekhta hoon (5H Tula, 5L Shukra 11H Mesh) → attraction strong hai, par stability se zyada excitement chalti hai" — devotee feels real-guru explanation. Sampling unchanged: temp=0.5, top_p=0.9, max_tokens=450 Mode-1 / 600 Mode-2.

## External Dependencies

- **Database**: SQLite for user authentication; PostgreSQL with `pgvector` for the knowledge base.
- **Astrology Engine**: `pyswisseph` (Swiss Ephemeris).
- **AI/NLP**: OpenAI API (GPT models).
- **PDF Generation**: `reportlab`.
- **Mobile Development**: Expo (React Native).
- **Image Processing**: Pillow.
- **Ask Pipeline V2**: AI Ear for multi-intent extraction, `ai_ask_v2` dispatcher for fan-out, and AI Mouth for conversational narration.