# Cosmic Lens — Workspace

## Overview

Cosmic Lens is a pnpm workspace monorepo using TypeScript for a mobile Vedic Astrology application. It aims to provide accurate astrological analysis, including kundli calculations, numerology, Vastu, and AI-driven interpretations. The project's vision is to achieve over 97% accuracy through a robust calculation engine, a sophisticated AI, and continuous learning, ultimately creating a comprehensive astrological and numerological life mastery system.

## User Preferences

- The user wants the AI to act as a replit coding agent.
- The user prefers a strict development methodology: `engine.py file (sahi folder mein)`, `Unit test (test_engine.py)`, `locked_facts.py wiring`, `Rule entry in system prompt`, `Deterministic post-injector`, `3x smoke test on real chart (/tmp/k.json, /tmp/req.json, /tmp/qm.json)`, `Architect review (code_review skill)`, `replit.md entry` are mandatory for every new engine.
- The user has provided a clear roadmap for project phases and expects strict adherence to it. Deviations require explicit user confirmation.
- The user expects the AI to avoid hallucinating, especially regarding timing-related questions.
- The user wants the AI to integrate classical Vedic knowledge through RAG for opinion-based questions, but strictly NOT for timing questions.
- The user has defined a brand voice: "Powered by Advanced Cosmic Intelligence" and explicitly states "AI never named; AI = language layer only; backend = facts; post-injectors = mandatory citation safety net."
- The user prefers deterministic post-injectors to ensure mandatory facts are cited even if the AI misclassifies a topic.
- The user prefers that the AI refine language for opinion questions but must cite engine facts.
- The user wants medical outputs to include a mandatory medical disclaimer banner and a 3-confirmation rule for severity tiers.
- The user wants financial outputs to include a mandatory financial disclaimer banner and a tiered risk profile.
- The user wants remedies outputs to include a mandatory disclaimer ("Remedies SUPPLEMENT, never substitute action") and provide both free and paid alternatives.
- The user wants Astrocartography outputs to include a mandatory disclaimer ("energetic affinity, not guaranteed luck").
- The user expects ethical and safety disclaimers for sensitive topics like medical, lifespan, death prediction, and destiny vs. guidance.
- The user prefers a "Numerology PDF Pro" branding for the report, with a hybrid numerology + Vedic Astrology content, framed with numerology language.

## System Architecture

The project is a pnpm workspace monorepo using Node.js 24 and TypeScript 5.9.

**Mobile App (Expo React Native):**
- **UI/UX**: Full-screen nebula background, card glow effects, glassmorphism, zodiac-based accent theming, and dark/light mode with bottom tab navigation.
- **Features**: Daily Rashifal, Panchang, Kundli Milan, Muhurat finder, Numerology, Remedies, Vastu tips, Dosha analysis, personalized forecasts ("Risk Radar"), and a Life Mastery Report.
- **Localization**: Supports 25 languages with region detection and a two-layer architecture for UI and Vedic content.

**Backend (Python Flask):**
- **Architecture**: Modular engines for Vedic astrological calculations (e.g., `planet_strength.py`, `ashtakavarga.py`).
- **CLE (Cosmic Lens Engine) Format**: Every new domain engine must include D9 Navamsa cross-check, a domain-specific D-chart, KP cuspal sub-lord, Jaimini karaka mapping, a 3-bucket tense detector, and brand-safety guards.
- **LOCKED FACTS Protocol**: `locked_facts.py` centralizes deterministic data for AI responses.
- **AI Orchestration**: `openai_helper.py` injects facts, enforces verbatim citation, and includes deterministic post-injectors for safety.
- **RAG (Retrieval-Augmented Generation)**: Uses `pgvector` for classical Vedic knowledge, bypassed for timing questions.
- **Engine-Aware Anti-Hallucination Pipeline**: Ensures timing answers are correctly sourced from engines, with a smart timing-validator that softens when engine data is unavailable.
- **Supertype Narrator Contract**: Classifies answers into 5 supertypes (PLANET_QUERY, PROBLEM_QUERY, TIMING_QUERY, DECISION_QUERY, GENERAL_ANALYSIS) with strict per-type response rules.
- **Question-Intent Classifier**: A deterministic function (`_classify_ask_intent`) identifies 13 intent categories, providing a canonical source of truth for routing.
- **AI-ONLY Question Understanding**: Replaced multi-layer understanding with a single LLM classifier call for intent, topic, and confidence.
- **Emotional Treatment Playbook**: A cross-engine module defining a `(emotional_tone × domain) → directive` matrix for nuanced AI responses.
- **Structured-Output Mode**: For specific engines (e.g., wealth), forces strict JSON output with defined schemas, disclaimers, and an "EMPATHY SANDWICH" narrative pattern.
- **Health Engine**: Deterministic Vedic + KP health/illness engine with 25 layers, 12 question buckets, and critical brand-safety guards including medical disclaimers and helpline information.
- **Wealth / Finance Engine**: Deterministic Vedic + KP wealth/finance engine with 25 layers, 12 question buckets, and critical brand-safety guards including financial disclaimers and advisor recommendations.
- **Stock Market Engine**: Deterministic Vedic-rule-based stock/trading/investment guidance with 17 core natal layers and a 10-category question classifier, enforcing anti-speculation policies.
- **Love vs. Arrange Engine**: Pure-Python deterministic scorer using classical Vedic rules for love vs. arrange marriage verdicts, with confidence-ratio-based public verdicts.
- **Yoga Registry**: Wires existing yoga detectors (Dhana, Raj, Vipareeta, etc.) into the minimal-prompt path, providing structured yoga facts.
- **Dosh Engine**: Wires `dosh_engine.analyze_doshas()` into the minimal-prompt path, presenting structured `DOSH_FACTS`.
- **Health Pipeline Orchestration**: Uses `health_topic_matcher.py`, `health_recipe_composer.py`, and `health_rules.py` to derive structured `phase76_findings` for health questions based on 132 topics and 34 rules.
- **LLM Full Chart Mode** (`LLM_FULL_CHART_MODE`, default ON since 30 Apr 2026): Env-gated passthrough that hands the LLM a comprehensive Hinglish chart-context block built by `kundli_full_context.build_full_chart_context`. Sections: 1.Birth/Lagna, 2.Grahas (with dignities), 3.Bhavas, 4.Current Dasha, 5.Upcoming Vimshottari (remaining antardashas + next 5 mahadashas), 6.Navamsha D9 (with vargottama detection), 7.Yogas/Doshas/Sade-Sati/Gochar (sourced from `chart_intelligence.analyze_chart`), 8.Niyam (anti-hallucination + Hinglish rails). The passthrough call lives in `openai_helper.ai_ask` and uses Replit AI Integrations' OpenAI proxy (`AI_INTEGRATIONS_OPENAI_*` env vars) with model from `OPENAI_MODEL` (default `gpt-5.4`). For gpt-5 / o-series models the `temperature` parameter is conditionally dropped (those models reject it).
- **Phase 2.4 Tier-1.5 Topic-Lock Focus** (01 May 2026): Quality-test follow-up to Phase 2.3 surfaced a **question→house mapping accuracy gap**: when devotee asks topic-specific questions, the LLM was citing adjacent / classically-unrelated houses (test 5 topics → 2/5 pass; friend-circle question pulled 9H, younger-sibling pulled 11H, children pulled 7H). Goal: **strict bhava focus + dasha-anchored timing** without rewriting the rule engine. Two-layer fix:
  - **Prompt-level expansion** (`openai_helper.py` `_PT_SYS_INTRO`, expanded from 15 → 17 rules, header comment updated): Rule 6 EXPANDED from a 5-topic cheat-sheet (Health/Career/Marriage/Wealth/General) to a **17-topic BPHS-aligned classical bhava+karaka mapping** (added: friend circle → 11H+Guru; elder sibling → 11H+Mangal; younger sibling → 3H+Mangal; children → 5H+Guru putra-karaka; mother → 4H+Chandra matr-karaka; father → 9H+Surya pitr-karaka; home/property → 4H+Mangal/Shukra; education → 4H+5H+Budh+Guru; higher studies → 9H+5H+Guru gyaan-karaka; court/mukadma → 6H+8H+Mangal+Shani; foreign travel → 12H+9H+Rahu videsh-karaka; spirituality → 9H+12H+5H+Guru+Ketu moksha-karaka). Added Rule 16 STRICT FOCUS (if a TOPIC-LOCK block appears at the top of the user message — see helper layer below — it OVERRIDES Rule 6: cite ONLY the listed houses/lords/karakas, NEVER cite anything in the "DO NOT cite" list, even if it seems classically interesting; clarity over breadth). Added Rule 17 DASHA-FIRST TIMING (every detailed answer must explicitly link the current Mahadasha–Antardasha lord to the topic-house or topic-lord — `"Aap [MD]–[AD] mein chal rahe ho. [MD] aapka [N]L hai, isliye agle [period] tak [topic-area] mein [effect] hoga"` — single-fact answers per Rule 1 are exempt; if dasha is unknown, skip the dasha-link entirely). System-prompt is now ~13.8 KB (was 7709 chars after Phase 2.3 → ~13856 after the Rule 6 expansion + Rules 16/17).
  - **Topic-detector + TOPIC-LOCK injection** (`openai_helper.py` Phase 2.4 block, ~290 lines after `_PT_SYS_INTRO` close): Five new module-level constants/functions. (a) `_TOPIC_RULES` — ordered list of 17 topic dicts with `topic_id`, human `label`, compiled regex `pattern` (Hindi/English/Hinglish keywords incl. `bade bhai`, `chhote bhai`, `bachhe`, `dost`, `mukadma`, `videsh`, `padhai`, `higher stud(y|ies)` etc.), `houses` (e.g. `[11]` for friends, `[3]` for younger sibling, `[12, 9]` for foreign travel), `karakas` (Sanskrit-named graha karakas with role hint), and `banned` (houses classically unrelated to that topic); rule order matters — `higher_studies` is placed BEFORE `education_basic` because the generic "study/studies" pattern would otherwise eat questions like "higher studies videsh mein possible?". (b) `_detect_topic(question)` — first-match scan over `_TOPIC_RULES`, returns the rule dict or None (None → topic uncertain → no lock injected, LLM falls back to expanded Rule 6 alone). (c) `_topic_lagna_sign_idx(kundli)` — extracts lagna sign 0-11 from `kundli.ascendant`/`kundli.lagna`. (d) `_topic_house_lord(kundli, house_num)` — whole-sign lord (BPHS Ch.5) for that house given user's lagna. (e) `_topic_current_dasha(kundli)` — current MD-AD planet pair from `kundli.dashas` Vimshottari tree (mirror of `kundli_full_context._section_future_dasha` lookup but returns just the lord names). (f) `_build_topic_lock(rule, kundli)` — composes the user-facing TOPIC-LOCK block in Hinglish with `━━━ TOPIC-LOCK ━━━` ASCII boundary, substituted house lords (e.g. for Dhanu lagna friend question: `"Cite SIRF: 11H + 11L (Shukra)"`), karaka graha line, current-dasha line with mandatory "in elements ka topic-house/lord se rishta MANDATORILY explain karein" instruction, explicit "DO NOT cite" line listing the banned houses by number, plus a Rule 16/17 reminder. Returns `""` on any failure (graceful — chart-context still ships, only the lock is skipped). Wired at all 3 passthrough emit sites (`_build_messages` ~L2755, sync `ai_ask` ~L13760, stream `ai_ask_stream` ~L16947) — the lock is **PREPENDED to the user message** (not the system prompt) so the system prompt stays cacheable across requests while the topic instruction still gets primary attention right before the question. Each wire site emits `topic_lock` + `lock_chars` in the `PASSTHROUGH.MESSAGES_BUILT` trace event for production telemetry.
  - **Architect-driven hardening (3 follow-up fixes, post-review)**: A code-review pass surfaced three issues that were patched in the same phase before sign-off. (i) **HIGH — multi-intent ambiguity**: `_detect_topic` originally returned the first matching rule, so `"shaadi aur bachhe kab honge?"` would single-route to `marriage` and Rule 16 would then forbid the 5H (children). Now `_detect_topic` collects ALL matches and (a) returns None if >1 distinct `topic_id` matched, or (b) does a secondary anchor-scan when a conjunction (`aur`, `and`, `&`, `plus`, `saath saath`, `tatha`, `evam`) is present and another strong topic noun appears, returning None in that case too. The intent: a half-right strict-focus lock is worse than no lock for mixed asks; deferring to Rule 6 keeps the answer broad. Verified live — `shaadi aur bachhe` now logs `topic_lock: null, lock_chars: 0`. (ii) **MEDIUM — dasha shape mismatch**: `_topic_current_dasha` only walked `kundli["dashas"]`, but `kundli_full_context._section_dasha` reads `kundli["currentDasha"]` (with `cd.maha` / `cd.antar`) for current-dasha display. The lock therefore often emitted `"(unknown — skip dasha-link)"` even when a current dasha was present. Now the function reads `currentDasha.maha`/`currentDasha.antar` first (parity with `kundli_full_context.py:311`), then falls back to the dashas-tree walk for legacy charts. Verified live — `lock_chars` for the same career question went 510 → 584 because the dasha-line is now populated. (iii) **LOW — `?` lord guard**: previously, if lagna was unknown, `_topic_house_lord` returned `"?"` and `_build_topic_lock` still emitted a lock with `"11L (?)"` strings — Rule 16 would then forbid the banned houses without offering a positive substitute. Now `_build_topic_lock` aborts (returns `""`) if `_topic_lagna_sign_idx` is None or any house-lord lookup yields `"?"`. Verified by unit test.
  - **Smoke test (17/17 PASS)**: 17 unit tests for `_detect_topic` cover all topic patterns including the higher_studies/education_basic order-dependent fix — 17/17 pass. End-to-end on the live API with a Dhanu lagna demo kundli: 17/17 leak-detection checks pass. For each topic the test confirms (a) at least one expected topic-house IS cited in the answer (regex matches `\b{N}H\b`, `\b{N}th\b`, `\b{N} bhav(aav|)\b`, `\b{N} ghar\b`) AND (b) NONE of the banned houses leak. Tests covered: elder_sibling cites 11H but not 3/5/7/9; younger_sibling cites 3H but not 5/7/11; siblings_general cites 3+11 but not 5/7; friend_circle cites 11H but not 3/5/7/9; children cites 5H but not 3/7/11; mother cites 4H; father cites 9H; home_property cites 4H; education_basic cites 4+5; higher_studies cites 9+5; court_case cites 6+8 but not 4/5/7/9/11; foreign_travel cites 12+9; spirituality cites 9/12/5; marriage cites 7/5/8; career cites 10/6/11; wealth cites 2/11/5; health cites 1/6/8/12 but not 7/11. Production telemetry confirms lock fires for all 17 topic_ids with `lock_chars` in the 485-576 range.
- **Phase 2.3 Tier-1 Anti-AI-Feel** (30 Apr 2026): User feedback after Phase 2.2 — persona Cosmo set ho gaya, but agar reply mein "As an AI", "consult a professional astrologer", "Hope this helps" type tells leak ho jayein, ya hedging ("shayad", "ho sakta hai") ya generic "tum" address use ho, to user ko turant lag jata hai "yeh AI hi hai". Goal: aisa lage ki ek **highly trained Vedic guru** real-time mein unki kundli padh raha hai. Two-layer fix:
  - **Prompt rules** (`openai_helper.py` `_PT_SYS_INTRO`, expanded from 11 → 15 rules): Added Rule 12 GURU-TONE (devotee ko warm "aap"/"jajman" se address; bade detailed answers mein occasional opening "Pranam" / "Suniye jajman" / "Dekhiye"; robotic tone aur dry bullet-listing forbidden). Added Rule 13 LAGNA-AWARE PERSONALIZATION (har detailed answer mein user ki lagna / rashi / nakshatra ka subtle reference — "Aap Dhanu lagna ke jatak hain, isliye Guru ka role primary hai" — taki devotee feel kare ki Cosmo unhe personally jaanta hai, generic answer nahi de raha; turn-wise alag angle rotate karna chahiye). Added Rule 14 NO-HEDGING (hedge words "shayad", "ho sakta hai", "may be", "might", "perhaps", "kind of", "possibility hai" forbidden; jab evidence clear hai → confident "hoga", "dikhta hai", "prabal hai", "saaf nazar aata hai" use karo; sirf jab actual evidence nahi hai (Rule 11) tab "iska clear sanket abhi nahi mil raha" — yeh weak nahi, scholarly humility hai). Added Rule 15 NO-AI-TELLS (Rule 2 ka defense-in-depth — explicit forbidden phrases list: "As an AI...", "I'm a language model...", "I cannot help with...", "Please consult a professional astrologer/qualified expert/real astrologer/practitioner/doctor/financial advisor/legal expert", "Hope this helps!", "Let me know if you have any questions", "I recommend/suggest/advise consulting/seeing/speaking to a professional"; explicit reminder ki "Tum Cosmo ho — Vedic Jyotishi KHUD ho. Aap hi 'professional astrologer' ho devotee ke liye"). System-prompt ab 7709 chars (5982 → 7709 over Phases 2.2 + 2.3).
  - **Defensive post-processor** `_scrub_ai_tells(text)` (new module-level function in `openai_helper.py` after `_PT_SYS_INTRO`): Belt-and-suspenders safety net. Even with Rules 2 + 15 in the prompt, the LLM may occasionally slip in a tell. The scrub uses (a) a fast-path substring check (~50 common AI-tell triggers like "as an ai", "hope this helps", "consult a professional", "chatgpt", "i recommend") to skip work in the common case, then (b) a slow-path of 9 compiled regex patterns (identity tells, capability disclaimers, "consult a professional X" with both bare + gerund verb forms, sycophantic closures, brand names) that drop any sentence containing a tell from the final reply. Sentence splitter handles English `.!?` plus Devanagari danda `।`. Idempotent + fail-safe — on any error returns input unchanged so the answer pipeline never breaks. If EVERY sentence is a tell (rare worst case), returns persona-neutral fallback "Iss point pe aapki kundli mein clear sanket abhi nahi mil raha — koi aur swaal puchhiye". Wired into all 3 LLM emit points: `ai_ask` sync passthrough return, `ai_ask_stream` final emit, and `ai_ask_stream` partial-final emit (mid-stream OpenAI failure salvage path). Each scrub call emits a `PASSTHROUGH(.SCRUBBED|stream).SCRUBBED` trace event when it actually trims, so production telemetry can track how often the safety net fires (ideally ~0%; rises = prompt rules need tightening).
  - **Smoke test (14/14 PASS)**: 11 unit tests for `_scrub_ai_tells` cover identity tells, sycophant tells, mid-sentence consult-professional, brand-name leaks (ChatGPT/OpenAI/Claude/Gemini/GPT-4), gerund verb forms ("I suggest seeing a qualified jyotishi"), all-tell fallback, and pass-through for benign text — 11/11 pass. End-to-end on the live API: 5/5 identity questions return canned Cosmo line via `shortcut:intro` (zero LLM cost, ~10ms); 6/6 Tier-1 checks on a real detail-grade career answer ("Career mein next 6 months") — confirmed reply contains explicit lagna reference ("Aap Dhanu lagna ke jatak hain, aur abhi Guru mahadasha–Rahu antardasha chal rahi hai"), zero AI/GPT/Cosmic-Lens leaks, zero hedge words (shayad/might/may be/perhaps), zero "consult a professional", zero "Hope this helps / Let me know if", and uses "aap"/"aapki" only (no "tum"); regression intact: "Mera lagna kya he" still 1-line "Aapka Lagna Dhanu hai." and "Mera nakshatra" still "Aapka Janma Nakshatra Ardra hai."; source attribution intact: "Yeh apko kaise pata" → "Aapki kundli mere paas hai, isliye seedha mujhse pata chalta hai." with no Section/JSON/raw-degree leak.
- **Phase 2.2 Persona + Source Attribution** (30 Apr 2026): User feedback that the assistant was answering "Yeh apko kaise pata?" by exposing internal data ("Section 1, Section 3, Lagna: Dhanu 268.92°") instead of naturally saying "Aapki kundli mere paas hai". Also that any identity question must use the **Cosmo** brand voice — never the words "AI", "GPT", "bot", "language model", etc. Two-part fix:
  - **Prompt rules** (`openai_helper.py` `_PT_SYS_INTRO`, L77): Added Rule 2 PERSONA — IDENTITY (name = "Cosmo", a "highly trained advance cosmic intelligence", with an explicit denylist of forbidden words: AI / A.I. / artificial intelligence / ChatGPT / GPT / language model / LLM / OpenAI / Anthropic / Google / bot / chatbot / machine / program / software / model / computer). Added Rule 3 SOURCE ATTRIBUTION — when devotee asks "yeh aapko kaise pata / source kya hai / kaise dekha / proof kya hai", reply naturally with "Aapki kundli mere paas hai" / "Aapke janma-vivran mere saamne hain", NEVER expose "Section N", raw degrees (`268.92°`), JSON, "data field", "context block", "system prompt", file/database names. Renumbered Rules 4–11 (and fixed a Phase 2.1 numbering bug where two rules were both labelled "4" — FOCUS + HISTORY). Two new rules push the system-prompt size from 5982 → 8623 chars; still well under any model limit. Updated module-header comment to document the 11-rule structure and the Phase 2.2 additions.
  - **Shortcut layer** (`shortcuts.py`): The `_INTRO` canned reply (used by the `shortcut:intro` fast-path) previously read "I'm Cosmic Lens — a Vedic astrology AI" — i.e. it leaked the forbidden word "AI" before the LLM ever saw the question. Rewrote all three locale variants (en/hi/hn) to use the new persona — the Hinglish text now reads verbatim: *"Main ek highly trained advance cosmic intelligence hun. Mera naam Cosmo hai. Aapki kundli mere paas hai — career, shaadi, health, paisa, current dasha — kuch bhi puchhiye, aapke chart ke aadhar pe jawab dunga."* Also expanded `_INTRO_RX` to catch additional phrasings: `kya tum (AI|bot|chatbot|robot) ho`, `(tum|aap|tu) (ai|bot|...) ho`, `are you (an? )?(ai|bot|chatbot|robot|human|real)`, `(tumhara|aapka|tera) naam kya hai`, `what(s|'s|\s+is) your name`, `your name kya hai`, plus romanised `kon` alongside `kaun`. Now any identity question short-circuits at the route layer (no LLM call, no quota slot, ~10 ms) with the exact Cosmo line.
  - **Smoke test (10/10 PASS)**: 8 identity variants ("Tum kaun ho", "aap kaun ho", "kya tum AI ho", "tumhara naam kya hai", "Aapka naam kya hai", "are you a bot", "who are you", "what is your name") all return the canned Cosmo reply via `source=shortcut:intro` with NO leakage of AI/GPT/ChatGPT/bot/Cosmic Lens. Source-attribution test: "Yeh apko kaise pata" (after a lagna setup turn) → "Aapki kundli mere paas hai, isliye seedha mujhse pata chalta hai." with NO Section/JSON/raw-degree leak. Regression: "Mera lagna kya he" still returns the Phase 2.1 one-liner "Aapka Lagna Dhanu hai."
- **Phase 2.1 Length-Match Fix** (30 Apr 2026): User feedback that single-fact questions like "Mera lagna kya he" were getting full TL;DR + 4-bullet treatment when they only deserve a 1-line answer ("Aapka Lagna Dhanu hai."). Added a NEW Rule 1 ("LENGTH MATCH KARO") to `_PT_SYS_INTRO` (`openai_helper.py` L80) at the TOP of the rules list, with explicit examples: single-fact factual queries (lagna / nakshatra / rashi / current dasha / shubh time / lagna lord / yog) → 1 line ONLY, no TL;DR prefix, no bullets, no breakdown, no CTA. Detail/scope/explain queries continue to use the existing TL;DR + 3-4-bullet structure (Rules 2-3 renumbered, all other rules pushed by +1). Smoke test: "Mera lagna kya he" → 22 chars / 1 line / 0 bullets ✓; "Mera janma nakshatra" → 39 chars / 1 line ✓; "Career next 6 months" still gets TL;DR + 4 bullets / 1003 chars ✓. The model now adapts answer length to question complexity automatically.
- **Phase 2 Output Polish** (30 Apr 2026): Two-part follow-up to Phase 1 — (a) **Prompt extraction**: the Phase 1 8-rule guide is now a single module-level constant `_PT_SYS_INTRO` at `openai_helper.py` L73; the three passthrough call-sites (`_build_messages` ~L2018, `ai_ask` ~L13007, `ai_ask_stream` ~L16158) all reference the same constant, so any future tuning is a single-edit change. (b) **Real SSE streaming**: `ai_ask_stream` (previously a one-shot envelope wrapper) was rewritten to perform genuine token-by-token streaming. It builds the full chart-context block locally (via `kundli_full_context.build_full_chart_context`), opens an OpenAI `stream=True` chat completion (timeout = module-scope `_PRIMARY_LLM_TIMEOUT_S=25s`, model from `OPENAI_MODEL`), yields `{"kind":"delta","text":...}` per OpenAI chunk, then a final `{"kind":"final", source:"ai_passthrough_stream", text, topic, follow_ups}` event. The Flask SSE route at `flask_app.py` L5953 forwards delta events as `data: {"delta": "..."}` and the final event as `data: {"done": true, "text": "...", ...}`. Smoke test: 374 delta chunks, first-delta at 7.7s, final at 10.9s, source `ai_passthrough_stream`. (c) **Mobile chat UX**: `ask.tsx` assistant messages now show an action row under the markdown reply with three buttons — `Sun lo` (existing voice TTS), `Copy` (uses `expo-clipboard`, flips to "Copied" + check icon for 1.4s), and `Share` (uses RN `Share` API for the native share sheet). All three use shared `assistantActionsRow` / `actionPlainBtn` styles; voice button keeps the accent border to remain the primary affordance.
- **Phase 1 Prompt Polish** (30 Apr 2026): The passthrough `_sys_intro_pt` system prompt (in `openai_helper.py`, applied to BOTH streaming L1957+ and non-streaming L13007+ paths) was upgraded from a 4-line minimal intro to an 8-rule output style guide. Rules: (1) TL;DR-first 1-line answer, (2) 3-4 bullet length cap, (3) question-relevant house/planet focus filter with quick-reference table for Health/Career/Marriage/Wealth/General, (4) reuse last 6 conversation turns instead of repeating facts, (5) ban generic Sun-sign/rashi-falit phrasing ("Mithun rashi ke hisaab se" → forbidden) — always personalize as "aapki kundli ke context mein" / "aapke Lagna se", (6) rotate CTA wraps instead of repeating "Agar chaho to...", (7) cite D9 navamsha placement for marriage/long-term-health/career/spiritual questions and call out vargottama planets, (8) anti-hallucination — only classical Vedic relationships (dignity, drishti, listed yogas, dasha, nakshatra) are permitted. Applied to both streaming and non-streaming passthrough. Smoke-test confirmed: TL;DR present, ~187 words / 11 lines (vs previous wall-of-text), no generic-rashi phrasing, no "Agar chaho to..." CTA, ~8s latency (down from ~30s).

## External Dependencies

- **Database**: SQLite for user authentication; PostgreSQL with `pgvector` for the knowledge base.
- **Astrology Engine**: `pyswisseph` (Swiss Ephemeris).
- **AI/NLP**: OpenAI API (GPT models).
- **PDF Generation**: `reportlab`.
- **Mobile Development**: Expo (React Native).
- **Image Processing**: Pillow.
- **Ask Pipeline V2**: AI Ear for multi-intent extraction, `ai_ask_v2` dispatcher for fan-out, and AI Mouth for conversational narration.