# Cosmic Lens
A mobile Vedic Astrology application delivering accurate astrological analyses and AI-driven interpretations to users.

## Run & Operate
- **Run**: `pnpm dev`
- **Build**: `pnpm build`
- **Typecheck**: `pnpm typecheck`
- **Codegen**: `pnpm codegen`
- **DB Push**: `pnpm db:push`
- **Required Env Vars**: `OPENAI_API_KEY`, `DATABASE_URL`, `HEALTH_LLM_MODEL`, `HEALTH_ROUTER_MODEL`, `FINANCE_STATIC_BYPASS`, `HEALTH_STATIC_BYPASS`, `LEAN_PACK_MODE`, `LAYER2_FUZZY`, `LAYER3_LLM`

## Stack
- **Frameworks**: React Native (Expo), Flask (Python)
- **Runtime Versions**: Node.js 24, TypeScript 5.9, Python 3.10+
- **ORM**: SQLAlchemy
- **Validation**: Pydantic
- **Build Tool**: pnpm

## Where things live
- `apps/mobile`: Expo React Native application.
- `artifacts/api-server`: Flask backend.
- `packages/`: Shared utilities and components.
- **DB Schema**: `artifacts/api-server/database.py`
- **API Contracts**: Implicitly defined by Flask routes in `flask_app.py`
- **Theme Files**: `apps/mobile/src/theme/`
- **User Memory Layer (UCML)**: `artifacts/api-server/user_memory.py`

## Architecture decisions
- **Monorepo Structure**: Uses pnpm workspaces for dependency management and code sharing.
- **AI Orchestration**: `openai_helper.py` manages fact injection, verbatim citation, and deterministic post-injectors.
- **User Context Memory Layer (UCML) v1**: Silent, deterministic memory module (`user_memory.py`) building per-user profiles from question history with three tiers: `user_facts` (L1 atomic), `user_behavior` (L2 aggregates), `user_personality` (L3 vector). Engine accesses DB; LLM never touches it.
- **Health Timing Engine v1.0**: 9-step health-risk timing engine at `event_timing/health/health_engine_v1.py` with medical disclaimers and age guards. Emits a `▸ TIMING ENGINE` health row to `locked_facts.py`.
- **Baby Engine Active-Window Marking (Phase 2.5.5, May 7 2026)**: Step 5 dasha activation now explicitly marks each window with `active_window` (bool), `active_priority` (`PEAK`/`STRONG`/`TRIGGER`/`BACKGROUND`/`None`), and `active_lords_in_window` (list of role=lord strings) when a FINAL-GATE-PASSED promoter planet (survived Step 3c cross-chart ∩ Step 4c KP) is ruling at MD/AD/PD level. Priority hierarchy: `PEAK` = AD AND PD both gate-passed promoters, `STRONG` = AD-only, `TRIGGER` = PD-only (short pulse), `BACKGROUND` = MD-only. Reason: weighted score by itself mixes promoter contribution with obstructor noise — user wants the simple classical "kab promoter dasha aa rahi hai" check surfaced cleanly. New top-level result fields `child_active_windows` (chronological list capped at 8 upcoming entries with priority/active_lords/window/score/risk_raw) and `next_child_window` (first upcoming entry, or `None`). New `STEP5b active_windows_in_horizon=… next_priority=…` factor trace. Existing `next_3_windows` (score-sorted) untouched — the two views are complementary: score-sorted shows highest-net-favorability, active-list shows when filter-passed planets actually rule. No Sookshma (SD) added per user request — keep dasha simple at MD/AD/PD only. Tests: 1 new (53/53 green) — verifies top-level keys, structure invariants, chronological ordering, capped at 8, factor trace emission.
- **Baby Engine KP 2-5-11 Significator Filter (Phase 2.5.4, May 7 2026)**: New `_step4c_kp_significator_filter` runs after Step 4 weighted ranking and before Step 5 dasha activation. Implements the **classical Krishnamurti Paddhati childbirth rule**: a planet PROMISES a child in its AD/PD only if BOTH its **nakshatra-lord (NL/star-lord)** AND its **sub-lord (SBL)** signify at least one of the child houses **{2, 5, 11}** (2=family-expansion/kutumb-vridhi, 5=primary-progeny/putrasthana, 11=fulfillment-of-desire/gain-of-children). Strict AND-rule (both NL and SBL must signify ≥1 of {2,5,11}); planets failing this go into `blocked` list. Negation flag (`kp_negated=True`) raised for planets whose NL/SBL signifies ONLY houses {1,4,10} with zero 2-5-11 link — exposed for trace transparency, not used as hard gate. New helper `_get_planet_kp_lords(kp, planet)` extracts NL+SBL tolerantly from `kp.planets` (handles `nl`/`nakshatra_lord`/`starLord` and `sb`/`sub_lord`/`subLord`/`sl` key spellings). **Final-gate composition**: Step 5 now receives a `final_gate_map` that is the AND of Step 3c cross-chart filter AND Step 4c KP filter. Each gate independently safety-disables when its data is unavailable (Step 3c when D9+D7 both missing; Step 4c when KP block missing). When BOTH gates disabled → fall back to legacy D1-only promoter logic by passing `cross_map=None`. Final-gate map exposes per-planet `cross_pass`/`kp_pass` flags for downstream debugging. Result block adds `kp_significator_filter: {available, rule, passed, blocked, per_planet: {nl, sbl, nl_houses, sbl_houses, nl_signifies_child, sbl_signifies_child, kp_promotes_child, kp_negated}}`. New `STEP4c KP_2-5-11 passed=… blocked=…` factor trace. **Architect-fix tri-state safety (round 2)**: (a) gate now requires BOTH `kp.planets` AND `kp.significations` non-empty to activate (OR-check would over-block); (b) per-planet result is now **tri-state** — `pass` (NL+SBL both signify ≥1 of {2,5,11}), `fail` (both known but at least one fails), `unknown` (NL/SBL or signified-houses missing → degraded data); final-gate composition treats `unknown` as neutral (`kp_pass=True`) so partial KP data never collapses promoter eligibility — only an explicit `fail` blocks; (c) `kp_negated` now strict-only (signified-houses set is non-empty subset of {1,4,10} — pure negation, no other-house mitigation); (d) factor trace and result block now also expose `unknown` planets list. **Phase 2.5.4-r4 — Strong-cross-chart rescue**: Added an override clause to the final-gate composition: if a planet has `confirmations == 3` (5H credential present in **all three** of D1, D9, D7 — i.e. is 5L of that chart, occupant of 5H, aspecting 5H, or Jupiter karaka), a KP `fail` no longer vetoes it. Reason: KP is a **timing refinement**, not a promise-killer when D1+D9+D7 are unanimously positive. A super-strong child-significator (e.g. exalted Jupiter as 5H occupant + 5L of D9 + 5L of D7) cannot be wiped out merely because its NL/SBL happens to miss 2/5/11 in KP. The override is **narrow on purpose**: requires both gates active, KP must explicitly say `fail` (not unknown), and confirmations must be the full 3/3 — partial 2/3 cross-chart confirmation does NOT trigger the rescue (KP veto still applies). Final-gate map now exposes per-planet `kp_status` and `kp_override_by_strength` flags for trace transparency. Tests: 1 new (52/52 green) — strong-cross-chart Jupiter (Aries lagna, in 5H, KP NL=Mars+SBL=Mars→[6,8] explicit fail) survives in `top_child_planets` when confirmations==3. **Phase 2.5.4-r3 — Negation hard-block**: `kp_negated` is no longer just a diagnostic flag — it is now a **HARD BLOCK**. If a planet's NL or SBL signifies ONLY houses {1,4,10} (pure negation, no mitigation from any other house), `kp_status="fail"` regardless of any 2/5/11 link on the OTHER lord. Reason: a soft negation flag wasn't actually filtering anything, defeating the purpose of the gate. Pure-negation planets cannot promise a child even partially — classical KP allows mitigation only when the negation house appears MIXED with positive houses (e.g. `[4,5]` still passes because 5 is present), but a pure subset of {1,4,10} = real obstruction. Tests: 5 new (51/51 green) — empty-KP fallback, strict AND-rule, partial-KP-data (planets-only / significations-only) gate-disable, missing per-planet KP → unknown-not-blocked, pure-negation hard-block (Jupiter NL=Venus→[5,11] PASS-side but SBL=Saturn→[4,10] PURE-NEG → blocked). 3x synthetic smoke verified (Aries-Jup11+KP: passed=Jupiter,Sun; Libra-mixed+KP: cross_conf=Jupiter,Moon,Saturn but Saturn KP-blocked → final=Jupiter,Moon — gate intersection working). Obstruction/risk classifications still untouched. Lean Step 1 + D7 picture + D1∩D9∩D7 cross-chart + NO_GENDER_PREDICTION all intact.
- **Baby Engine Cross-Chart 5H Confirmation Filter (Phase 2.5.3, May 7 2026)**: New `_step3c_cross_chart_filter` runs after Step 3b D7 picture. For each Step-1 survivor it counts 5H-credentials (5L of that chart's lagna / occupant of 5H / aspecting 5H / Jupiter karaka) inside **all three charts (D1, D9, D7)** and flags `cross_confirmed` when confirmations ≥ 2 of 3. Step 5 dasha activation is now gated by this map — a planet only enters `promoter_lords` (CHILD_PROMOTER classification in its AD/PD) when cross-confirmed; otherwise its contribution is NEUTRAL even if it carries D1 promoter tags. This blocks "D1-only paper-promise" planets (e.g. a strong 5L that is debilitated/dusthana in D9 + has zero D7 5H link) from masquerading as true child-givers in their dasha. Obstructor / risk classifications are unaffected (always apply, regardless of cross-confirmation). New helper `_build_d9_chart()` mirrors `_build_d7_chart()` shape (lagna_si + planets with D9-houses) and is also passed into refactored `_step2_d9_verify(kundli, candidates, d9_chart=None)` — single source of truth so dignity-score and cross-confirmation can never disagree. **Bonus bug-fix**: previous `_step2_d9_verify` was calling `compute_d9(kundli)` with the wrong arg type (signature is `compute_d9(planets, lagna_lon)`); error was silently swallowed and every survivor was returning the 8.0 fallback dignity, defeating the whole D9-verify step. Now D9 dignity actually fires (exalted 25 / own 20 / neutral 12 / debilitated 5). New `cross_chart_filter` block in result dict (`confirmed_planets`, `per_planet` with `confirmations`/`confirmed_in`/`chart_links`/`cross_confirmed`, `rule`). New `STEP3c cross_confirmed=…` factor trace. **Architect-fix safety gate**: when BOTH D9 and D7 are unavailable (degraded data — e.g. missing `ascendant_longitude`), the ≥2/3 rule is structurally unreachable from D1 alone, so Step 5 would zero-out every promoter and collapse all dasha windows. The cross-chart gate now auto-disables in that case (`cross_map_for_gate=None`); Step 5 falls back to legacy D1-only promoter logic. Result block exposes `available_charts: {D1,D9,D7}` + `gate_active` for transparency. Tests: 3 new (46/46 green) — Jupiter always cross-confirmed, per-planet structure invariants, edge-case gate-disabled when D9+D7 unavailable. 3x synthetic smoke verified. NO_GENDER_PREDICTION + lean Step 1 + D7 picture all intact.
- **Baby Engine Step 1 Lean Refactor (Phase 2.5.2, May 7 2026)**: STEP1 D1 filter trimmed from 5 inclusion categories down to 3 core ones to remove over-engineering. KEPT: lordship 5L (+18) / 9L (+12) / 11L (+10), occupants of 5/9/11 (+14/+10/+8), aspects to 5H (+9), karakas Jupiter (+14) / Moon (+8) / Venus (+7). REMOVED: 7L (+8) and 2L (+6) lordship — out of scope for baby engine, marriage engine already covers partner-axis; 6/8/12 occupant boost (+4) — fertility-leak signal already lives in Step 5 dasha obstruction-bearer classification (was double-counting); Sun (+6) and Mars (+6) karaka bonus — double-counts what lordship/occupancy already covers; functional-malefic surcharge (+4) — Step 4 weighted ranking already handles dignity. Orphan `_FUNC_MALEFICS` lagna-table also deleted (was its only consumer); `_FUNC_BENEFICS` table retained — still drives Step 4 `_karaka_score` (+2 boost for functional benefics). PUTRA-KARAKA label renamed to PROGENY-KARAKA (continues Phase 2.5 gender-neutral rule). Net effect: cleaner survivor set, less noise into D9/D7/KP/dasha downstream, no behavioral regression (43/43 tests green incl. new `test_step1_excludes_2L_7L_obstruction_funcmalefic` regression guard).
- **Baby Engine D7 Picture Extension (Phase 2.5.1, May 7 2026)**: Added Step 3b `_step3b_d7_picture` after the D7 score step. Builds the full Saptamsha chart **once** via new `_build_d7_chart()` helper (tier-1 external `divisional_charts.compute_d7(planets, asc_lon)`, tier-2 internal Parashara odd/even longitude rule) and passes it to both `_step3_d7_progeny` (score) and `_step3b_d7_picture` (snapshot) so the score and the picture can never disagree. The picture captures: D7 lagna sign, **D7 1L** (planet/sign/house_in_d7/dignity exalted-own-debilitated-neutral/aspects-D7-5H), **D7 5L** (planet/sign/house_in_d7/dignity/in_child_house/in_dusthana/well_placed), occupants of D7 1H + D7 5H, planets aspecting D7 1H + D7 5H, and a `flags` block (`jupiter_aspects_d7_5h`, `benefic_in_d7_5h`, `malefic_in_d7_5h`, `d7_5l_in_dusthana`, `d7_5l_well_placed`, `d7_5l_in_child_house`, `d7_1l_aspects_5h`). New `_detect_d7_yogas()` emits **D7-Progeny-Yoga** (protective — D7 5L well-placed + Jupiter aspect/benefic on D7 5H), **D7-Bandhya** (high — D7 5L in D7 dusthana + no Jupiter rescue), **D7-Lagna-Activation** (protective — D7 1L aspects D7 5H); merged into main `yogas` list so verdict / `child_promised` / risk_flags / remedies pipelines pick them up automatically. New `d7_picture` field returned by `compute_baby_window`. Wired into `locked_facts.py` baby_line as a second-line trace (`D7 lagna … | 1L: X@H?/<dignity>/<sign> | 5L: Y@H?/<dignity>/<sign> | 5H_occ … | 5H_asp …`). Tests: 4 new (43/43 green) — picture structure, unavailable-graceful-fallback, D7-Progeny-Yoga emission, D7-Bandhya emission. 3x synthetic smoke verified (Aries+Jupiter-in-5 fired D7-Progeny-Yoga; Cancer+Mars-in-8 returned OBSTRUCTED with debilitated D7 1L Moon; Libra-mixed surfaced exalted D7 5L Mars). NO_GENDER_PREDICTION directive remains intact (no gender-hint vocabulary added).
- **Baby (Childbirth) Timing Engine v1.0 (Phase 2.5, May 7 2026)**: 9-step baby/conception/progeny timing engine at `event_timing/baby/baby_engine_v1.py`, mirroring health v1 / marriage v2.4 / finance v1 / travel v1 architecture (FILTER → VERIFY → ACTIVATE → TRIGGER). Pipeline: STEP1 D1 child-significator filter (5L PUTRASTHANA primary + 9L dharma + 11L gain + 7L partner + 2L family + occupants of 5/9/11 + planets aspecting 5H + karakas Jupiter PUTRA-KARAKA/Moon/Venus/Sun/Mars; obstruction-house occupants of 6/8/12 tagged as fertility-leak signal) → STEP2 D9 dignity → STEP3 **D7 Saptamsha** (Parashara's children chart, with internal longitude-based D7-sign computation when `compute_d7` helper missing, and final D1-dignity fallback) → STEP3.5 KP CSL of 5th & 11th cusps with precedence OBSTRUCTED > ANCHORED > CHILD_YES > NEUTRAL → STEP4 weighted ranking (D1·30 + D9·20 + D7·25 + KP·15 + karaka·10) → STEP5 dasha activation (AD=5, PD=6, MD=1; signed child-promoter vs obstruction-bearer with separate risk_score for Saturn/Mars in 8H/12H and Ketu on 5H) → STEP6 transits (Jupiter on 5/9/11 = primary conception window, Saturn on 5H = delay/postponement, Rahu on 5H = unconventional/IVF route, Mars on 8/12 = miscarriage risk, Sade Sati on Moon = emotional family stress) → STEP7 SAV bindus on 5H + 11H (santan band) → STEP9 yogas (Putra Yoga / Santan-Prapti / Putra-Karaka-Bala positive; Bandhya / Putra-Dosha / Miscarriage-Yoga negative; Adoption-Indicated informational). Verdict = `CHILD_PROMISED | FAVORABLE | DELAYED | OBSTRUCTED | UNKNOWN`; tier = `watchful | supportive | celebratory | consult`. Composite `child_promised` flag requires 3+ confirmations (positive Putra/Santan yoga + KP 5-cusp CHILD_YES + SAV santan-band ≥ MEDIUM + Jupiter in top-3 ranked). **Mandatory llm_directives include `NO_GENDER_PREDICTION`** (PCPNDT Act compliance + global ethical norm — engine NEVER predicts boy/girl, no signification area carries gender-hint vocabulary, regression-tested) plus `BABY_DISCLAIMER`, `NOT_MEDICAL_ADVICE`, `NO_GUARANTEED_CONCEPTION`, `NO_GUARANTEED_DATE`, `SEVERITY_TIER:<tier>`. Conditional `CHILD_PROMISED_INDICATED`, `CONSULT_FERTILITY_SPECIALIST` (when OBSTRUCTED), `MEDICAL_PRECAUTION_RECOMMENDED` (when Bandhya/Miscarriage yoga), `USER_TOO_YOUNG_FRAMING` (age<20), `ADVANCED_AGE_FERTILITY_CONTEXT` (age≥45). Engine delegates remedies via `remedy.get_remedies(topic="baby")` with graceful fallback to `topic="health"` + progeny-area tags when baby-topic catalog absent. Wired in `locked_facts.py` (▸ Baby row + baby_remedies_block). Thread-local cache `get_last_baby_result()`. Tests at `test_baby_engine.py` (37 cases incl. 5 architect-pattern regressions + dedicated NO_GENDER_PREDICTION ethical-guard tests).
- **Travel Timing Engine v1.0 (Phase 2.4, May 7 2026)**: 9-step travel/foreign-travel timing engine at `event_timing/travel/travel_engine_v1.py`, mirroring health v1 / marriage v2.4 / finance v1 architecture (FILTER → VERIFY → ACTIVATE → TRIGGER). Pipeline: STEP1 D1 travel-significator filter (12L FOREIGN + 9L long-distance + 3L short-trips + 7L away-from-home + 4L home-anchor INVERTED + occupants of 3/9/12 + planets aspecting 9H + karakas Rahu/Moon/Jupiter/Mercury/Saturn/Venus) → STEP2 D9 dignity → STEP3 **D4 Chaturthamsha** (Parashara's residence chart, with D1-dignity fallback when D4 helper missing) → STEP3.5 KP CSL of 3rd, 9th & 12th cusps → STEP4 weighted ranking (D1·30 + D9·20 + D4·25 + KP·15 + karaka·10) → STEP5 dasha activation (AD=5, PD=6, MD=1; signed travel-promoter vs home-anchor with separate risk_score for Mars/Rahu uprooting) → STEP6 transits (Jupiter on 9/12 = visa-luck/foreign-fortune, Saturn on 12H = long-stay/delayed-visa, Saturn on 4H = uprooted, Rahu on 12H = sudden foreign-shift / on 9H = sudden long journey, Mars on 3/9 = accident risk, Sade Sati on Moon = relocation pressure) → STEP7 SAV bindus on 9H + 12H (foreign band) → STEP9 yogas (Foreign-Settlement / Tirtha / Vidya-Yatra / Travel-Yoga positive; Sthanabhrama / Visa-Block / Risk-Travel negative). Verdict = `TRAVEL_PROMISED | FAVORABLE | LOW_PROBABILITY | HIGH_RISK_TRAVEL | UNKNOWN`; tier = `watchful | supportive | celebratory | consult`. Composite `foreign_promised` flag requires 3+ confirmations (foreign yoga + KP 12-cusp travel + SAV foreign-band ≥ MEDIUM + Rahu in top-3 ranked). Mandatory llm_directives: `TRAVEL_DISCLAIMER`, `NO_GUARANTEED_VISA_OUTCOME`, `NO_GUARANTEED_TRAVEL_DATE`, `NO_DESTINATION_NAMING`, `SEVERITY_TIER:<tier>`; conditional `FOREIGN_TRAVEL_INDICATED`+`CONSULT_PROFESSIONAL_FOR_LEGAL` (when foreign_promised), `ADVISE_TRAVEL_INSURANCE_AND_CAUTION` (when HIGH_RISK_TRAVEL). Engine delegates remedies via `remedy.get_remedies(topic="travel")` with graceful fallback to `topic="career"` + travel-area tags when travel-topic catalog absent. Wired in `locked_facts.py` (▸ Travel row + travel_remedies_block). Thread-local cache `get_last_travel_result()`. Tests at `test_travel_engine.py` (28 cases incl. architect-pattern regressions).
- **Finance Timing Engine v1.0 (Phase 2.3, May 7 2026)**: 9-step wealth/finance timing engine at `event_timing/finance/finance_engine_v1.py`, mirroring health v1 / marriage v2.4 architecture (FILTER → VERIFY → ACTIVATE → TRIGGER). Pipeline: STEP1 D1 wealth-significator filter (2L/5L/9L/11L wealth + 6L/8L/12L leak + occupants of 2/11/12 + planets aspecting 2H + karakas Jupiter/Venus/Mercury/Sun/Moon) → STEP2 D9 dignity → STEP3 **D2 Hora** (Parashara's wealth chart, replaces D30) → STEP3.5 KP CSL of 2nd & 11th cusps → STEP4 weighted ranking (D1·30 + D9·20 + D2·25 + KP·15 + karaka·10) → STEP5 dasha activation (AD=5, PD=6, MD=1; signed wealth-promoter vs leak-bearer) → STEP6 transits (Jupiter on 2/5/11 = gain, Saturn on 2H = squeeze / 12H = drain, Rahu on 11H = sudden gain / 6H = debt risk, Sade Sati on Moon) → STEP7 SAV bindus on 2H + 11H → STEP9 yogas (Dhana / Lakshmi / Kubera / Gaja-Kesari / Vipreet-Rajyoga positive; Daridra / Kemadruma / Papakartari-on-2H negative). Verdict = `WEALTH_PROMISED | STABLE | STRESSED | HIGH_LEAK_WINDOW | UNKNOWN`; tier = `watchful | supportive | celebratory | consult` (matches **money topic** in remedy engine). Mandatory llm_directives: `FINANCIAL_DISCLAIMER`, `NOT_INVESTMENT_ADVICE`, `NO_GUARANTEED_WEALTH`, `NO_GUARANTEED_LOSS`, `SEVERITY_TIER:<tier>`, `NO_STOCK_TICKER_NAMING`. 3-confirmation rule for `consult` tier. Engine delegates remedies via `remedy.get_remedies(topic="money")`. Wired in `locked_facts.py` (▸ Finance row + finance_remedies_block) alongside legacy `wealth_timing()`. Thread-local cache `get_last_finance_result()` for downstream consumers. Tests at `test_finance_engine.py` (24 cases).
- **Remedy Engine v1.1 (Phase 2.2, May 6 2026)**: Standalone hybrid 3-tier engine at `remedy/` (catalog + remedy_engine_v1 + conflict_check + substitutions + stack_builder). Public API `get_remedies(topic, planets, areas, severity, user_facts)` + `render_for_locked_facts(result)`. Covers **health / marriage / career / money / business** × 9 grahas × 3 tiers (PRACTICAL first → AYURVEDIC → VEDIC last). Money topic = dhan-yog/savings/debt/investing (separate from career = role/income-source). Business topic = startup/founders/cashflow/scaling (separate from career = employee track). New SYSTEM_PRACTICES added for `savings, debt, investing, income_growth, expense_control, emergency_fund, credit_score, taxes, gold_assets, founders_fit, product_market_fit, cashflow, hiring, sales_pipeline, partnerships, scaling, brand, legal_compliance`. Money/business severity tiers: `watchful / supportive / celebratory / consult`. Anti-superstition: vedic never alone, every entry carries KPI + cost + caveats. Health engine delegates via `_compute_health_remedies`. Marriage uses `top_marriage_planets`, career uses `house_lords`. All topics emit parallel `▸ <TOPIC> REMEDIES` blocks via `locked_facts.py`.
- **Marriage Engine v2.4**: Integrates multi-format DOB parsing and practical-age floor for marriage window predictions, influencing LLM directives for young users.
- **User-Facing Translation Layer (GOLDEN RULE)**: Bans all astrology jargon from user-facing output.

## Product
- **Mobile App**: Daily Rashifal, Panchang, Kundli Milan, Muhurat finder, Numerology, Remedies, Vastu tips, Dosha analysis, personalized forecasts ("Risk Radar"), Life Mastery Report.
- **AI-driven Insights**: AI-powered interpretations with anti-hallucination pipelines, RAG for classical Vedic knowledge, and structured output modes.
- **Localization**: Supports 25 languages with region detection.

## User preferences
- The user wants the AI to act as a replit coding agent.
- The user prefers a strict development methodology: `engine.py file (sahi folder mein)`, `Unit test (test_engine.py)`, `locked_facts.py wiring`, `Rule entry in system prompt`, `Deterministic post-injector`, `3x smoke test on real chart (/tmp/k.json, /tmp/req.json, /tmp/qm.json)`, `Architect review (code_review skill)`, `replit.md entry` are mandatory for every new engine.
- The user has provided a clear roadmap for project phases and expects strict adherence to it. Deviations require explicit user confirmation.
- The user expects the AI to avoid hallucinating, especially regarding timing-related questions.
- The user wants the AI to integrate classical Vedic knowledge through RAG for opinion-based questions, but strictly NOT for timing questions.
- The user has defined a brand voice: "Powered by Advanced Cosmic Intelligence" and explicitly states "AI never named; AI = language layer only; backend = facts; post-injectors = mandatory citation safety net."
- The user prefers deterministic post-injectors to ensure mandatory facts are cited even if the AI misclassifies a topic.
- The user prefers that the AI refine language for opinion questions but must cite engine facts.
- The user wants medical outputs to include a mandatory medical disclaimer banner and a 3-confirmation rule for severity tiers.
- The user wants financial outputs to include a mandatory financial disclaimer banner and a tiered risk profile.
- The user wants remedies outputs to include a mandatory disclaimer ("Remedies SUPPLEMENT, never substitute action") and provide both free and paid alternatives.
- The user wants Astrocartography outputs to include a mandatory disclaimer ("energetic affinity, not guaranteed luck").
- The user expects ethical and safety disclaimers for sensitive topics like medical, lifespan, death prediction, and destiny vs. guidance.
- The user prefers a "Numerology PDF Pro" branding for the report, with a hybrid numerology + Vedic Astrology content, framed with numerology language.

## Gotchas
- **Static Gate Parity**: Any new engine/gate added to `/api/ask` MUST simultaneously be added to `/api/ask/stream`.
- **Cache Invalidation**: Any policy change requires bumping the cache namespace (`_vX`).
- **Vedic Vocab Stripping**: The validator strips Vedic terms unless explicitly opted in or the user's question contains a "tech-request" keyword.
- **Topic-Lock Conflicts**: When multiple injection points compete, the latest injected point in the user message order wins.
- **DB-Load Enforcement**: For authenticated users, `kundli` is always loaded from the database; client-supplied `kundli` is for anonymous demo mode only.
- **LLM Temperature**: The `temperature` parameter is often rejected by `gpt-5` models.
- **Telemetry Best-Effort**: Telemetry inserts are wrapped in `try/except` and silently drop rows on contention.

## Pointers
- **Relevant Skills**: `code_review`, `telemetry`, `test_generation`
- **External Docs**:
    - [pnpm workspaces](https://pnpm.pnpm.io/workspaces)
    - [Expo React Native](https://docs.expo.dev/)
    - [Flask Documentation](https://flask.palletsprojects.com/en/latest/)
    - [Pyswisseph Library](https://pyswisseph.readthedocs.io/en/latest/)
    - [OpenAI API Documentation](https://platform.openai.com/docs/api-reference)