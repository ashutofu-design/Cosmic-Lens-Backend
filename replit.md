# Cosmic Lens
A mobile Vedic Astrology application delivering accurate astrological analyses and AI-driven interpretations to users.

## Run & Operate
- **Run**: `pnpm dev`
- **Build**: `pnpm build`
- **Typecheck**: `pnpm typecheck`
- **Codegen**: `pnpm codegen`
- **DB Push**: `pnpm db:push`
- **Required Env Vars**: `OPENAI_API_KEY`, `DATABASE_URL`, `HEALTH_LLM_MODEL`, `HEALTH_ROUTER_MODEL`, `FINANCE_STATIC_BYPASS`, `HEALTH_STATIC_BYPASS`, `LEAN_PACK_MODE`, `LAYER2_FUZZY`, `LAYER3_LLM`

## Stack
- **Frameworks**: React Native (Expo), Flask (Python)
- **Runtime Versions**: Node.js 24, TypeScript 5.9, Python 3.10+
- **ORM**: SQLAlchemy
- **Validation**: Pydantic
- **Build Tool**: pnpm

## Where things live
- `apps/mobile`: Expo React Native application.
- `artifacts/api-server`: Flask backend.
- `packages/`: Shared utilities and components.
- **DB Schema**: `artifacts/api-server/database.py`
- **API Contracts**: Implicitly defined by Flask routes in `flask_app.py`
- **Theme Files**: `apps/mobile/src/theme/`
- **User Memory Layer (UCML)**: `artifacts/api-server/user_memory.py`
- **Engines**: `artifacts/api-server/event_timing/{health,marriage,finance,travel,baby}/`, `artifacts/api-server/remedy/`
- **LLM Orchestration**: `artifacts/api-server/openai_helper.py`, `artifacts/api-server/reply_cosmo/engine_locked_to_llm/locked_facts.py`

## Architecture decisions

### Foundational
- **Monorepo Structure**: pnpm workspaces for dependency management and code sharing.
- **AI Orchestration**: `openai_helper.py` manages fact injection, verbatim citation, and deterministic post-injectors.
- **User Context Memory Layer (UCML) v1**: Silent, deterministic memory module (`user_memory.py`) building per-user profiles from question history with three tiers: `user_facts` (L1 atomic), `user_behavior` (L2 aggregates), `user_personality` (L3 vector). Engine accesses DB; LLM never touches it.
- **User-Facing Translation Layer (GOLDEN RULE)**: Bans all astrology jargon from user-facing output.

### Engine Lineage (Phases 2.2 → 2.5.11, May 2026 — consolidated)
Five timing engines (Health v1, Marriage v2.4, Finance v1, Travel v1, Baby v1) all follow the same 9-step pipeline (FILTER → VERIFY → ACTIVATE → TRIGGER): D1 lean filter → D9 dignity → divisional chart (D7 babies / D2 finance / D4 travel; **Health: D30 layer REMOVED in 2.5.11.9 — KP CSL of 6/8 cusps + D9 dignity now carry the disease-verify load**) → KP CSL of topic cusps → weighted ranking (default D1·30 + D9·20 + Dx·25 + KP·15 + karaka·10; **Health rebalanced post-2.5.11.9 to D1·40 + D9·30 + KP·20 + karaka·10**) → dasha activation (AD=5, PD=6, MD=1; signed promoter vs blocker) → transits (Jupiter/Saturn/Rahu/Sade-Sati on topic houses) → SAV bindus on topic houses → topic-specific yogas. Each engine emits a `▸ <TOPIC>` row to `locked_facts.py` + mandatory `llm_directives` (disclaimers + `SEVERITY_TIER`) + thread-local cache (`get_last_<topic>_result()`). Every engine has a paired `test_<topic>_engine.py`. Highlights: **Baby engine** (2.5.0→2.5.10) added cross-chart filter (D1∩D9∩D7), KP 2-5-11 significator gate, K.N.Rao Double Transit, gestation-aware re-rank (delivery-chart check + monthly safety scan), outcome-based gate fallback, mandatory `NO_GENDER_PREDICTION` directive (PCPNDT compliance) — 60/60 tests green. **Remedy Engine v1.1** (2.2): hybrid 3-tier (PRACTICAL → AYURVEDIC → VEDIC last) covering health/marriage/career/money/business × 9 grahas; anti-superstition rules, every entry carries KPI + cost + caveats. **Dasha Timeline Block** (2.5.11): rewrote `_format_dasha_block` to emit current MD/AD/PD + every dasha transition over next 5 years, handles both shape-A `{planet,startDate,subDashas}` and shape-B `{lord,start,antardashas}`, hard-cap 60 lines, three-tier graceful fallback. **Marriage Engine v2.4**: multi-format DOB parsing + practical-age floor for marriage windows.

### Active Development (latest)
- **Health Engine STEP 6/7 Permanent Removal — Phase 2.5.11.11 (May 7 2026)**: User decision — **STEP 6 (transit triggers) + STEP 7 (Ashtakavarga SAV bindus) permanently removed**. Pipeline reduced 8-step → **6-step**: D1 → D9 → KP convergence → weighted rank → dasha activation → yogas. Rationale: live-sky transits already injected globally via openai_helper's `current_transits` block (zero loss for the LLM); SAV vitality bands rarely flipped severity tiers in practice. **Changes**: (a) `_step6_transits` + `_planet_sign_at` + `_step7_ashtakavarga` helpers renamed `_DELETED_*` and orphaned (kept as commented archival code, never called); (b) orchestrator STEP 6/7 blocks deleted; STEP 8 (yogas) renumbered → STEP 6 (factors line now `STEP6 yogas=...`); (c) `_severity_of_window(score, transit_load=0.0)` — `transit_load` kept as no-op default for back-compat, severity now keys off score only; (d) `_derive_verdict(top_score, yogas)` signature reduced from 4 args → 2 (dropped `ashta_band` + `transit_load`); HIGH_RISK_WINDOW now requires `score≥8 AND has_arishta`, VULNERABLE = `score≥5`; (e) output schema dropped `transits` + `ashtakavarga` keys; (f) risk_flags `LOW_SAV_LAGNA_VITALITY` + `HEAVY_TRANSIT_LOAD` removed; (g) docstring + STEP-numbering comments updated. **Test updates**: `test_severity_bands` now asserts transit_load is no-op; `test_derive_verdict_*` use new 2-arg signature; pipeline expected-fields list dropped `transits`/`ashtakavarga`. **Verification**: 23/24 tests pass (TestFullPipeline env-only fail — `/tmp/k.json` fixture missing). Live anon health Q returned 200 OK clean answer post-restart. **Trade-off**: lost engine-internal transit boost on dasha severity scoring — if a malefic dasha + transit combo previously bumped a window from "moderate" to "serious", it now stays "moderate". User accepted this since the LLM still sees current transits via the global block and can narrate them qualitatively.
- **Health Engine STEP 3↔STEP 4 Cross-Wiring — Phase 2.5.11.10 Option A (May 7 2026)**: Lightweight wiring (no merge) — STEP 4 ranked entries now carry STEP 3's `convergence_score` (0-100) + `at_risk` boolean as cross-reference columns. Implementation: `_step4_rank` signature gained optional `kp_layer=None` kwarg; builds case-insensitive O(1) `convergence_lookup` from `kp_layer["at_risk_planets"]` at function entry; each ranked planet dict gets `convergence_score` + `at_risk` fields (default 0/False when kp_layer absent — backward-compat preserved). Orchestrator passes `kp_layer=kp_layer`; factors line now logs `STEP4 ranked=Saturn:78.5/conv=100,Mars:62.3/conv=50,...`. **Schema additions**: `top_health_planets[]` + `weighted_breakdown` both gained `convergence_score, at_risk`. **Why no merge**: STEP 3 = binary qualifier gate (different output schema, different consumers — risk_flags + LLM directives), STEP 4 = continuous ranker (feeds STEP 5 dasha activation). Different scales (0-100 vs weighted-percent), different responsibilities. SOLID separation preserved; only the cross-reference flows. **Verification**: 23/24 tests pass (1 new test `test_step4_carries_step3_convergence_score` validates Saturn=100/Jupiter=0/backward-compat=0 — all PASS; TestFullPipeline env-only fail). Live anon health Q returned 200 OK / clean answer post-restart.
- **Health Engine STEP 3 v2 — Phase 2.5.11.10 (May 7 2026)**: User-spec'd KP convergence layer expanding STEP 3 from a simple 2-cusp verdict into a 3-layer at-risk planet filter. **Layer A (cusp verdicts)**: 12th CSL added alongside existing 6th + 8th — new `verdict_12 ∈ {HOSPITAL_YES, HOSPITAL_NO, NEUTRAL}` (signifies 6/8/12 → YES, signifies 1/5/11 → NO). **Layer B (per-survivor KP signification)**: For each D1+D9 survivor, `_planet_signified_houses(kp, planet)` lookup checks composite NL→sub→sub-sub signification (KP module pre-aggregates the 3 levels upstream); planet flagged `kp_signifies_dusthana=True` if any of 6/8/12 hit. **Layer C (active-CSL gate)**: New helper `_current_dasha_lords(chain, now)` finds the dasha window containing `now` and returns `{md, ad, pd}` lords; STEP 3 builds `csl_to_cusp` reverse-map and intersects with current MD/AD/PD lords → `active_csl_planets` list (CSL "fires" only when its planet runs the dasha NOW). **Final at_risk_planets**: D1+D9 survivors that EITHER signify 6/8/12 in KP OR are an active CSL right now, sorted by `convergence_score = 20(D1 survivor) + 20(D9≥12 verified) + 30(KP dusthana sig) + 30(active CSL now)` — max 100. **Orchestrator changes**: `_flatten_dasha_chain` + `_current_dasha_lords` moved BEFORE STEP 3 (was at STEP 5); `_step3_kp_layer` signature gained 3 optional kwargs `(d1_map, d9_scores, current_dasha)` — backward-compat preserved (no kwargs → legacy Layer-A-only). **New risk_flags**: `KP_12CSL_DUSTHANA`, `KP_CSL_ACTIVE_NOW`, `KP_CONVERGENCE_STRONG` (any at-risk planet ≥70/100). **Output schema**: `kp_layer` extended with `csl_12, verdict_12, csl_12_signifies, active_csl_planets[], at_risk_planets[{planet, kp_signifies, kp_dusthana_houses, is_active_csl_now, active_csl_of, d9_score, convergence_score}]`. **Verification**: 21/21 unit tests pass (5 new STEP 3 v2 tests: 12th HOSPITAL_YES + HOSPITAL_NO, full convergence math with Saturn=100/Mars=50/Jupiter-excluded/Mercury-excluded, active-CSL-without-dusthana-sig branch, backward-compat with no kwargs); TestFullPipeline env-only failure (`/tmp/k.json` missing). Live anon health Q returned 200 OK / 335-char clean answer. **Why this matters**: Pre-2.5.11.10 STEP 3 only said "is the disease vector present in cusp verdicts?" — now it says "WHICH specific surviving planet is BOTH a KP-signified disease vector AND firing in the user's current dasha?" — direct answer to user's "konsa planet health issue de sakta he" question.
- **Health Engine D30 Removal — Phase 2.5.11.9 (May 7 2026)**: User decision — Trimshamsa (D30) verification layer permanently removed from `health_engine_v1.py`. Pipeline reduced 9-step → 8-step. Changes: (a) deleted `_step3_d30_verify()` function entirely; (b) dropped `compute_d30` import from health engine (still used elsewhere by `openai_helper.py` subtle-vargas misfortune block + `locked_facts.py` D30/D40/D45/D60 — untouched); (c) renamed `_step3_5_kp_layer` → `_step3_kp_layer` (KP promoted from STEP 3.5 → STEP 3); (d) `_step4_rank` signature lost `d30_scores` param, formula rebalanced from `D1·30 + D9·20 + D30·25 + KP·15 + karaka·10` → `D1·40 + D9·30 + KP·20 + karaka·10` (D30's 25% redistributed: +10 to D1, +10 to D9, +5 to KP); (e) output schema cleaned — `top_health_planets` & `weighted_breakdown` no longer carry `d30` field; (f) orchestrator STEP 3 block + factors line removed; (g) `test_health_engine.py` updated (`_step3_5_kp_layer` import + assertions renamed; `d30` removed from expected breakdown keys). **Verification**: 17/17 unit tests pass (TestFullPipeline needs `/tmp/k.json` fixture — env-only failure, not engine bug). Live anon health Q returned 200 OK / 47w clean answer. **Trade-off**: Lost Parashara's dedicated disease-chart confirmation; KP CSL of 6/8 + D9 dignity must now do the verify work alone — false-positive risk slightly higher, will be monitored in next health Q-bank stress wave.
- **Specific-Partner Synastry Bridge — Phase 2.5.11.6 (May 7 2026)**: New ask-flow bridge that routes specific-person partner Qs ("Mere BF se shaadi hogi", "wife loyal rahegi", "mere pati ka swabhav") to either a real synastry calculation (when partner kundli is saved) or a CTA card prompting the user to add the partner's birth details. **Detector** in `openai_helper.py`: new `_SPECIFIC_PARTNER_RE` matches "mere/meri/my <bf|gf|boyfriend|girlfriend|husband|wife|pati|patni|fiance|mangetar|partner|saathi>", "uske/iske saath shaadi/rishta", "is rishte me", "hum dono ki/ka/ke" — distinct from generic future-spouse Qs ("future husband kaisa hoga" — DOES NOT trigger). New `_hint_relation_from_q()` maps the Q's pronoun/noun to the right Profile.relation slot. **DB lookup** `_lookup_partner_profile(user_id)` queries `Profile.query.filter(user_id, relation IN [Husband, Wife, Boyfriend, Girlfriend, Fiance, Fiancee, Partner, Spouse], deleted_at IS NULL).order_by(updated_at DESC)`, returns `(partner_kundli_dict, partner_name, partner_relation)` or None. **Three-branch flow** wired in `raw_passthrough_ask` (signature gained `user_id`; both `/api/ask` and `/api/ask/stream` updated): (a) authed user + partner profile found → inject `=== PARTNER CHART ({Relation}: {Name}) ===` block + activate `partner_compat_rule` (confirm-first sentence, scan 5 dimensions Moon-Moon/Venus↔Mars/7H lord D1+D9/Mercury-Mercury/Rahu-Ketu, pick top 2-3, strengths→friction→outlook, MAX 1 planet from EACH chart, 70-110w, NO Devanagari, NEVER reveal raw partner positions); (b) authed user + partner MISSING → early-return `{requires_partner_profile: True, partner_cta: {label, action: "open_profile_edit", relation}, source: "requires_partner_profile"}` — no LLM call (saves cost+latency); (c) anon → falls through to `marriage_psychology_rule` nuanced framing. **Hardening (architect-applied)**: `_AMBIGUOUS_PARTNER_RE` + `_ROMANTIC_INTENT_RE` intent gate to reject biz-partner; `_sanitize_partner_label()` strips newlines/control-chars/role-markers/brackets/quotes; mutual-exclusion of `partner_compat_rule` with `marriage_psychology_rule` + `sensitive_depth_rule`. **Mobile** (`apps/cosmic-lens-mobile`): `profile-edit.tsx` RELATIONS extended with Boyfriend/Girlfriend/Fiance/Partner + accepts `?relation=` query param via `useLocalSearchParams` (one-shot via `_autoOpenedRef`); `ask.tsx` Message type gained `partnerCta?: {label, relation}` rendering inline gradient-accent button below bubble that calls `router.push({pathname: "/profile-edit", params: {relation}})`. **Diagnostic Wave 1 (May 7)**: 25-Q marriage/love stress battery via `/tmp/research/` harness — **23 PASS / 1 WARN / 1 FAIL** (92%). Two genuine misses (F02 "partner mujhe chhod dega kya?" 23w + E02 "kabhi loved feel kar paunga?" 28w) — both abandonment/existential-fear Qs that escape `_SENSITIVE_STATIC_RE`. Tests: 13/14 detector smoke pass; e2e biz-partner correctly rejected; e2e anon falls through to nuanced answer.
- **Marriage/Love Domain Deep Evolution — Phase 2.5.11.5 (May 7 2026)**: TEST→AUDIT→FIX→RETEST loop on 25-Q marriage/love stress battery (5 sub-domains × 5 angles). Phase-3 audit found 7 fails / 25 (72% baseline): breakup-recent blunt 1-line "nahi" (TRUST-CRITICAL), age-anxiety no reassurance, family-force-conflict no autonomy validation, specific-person flat haan/nahi, LDR generic, karmic-delay + Devanagari script leak, partner-trait gender stereotype. **Root-cause fixes** in `openai_helper.py`: (a) Expanded `_SENSITIVE_STATIC_RE` with 5 marriage-emotional trigger groups — `\bbreakup\b|brek-up|rishta toot|bf/gf chhod`, toxic-relation/controlling/abuse, `(3X-9X) ki hu...shaadi/single/akela` (age-marriage anxiety bidirectional), `family.{0,30}(pressure|force|maan nahi|zabardasti)` (30-char filler-word gap fix), `arranged.{0,30}(force|pressure|nahi karni)`, `one-sided|unrequited|jise pyar...wo nahi`. (b) New `_MARRIAGE_DOMAIN_RE` + `_is_marriage_domain_q()` broad detector. (c) New `marriage_psychology_rule` for STATIC marriage Qs (mutually exclusive with sensitive/long-story; sensitive supersedes) — 6 framing protocols: DELAY-type classifier (Saturn-mature/family-blockage/self-focus/karmic → "delay≠denial"+name+next-window), BREAKUP-phase classifier (40w min, empathy+classification+healing-window), PARTNER-trait psychology vocabulary (FORBIDS gendered framing), SPECIFIC-PERSON nuance ("depends on X,Y,Z"), FAMILY-CONFLICT autonomy validation, AGE-ANXIETY reassurance, FUTURE-WINDOW search. Hard rules: 45-65w normal / 65-90w emotional, MAX 1 planet, Latin Hinglish only. (d) Auto-promote `static_dasha_hint=True` for marriage-domain. **Verification**: 6/6 previously-failing Qs now PASS.
- **Engine Evolution Mode — Phase 2.5.11.4 (May 7 2026)**: TEST→FAILURE-DETECT→FIX→RETEST loop on 20-Q stress battery surfaced 4 architectural failures: (1) sensitive regex missed parent-illness ("Papa beemar"), anxiety/panic, suicidal/depression, addiction, job-loss; (2) planet-citation cap violated (Venus+Saturn together); (3) long-story Qs collapsed to TIER-1 brevity (~25w) felt dismissive; (4) anxiety/family-illness lacked safety-net referral. **Root-cause fixes**: (a) Expanded `_SENSITIVE_STATIC_RE` with parent-illness pattern (40-char filler-word gap between subject papa/maa/mummy/husband/wife/bhai/behen/family and illness keyword beemar/cancer/hospital/ICU/surgery/tumor/treatment, BOTH directions) + anxiety_attack/panic/suicid/depress/addiction/sharab/nasha/job-loss patterns. (b) New `_LONG_STORY_RE` + `_is_long_story_q()` (matches "N saal/mahine se", "pichhle N", "har mahine/hafte", "baar baar"; requires ≥12 words). (c) Wired `is_long_story` into `raw_passthrough_ask`: auto-promotes static_dasha_hint=True + injects `long_story_rule` (overrides TIER-1 brevity, mandates 35-50w: acknowledge-pattern → WHY-it-repeats → what-changes/when/actionable + Final line). (d) Strengthened `sensitive_depth_rule` with absolute hard-cap "AT MOST ONE planet name" + mandatory safety-net for anxiety/family-illness/addiction. **Verification**: 14/14 retest pass.

## Product
- **Mobile App**: Daily Rashifal, Panchang, Kundli Milan, Muhurat finder, Numerology, Remedies, Vastu tips, Dosha analysis, personalized forecasts ("Risk Radar"), Life Mastery Report.
- **AI-driven Insights**: AI-powered interpretations with anti-hallucination pipelines, RAG for classical Vedic knowledge, and structured output modes.
- **Localization**: Supports 25 languages with region detection.

## User preferences
- The user wants the AI to act as a replit coding agent.
- The user prefers a strict development methodology: `engine.py file (sahi folder mein)`, `Unit test (test_engine.py)`, `locked_facts.py wiring`, `Rule entry in system prompt`, `Deterministic post-injector`, `3x smoke test on real chart (/tmp/k.json, /tmp/req.json, /tmp/qm.json)`, `Architect review (code_review skill)`, `replit.md entry` are mandatory for every new engine.
- The user has provided a clear roadmap for project phases and expects strict adherence to it. Deviations require explicit user confirmation.
- The user expects the AI to avoid hallucinating, especially regarding timing-related questions.
- The user wants the AI to integrate classical Vedic knowledge through RAG for opinion-based questions, but strictly NOT for timing questions.
- The user has defined a brand voice: "Powered by Advanced Cosmic Intelligence" and explicitly states "AI never named; AI = language layer only; backend = facts; post-injectors = mandatory citation safety net."
- The user prefers deterministic post-injectors to ensure mandatory facts are cited even if the AI misclassifies a topic.
- The user prefers that the AI refine language for opinion questions but must cite engine facts.
- The user wants medical outputs to include a mandatory medical disclaimer banner and a 3-confirmation rule for severity tiers.
- The user wants financial outputs to include a mandatory financial disclaimer banner and a tiered risk profile.
- The user wants remedies outputs to include a mandatory disclaimer ("Remedies SUPPLEMENT, never substitute action") and provide both free and paid alternatives.
- The user wants Astrocartography outputs to include a mandatory disclaimer ("energetic affinity, not guaranteed luck").
- The user expects ethical and safety disclaimers for sensitive topics like medical, lifespan, death prediction, and destiny vs. guidance.
- The user prefers a "Numerology PDF Pro" branding for the report, with a hybrid numerology + Vedic Astrology content, framed with numerology language.

## Gotchas
- **Static Gate Parity**: Any new engine/gate added to `/api/ask` MUST simultaneously be added to `/api/ask/stream`.
- **Cache Invalidation**: Any policy change requires bumping the cache namespace (`_vX`).
- **Vedic Vocab Stripping**: The validator strips Vedic terms unless explicitly opted in or the user's question contains a "tech-request" keyword.
- **Topic-Lock Conflicts**: When multiple injection points compete, the latest injected point in the user message order wins.
- **DB-Load Enforcement**: For authenticated users, `kundli` is always loaded from the database; client-supplied `kundli` is for anonymous demo mode only.
- **LLM Temperature**: The `temperature` parameter is often rejected by `gpt-5` models.
- **Telemetry Best-Effort**: Telemetry inserts are wrapped in `try/except` and silently drop rows on contention.

## Pointers
- **Relevant Skills**: `code_review`, `telemetry`, `test_generation`
- **External Docs**:
    - [pnpm workspaces](https://pnpm.pnpm.io/workspaces)
    - [Expo React Native](https://docs.expo.dev/)
    - [Flask Documentation](https://flask.palletsprojects.com/en/latest/)
    - [Pyswisseph Library](https://pyswisseph.readthedocs.io/en/latest/)
    - [OpenAI API Documentation](https://platform.openai.com/docs/api-reference)
