# Cosmic Lens
A mobile Vedic Astrology application delivering accurate astrological analyses and AI-driven interpretations to users.

## Run & Operate
- **Run**: `pnpm dev`
- **Build**: `pnpm build`
- **Typecheck**: `pnpm typecheck`
- **Codegen**: `pnpm codegen`
- **DB Push**: `pnpm db:push`
- **Required Env Vars**: `OPENAI_API_KEY`, `DATABASE_URL`, `HEALTH_LLM_MODEL`, `HEALTH_ROUTER_MODEL`, `FINANCE_STATIC_BYPASS`, `HEALTH_STATIC_BYPASS`, `LEAN_PACK_MODE`, `LAYER2_FUZZY`, `LAYER3_LLM`

## Stack
- **Frameworks**: React Native (Expo), Flask (Python)
- **Runtime Versions**: Node.js 24, TypeScript 5.9, Python 3.10+
- **ORM**: SQLAlchemy
- **Validation**: Pydantic
- **Build Tool**: pnpm

## Where things live
- `apps/mobile`: Expo React Native application.
- `artifacts/api-server`: Flask backend.
- `packages/`: Shared utilities and components.
- **DB Schema**: `artifacts/api-server/database.py`, `artifacts/api-server/models.py`
- **API Contracts**: Flask routes in `artifacts/api-server/flask_app.py`
- **Theme Files**: `apps/mobile/src/theme/`
- **User Memory Layer (UCML)**: `artifacts/api-server/user_memory.py`
- **Question History**: `artifacts/api-server/question_history.py` (Q + A persistence)
- **Engines**: `artifacts/api-server/event_timing/{health,marriage,finance,travel,baby}/`, `artifacts/api-server/remedy/`
- **Shared Engine Helpers**: `artifacts/api-server/event_timing/_shared/` (KP scan, double-transit)
- **LLM Orchestration**: `artifacts/api-server/openai_helper.py`, `artifacts/api-server/reply_cosmo/engine_locked_to_llm/locked_facts.py`

## Architecture decisions

### Foundational
- **Monorepo Structure**: pnpm workspaces for dependency management and code sharing.
- **2-Path Routing**: Live ask endpoint splits into `raw_passthrough_timing` (engine-locked, deterministic) and `raw_passthrough_static` (LLM-driven, jargon-free). `RAW_PASSTHROUGH_MODE=1` is the only live path; legacy paths kept for fallback.
- **AI Orchestration**: `openai_helper.py` manages fact injection, verbatim citation, and deterministic post-injectors.
- **User Context Memory Layer (UCML) v1**: Silent, deterministic memory module (`user_memory.py`) building per-user profiles from question history with three tiers: `user_facts` (L1 atomic), `user_behavior` (L2 aggregates), `user_personality` (L3 vector). Engine accesses DB; LLM never touches it.
- **User-Facing Translation Layer (GOLDEN RULE)**: Bans all astrology jargon from user-facing output.

### Engine Lineage (Phases 2.2 → 2.5.11, May 2026 — consolidated)
Five timing engines (Health v1, Marriage v2.4, Finance v1, Travel v1, Baby v1) all follow the same pipeline (FILTER → VERIFY → KP-GATE → ACTIVATE → TRIGGER): D1 lean filter → D9 dignity → divisional chart (D7 babies / D2 finance / D4 travel) → KP CSL of topic cusps → weighted ranking → dasha activation (AD=5, PD=6, MD=1) → transits/yogas. Each engine emits a `▸ <TOPIC>` row to `locked_facts.py` + mandatory `llm_directives` + thread-local cache. Every engine has a paired `test_<topic>_engine.py`.

**Per-engine specifics**:
- **Health v1** (post-2.5.11.11): 6-step (D30 + transits + SAV all permanently removed). Weights D1·40 + D9·30 + KP·20 + karaka·10. KP CSL of 6/8/12 cusps + active-CSL gate + dusthana convergence in `at_risk_planets` ranking.
- **Finance v1** (post-2.5.11.12-b): 5-step (D2 Hora + transits + SAV + Yogas removed). KP CSL of 2/5/9/11 acts as **hard final-filter** with safe fallback. Weights D1·40 + D9·30 + KP·20 + karaka·10.
- **Travel v1**: full 8-step. KP-dasha gate (NL→SB→SS chain hits on 3/9/12) boosts MD/AD/PD windows independently of D1 ranking. Past-window lookback (15y, MD-diversity cap of 4 in top 12).
- **Marriage v2.4**: multi-format DOB parsing + practical-age floor for marriage windows + specific-partner synastry bridge.
- **Baby v1** (2.5.0→2.5.10): cross-chart filter (D1∩D9∩D7), KP 2-5-11 significator gate, K.N.Rao Double Transit, gestation-aware re-rank, mandatory `NO_GENDER_PREDICTION` directive (PCPNDT compliance).

**Shared helpers** (`event_timing/_shared/`):
- `kp_significator_scan.py`: `compute_kp_planet_scan(kp, domain, in_filter_set)` — scans all 9 vimshottari planets across 6 domains via `CONCERN_HOUSES` preset, returns layered houses + `delivers ∈ {STRONG,PARTIAL,ABSENT}` + audit-flag for engine-dropped-but-delivers planets. `kp_promote_survivors(d1_map, kp, domain, threshold=2)` — data-adaptive STEP1 promotion (mutates `d1_map` to set `in_filter=True` for any planet whose KP chain signifies ≥threshold domain houses; engine's hardcoded floor retained as safety net).
- `double_transit.py`: `check_double_transit(kundli, target_date, lagna_si, planets_d1, concern_houses)` — K.N.Rao Jupiter+Saturn classical rule. Returns `{verdict, score 0-100, anchors[]}`. Vedic full-strength aspects (J: 5/7/9; S: 3/7/10) checked vs concern-house signs AND concern house-lords' natal signs. **Caveat**: empirically validated as reliable for life events (marriage/baby/career) but FAILED on travel for one ground-truth chart (Phase 2.5.11.15-c) — needs framework re-design before broader rollout.

**Companion modules**:
- **Remedy Engine v1.1**: hybrid 3-tier (PRACTICAL → AYURVEDIC → VEDIC last) covering health/marriage/career/money/business × 9 grahas; anti-superstition rules, every entry carries KPI + cost + caveats.
- **Dasha Timeline Block**: `_format_dasha_block` emits current MD/AD/PD + every dasha transition over next 5 years; handles both `{planet,startDate,subDashas}` and `{lord,start,antardashas}` shapes; hard-cap 60 lines.
- **Ask-flow Sensitivity** (Phases 2.5.11.4-.6): `_SENSITIVE_STATIC_RE` covers parent-illness/anxiety/panic/suicidal/depression/addiction/job-loss; `_LONG_STORY_RE` for "N saal se" pattern; `_MARRIAGE_DOMAIN_RE` with 6 framing protocols; `_SPECIFIC_PARTNER_RE` routes "mere BF/wife/pati" Qs to real synastry (when partner kundli saved) or `requires_partner_profile` CTA.

### Active Development (last 3)
- **Kundli Milan Pro PDF — Phase 2.5.11.23 (May 8 2026)**: User mandate "premium relationship truth report — 24 pages, hidden Vedic+KP fusion engine + foreground human-truth prose, gpt-4o premium". **5 new modules** under `vedic/compat/`: (a) `d9_marriage.py` — D9 lagna/7H/Venus/7L per partner + cross-sync `score_0_10`, reuses `varga_deep`. (b) `synastry_7l.py` — each partner's 7L planet placed in OTHER's D1 (sign+house+aspects) + Venus/Jupiter overlays + nakshatra resonance. (c) `kp_marriage_promise.py` — 7th-cusp sub-lord significator chain returning `verdict ∈ {STRONG,PARTIAL,WEAK,UNAVAILABLE}` (STRONG iff signifies 2+5+11; WEAK if only 6/8/12). Couple wrapper emits `couple_verdict`. (d) `chapter_scores.py` — pure deterministic 7-chapter scores 0-10 (Emotional/Trust/Communication/Stability/Chemistry/Family/Future) from weighted koot+engine facts; **never LLM**. (e) `premium_chapters.py` — `polish_premium_chapters()` calls `gpt-4o` (NOT mini, env `COMPAT_PREMIUM_MODEL`, gated by `COMPAT_PREMIUM_POLISH=1`) for the 7 chapters' 3-block prose (`kya_dikh / kya_matlab / kya_dhyan` + grounding) + hidden_truth + special/damage/practical/verdict. Two-tier cache (in-process LRU + DB `KundliMilanCache` reused with `prem_` prefix); fingerprint includes `_PREMIUM_VERSION="p1"` + model so any policy/model bump auto-invalidates. Validator: koot fact-lock + per-chapter `score X/10` whitelist + chapter-key shape (`ch1..ch7`). **Always-safe fallback** — any LLM/JSON/validator failure returns deterministic chapter prose so user never sees an error. **`milan_pdf.py` extended ~580 lines** with `render_milan_pro_pdf()`: fixed 24-page A4 skeleton (P1 cover, P2 snapshot, P3 hidden-truth, P4-17 seven chapters × 2pp each, P18 special, P19 damage, P20 practical, P21 koot-decoded, P22 timing-sync, P23 verdict, P24 closing) — reuses `_cover_page`, `_snapshot_page`, `_bullets_page`, `_chapter_eyebrow`, `_chapter_title_block`, `_grounding_card`, `_font_pair`, `_is_manglik` from earlier 12-page renderer. **New endpoint** `POST /api/kundli-milan/pro-pdf` body `{milan, kundli_p1, kundli_p2, lang}` — assembles new engine outputs, calls polisher, renders 24pp PDF; bad-payload guard returns 400 `expected_milan_payload`. **Architect-driven hardening (4 blind spots fixed in same patch)**: (a) **Chapter-key contract drift** — polisher emitted `ch1..ch7` keys but renderer's `_PRO_CHAPTER_MAP` looked up canonical `emotional_compatibility/trust_loyalty/...` keys → P4-17 silently fell to placeholder text. Renderer now uses `by_key.get(canon) or by_key.get(f"ch{i}")` index-fallback so any future schema drift cannot regress to placeholders. New integration test (`test_end_to_end_polisher_to_renderer_chapter_keys_match`) renders fallback chapters + uses pypdf to extract real text and asserts placeholder string is absent + at least one fallback prose phrase reaches the page. (b) **Jargon leak** — `_kp_signature_line()` previously emitted `"7CSL signifies houses [...]"` to the P3 grounding card, violating the no-astrology-jargon rule. Replaced with plain-language band map (`STRONG/PARTIAL/WEAK → "Both charts carry a clear/mixed/faint marriage signal underneath"`). Same regression test asserts `"CSL"` and `"signifies houses"` never appear in extracted PDF text. (c) **Endpoint error leakage** — `/api/kundli-milan/pro-pdf` previously returned `{"error":..., "detail": str(exc)}` exposing internal/OpenAI error text to clients. Now returns generic `{"error":"pro_pdf_render_failed"}`; full exception still logged server-side. (d) **`chapter_scores._ch7_future` numeric coercion** — `pct = total/36` previously assumed `total` was numeric; malformed/string totals could throw or push `total_pct >100`. Now coerces via `float()` with try/except and clamps `total ∈ [0, 36]`. **63 unit tests green** (5 new test files + integration regression). **Live 3-lang smoke GREEN** post-restart on Vikram+Sanya 19.5/36: en=35890B/24pp, hi=57758B/24pp (Devanagari), hn=35921B/24pp; bad-payload→400.
- **My Reports — Local PDF Registry — Phase 2.5.11.22 (May 8 2026)**: User mandate "More me ek astrovastu pro he uske niche my reports — saari PDFs (Milan, Numerology, AstroVastu Pro, Business Vastu, Face Reading) ek jagah save honi chahiye". New `lib/localReports.ts` (AsyncStorage `cosmic.localReports.v1` + `documentDirectory/reports/`): `saveLocalReport`, `listLocalReports`, `deleteLocalReport`, `shareLocalReport`, `openLocalReport`. Wired into every PDF download path: `pdfLanguagePicker.openPdfUrl(url, meta?)` auto-saves after successful download (covers astrovastu-pro, astrovastu-pro-result, business-vastu); `numerology.tsx` adds direct `saveLocalReport` after its in-flow `FileSystem.downloadAsync`. `app/my-reports.tsx` extended with new "Saved on this device" section above the existing server-side history (Open / Share / Delete buttons per row, kind-icon + title + subtitle + KB + date). `app/(tabs)/profile.tsx` MY DATA section gains "AstroVastu Pro" → `/astrovastu-pro-options` and "My Reports" → `/my-reports` rows above the existing My Kundli row. **Architect-driven hardening (4 blind spots fixed in same patch)**: (a) **Broken-entry leak** — `saveLocalReport` previously wrote `localUri: dest` even when `copyAsync` failed → user taps Open and gets nothing. Now verifies `getInfoAsync(dest).exists`; falls back to `sourceUri` if dest missing; **refuses to register** if neither URI is readable. (b) **AsyncStorage race condition** — non-atomic read-modify-write would drop entries on concurrent saves. Added per-process async mutex `withWriteLock` wrapping every save/delete/prune. (c) **My-reports error-state hides local items** — old `error ? <center/> : ...` early-return blanked the screen on API failure even when local PDFs existed. Rewritten: error-only screen now shows only when `localItems.length === 0`; otherwise a slim red error banner appears at the top of the list above the local section so user keeps access to their saved PDFs. (d) **Disk-bloat orphan files** — successful copies now best-effort-delete the original cache copy when `sourceUri` lives in `cacheDirectory`. Bonus: `listLocalReports` self-heals — silently prunes registry entries whose backing file disappeared (e.g. OS upgrade wipes cacheDir), persisted under the same lock so concurrent writes can't resurrect dead entries. **Branding rule preserved**: empty-state copy says "Powered by Advanced Cosmic Intelligence" semantics (Hinglish: "Aapke saare generated PDF reports … yahan automatically save honge"); never names AI/LLM. **Verified**: mobile typecheck clean, expo workflow restarted clean (web bundle 18s, 1832 modules). Milan PDF auto-save not yet hooked because mobile has no `/api/kundli-milan/pdf` trigger button — out of scope for this turn.
- **Kundli Milan PDF Premium 12-Page Redesign — Phase 2.5.11.21-C (May 8 2026)**: User mandate: redesign milan PDF to a premium "Cosmic Relationship Blueprint" (emotionally intelligent, modern, screenshot-worthy). `milan_pdf.py` rewritten to a **fixed 12-page A4 skeleton**: P1 cover (couple names, score+grade card, relationship-type tag, 3 pill tags, manglik banner), P2 snapshot (soul-summary + 3 indicator cards + 3 derived pill tags + 8-koot badge strip), P3-P8 six deep-schema chapters one per page (emotional_alignment / trust_loyalty / conflict_patterns / commitment_strength / marriage_stability / future_direction — each with eyebrow numbering, gold rule, body, optional grounding card), P9 "What Makes This Bond Special" (top 3 strong koots + first strength bullet), P10 "What Can Quietly Damage" (low/zero koots + first challenge + manglik warning), P11 "Practical Life Together" (3 score-band paragraphs + manglik para + marriage_stability snippet), P12 "Final Outlook" (future_direction + closing line). New helpers: `_hex`, `_canon_koot_key` (alias map: `vasya→vashya`, `maitri→graha`, `bhakut→bhakoot`), `_is_manglik` (single source of truth across cover/snapshot/derived pages), `_relationship_type_tag`, `_relationship_tags`, `_KOOT_STRENGTH_LANG`/`_DAMAGE_LANG` (8-entry maps), `_derive_special_bullets`, `_derive_damage_bullets`, `_practical_paragraphs`, `_final_paragraphs`, `_gold_rule`, `_cover_page`, `_chapter_eyebrow`, `_chapter_title_block`, `_grounding_card`, `_chapter_page`, `_bullets_page`, `_snapshot_page`, `_CHAPTER_MAP`. Pages 9-12 derive entirely from existing payload — **zero new LLM calls, zero new cost**. **Architect-driven hardening (3 blind spots fixed in same patch)**: (a) Variable page-count drift — chapters now ALWAYS emit 6 pages via legacy-fallback shim + deterministic placeholder, page count locked at 12 for deep / legacy-only / empty payloads (verified 12/12/12). (b) Koot key mismatch — real `/api/kundli-milan` payloads use `vasya/maitri/bhakut` not the canonical `vashya/graha/bhakoot`; `_canon_koot_key` maps via `_KOOT_KEY_ALIASES` with label-fallback + first-word fallback, so all 8 koots now produce derived bullets. (c) Manglik check inconsistency — old code checked `payload["manglik_dosh"]` only on derived pages but `manglik_dosh OR p1.manglik OR p2.manglik` on cover; unified through `_is_manglik` everywhere. **Tests** (`test_milan_pdf.py`): 4 original + 4 new regressions (`test_empty_payload_emits_exactly_12_pages`, `test_legacy_only_payload_emits_exactly_12_pages`, `test_canon_koot_key_resolves_real_payload_aliases`, `test_is_manglik_unified_across_partner_flags`) — 8/8 green. **Live verified** 3x post-restart: en (Vikram+Sanya 21.5/36 → 20.4KB / 12pp), hi Devanagari (Arjun+Meera 26.5/36 → 45.0KB / 12pp with NotoSansDevanagari Medium+ExtraBold embedded), hn Hinglish (Rohit+Anjali 12.5/36 → 20.4KB / 12pp). P9 bullets render "Nadi/Gana", P10 renders "Bhakut (Dosha)/Varna (Dosha)/Yoni (Low score)/Manglik energy" — confirming alias map works on live data. Mobile screen unchanged (PDF download button still posts cached milan response back).

**Earlier consolidated** (Phases 2.5.11.4 → 2.5.11.21-B): Ask-flow sensitivity + marriage psychology + specific-partner synastry bridge (.4-.6); Health/Finance KP convergence waves (.9-.12-b — Health D30 removal, KP 3-layer at-risk, Finance KP-as-final-filter); Travel past-window lookback + architect follow-up (.14, .14-b); Universal Double-Transit K.N.Rao shared helper (.15, .15-c — empirically failed on travel, kept as universal mandate); Travel Moon-floor + KP-Dasha-Gate + diversification (.16); KP-Driven STEP1 Auto-Promote `kp_promote_survivors` across all 5 engines (.18); Ask Q&A persistence on raw passthrough sync+stream exits with `answer_text/answer_source` (.19); Kundli Milan LLM Prose Polish HYBRID layer with gpt-4o-mini + 7-rule validator + LRU cache (.20); persistent two-tier DB cache `KundliMilanCache` + token trim (.20-A); validator multi-word nakshatra fix + dynamic max_tokens for non-Latin scripts (.20-B); deep 7-section schema with normalized digits + non-Latin Latin-anchor skip (.21); 7-section milan PDF download endpoint `POST /api/kundli-milan/pdf` with NotoDeva auto-registration (.21-B). All entries in git history.

## Product
- **Mobile App**: Daily Rashifal, Panchang, Kundli Milan, Muhurat finder, Numerology, Remedies, Vastu tips, Dosha analysis, personalized forecasts ("Risk Radar"), Life Mastery Report.
- **AI-driven Insights**: AI-powered interpretations with anti-hallucination pipelines, RAG for classical Vedic knowledge, and structured output modes.
- **Localization**: Supports 25 languages with region detection.

## User preferences
- The user wants the AI to act as a Replit coding agent.
- **Strict development methodology** for every new engine: `engine.py file (sahi folder mein)`, `Unit test (test_engine.py)`, `locked_facts.py wiring`, `Rule entry in system prompt`, `Deterministic post-injector`, `3x smoke test on real chart (/tmp/k.json, /tmp/req.json, /tmp/qm.json)`, `Architect review (code_review skill)`, `replit.md entry` are all mandatory.
- The user has provided a clear roadmap for project phases and expects strict adherence. Deviations require explicit user confirmation.
- The user expects the AI to avoid hallucinating, especially regarding timing-related questions.
- **UNIVERSAL TIMING RULE (Phase 2.5.11.15, MANDATORY for ALL engines)**: For ANY event-prediction question — past, present, OR future ("kab hoga / kab gaya tha / kab jaaunga / when") — Jupiter+Saturn DOUBLE TRANSIT (K.N.Rao classical rule) is COMPULSORY on the **concerned house AND concerned house lords**. Dasha alone is never sufficient. Use shared helper `event_timing/_shared/double_transit.py::check_double_transit(...)`. Concern-house presets (single source of truth): `travel=[3,9,12]`, `health=[1,6,8,12]`, `finance=[2,5,9,11]`, `marriage=[7,2,8,11]`, `baby=[5,11]`, `career=[10,6,2,11]`. Every timing engine MUST annotate past_windows / current_window / next_windows with `double_transit` field AND append the `DOUBLE_TRANSIT_TIMING_RULE_APPLIED` directive. **Caveat**: empirically failed on travel ground-truth (Phase 2.5.11.15-c) — rule is RETAINED but framework re-design pending before health/finance/marriage rollout.
- The user wants the AI to integrate classical Vedic knowledge through RAG for opinion-based questions, but strictly NOT for timing questions.
- Brand voice: "Powered by Advanced Cosmic Intelligence". AI is never named; AI = language layer only; backend = facts; post-injectors = mandatory citation safety net.
- The user prefers deterministic post-injectors to ensure mandatory facts are cited even if the AI misclassifies a topic.
- The user prefers that the AI refine language for opinion questions but must cite engine facts.
- Mandatory disclaimers per domain: medical (banner + 3-confirmation rule for severity tiers), financial (banner + tiered risk profile), remedies ("SUPPLEMENT, never substitute action" + free + paid alternatives), Astrocartography ("energetic affinity, not guaranteed luck"), and ethical/safety disclaimers for sensitive topics (lifespan, death prediction, destiny vs. guidance).
- Numerology PDF Pro branding: hybrid numerology + Vedic Astrology content, framed with numerology language.

## Gotchas
- **Static Gate Parity**: Any new engine/gate added to `/api/ask` MUST simultaneously be added to `/api/ask/stream`.
- **Cache Invalidation**: Any policy change requires bumping the cache namespace (`_vX`).
- **Vedic Vocab Stripping**: The validator strips Vedic terms unless explicitly opted in or the user's question contains a "tech-request" keyword.
- **Topic-Lock Conflicts**: When multiple injection points compete, the latest injected point in the user message order wins.
- **DB-Load Enforcement**: For authenticated users, `kundli` is always loaded from the database; client-supplied `kundli` is for anonymous demo mode only.
- **LLM Temperature**: The `temperature` parameter is often rejected by `gpt-5` models.
- **Telemetry Best-Effort**: Telemetry inserts are wrapped in `try/except` and silently drop rows on contention.
- **Anon Q&A**: `UserQuestion.user_id` is NOT NULL — anonymous Asks are intentionally NOT persisted. Add a separate telemetry table if anon analytics needed.
- **DB Migrations**: `db.create_all()` does NOT add columns to existing tables. Use `ALTER TABLE` directly when adding columns.
- **Naive datetime drift**: `datetime.utcnow()` + `_parse_iso()` may drift on `±HH:MM` ISO offsets across all 5 engines (deferred — needs monorepo-wide tz normalization).

## Pointers
- **Relevant Skills**: `code_review`, `telemetry`, `test_generation`
- **External Docs**:
    - [pnpm workspaces](https://pnpm.pnpm.io/workspaces)
    - [Expo React Native](https://docs.expo.dev/)
    - [Flask Documentation](https://flask.palletsprojects.com/en/latest/)
    - [Pyswisseph Library](https://pyswisseph.readthedocs.io/en/latest/)
    - [OpenAI API Documentation](https://platform.openai.com/docs/api-reference)
